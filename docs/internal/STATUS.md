# STATUS - agent-skills-toolkit

> The single live source of truth for "where are we / what is next." Keep this current; treat session logs as history, not roadmap.
> **For the linear, value-framed narrative of what shipped and why (engineer + non-engineer), read [`RELEASE-HISTORY.md`](RELEASE-HISTORY.md) - this file is the dense accretive tracker; that one is the readable through-line.**
> Last updated: 2026-07-27 (roadmap reconciled against the repo; see the note under Prioritized roadmap). Strategic context: [`release-plans/plan_v1.0.0/RELEASE-PLAN.md`](release-plans/plan_v1.0.0/RELEASE-PLAN.md) (v0.2). Decisions: [`release-plans/plan_v1.0.0/DECISIONS-OPEN.md`](release-plans/plan_v1.0.0/DECISIONS-OPEN.md) + the 2026-05-30 audit (`release-plans/plan_v1.0.0/audit/`).

## Current state

- **Declared tier:** Advanced (Gold). `library.json` `tier: advanced`, `standard: 0.12`, version `1.9.0`. Self-proving: the toolkit passes its own Gold gate (30-check spine, **601 tests, 24 skills**).
- **v1.9.0 (standards watch + the decisions discipline) CUT (2026-07-27):** maintainer-approved work outside the uplift program, taking the v1.9.0 number because a new skill is a MINOR. **The program renumbers: marketplace scope becomes v1.10.0, manage-and-studio becomes v1.11.0.** Two PRs: **#175** `askit-standards-watch` (skill 24) and **#174** the ADR `## Implementation sites` convention. `askit-standards-watch` discharges STANDARD.md sec 6's normative MUST that nothing implemented; the pin lives at `docs/internal/standards-watch/upstream-pin.json` and its unit is a **git blob SHA-1 per normative artifact**, because the upstream publishes no tags or releases and its HEAD moves on unrelated commits while `specification.mdx` sat unchanged 2026-05-16 to date. It imports only `readFileSync`, writes only to stdout, and a test fails the build if any write API or `child_process` appears. It detects and locates reliably and **refuses to judge materiality**, demonstrated by a worked example replaying a real upstream change a table-only differ would have missed. **THE DISCIPLINE FOUND A GAP ON FIRST USE:** applied to ADR 0038 (written the previous day), the mandated grep surfaced **four HTML render sites that fix had missed** - the exec paragraph, masthead, ID strip, and metadata section were still emitting a blank tier and a literal `null (null)`. Fixed with two new tests. **CodeQL found `js/incomplete-sanitization` a fourth and fifth time** in the new code, written by a third independent author; the three copies are collapsed into `scripts/lib/md-escape.mjs`, since the earlier "revisit if a third appears" condition was met. Spine 30 / Standard 0.12 unchanged; 563 -> 601 tests. **Marketplace re-pin STAGED, not executed.**
- **v1.8.0 (deep builders, measured advisory) CUT (2026-07-26):** R2 of the uplift program. R3 and R4 remain PENDING. Eight PRs: **#166** SP2a craft docs (4 stubs of 22-41 lines became real craft guides), **#171** SP2b examples (**all 13 builders now ship examples; before this release not one did** - 25 documents, 7 runnable artifacts, every runnable one executed during review), **#169** F3 seeded-defect fixture (9 planted defects + 3 bait entries, gate-clean so it measures the judgment layer not the gate), **#172** F3 `advisory-score.mjs` (precision/recall, dispatches no model, proven three ways), **#167** corpus batch 3 (4 new targets, readings 18-25), **#165** `covers.skill` triggering eval sets (the one eval form that had never existed on disk), **#168** ADR 0038, **#170** the hook craft-doc correction. F5 authoring rows are now MEASURED with four caveats. **516 -> 561 tests; spine 30 / Standard 0.12 unchanged.** Release packet: `docs/internal/release-plans/plan_v1.8.0/`. **HEADLINE FIX, ADR 0038:** the conformance report invented a declared tier from the earned one, so a plugin declaring nothing rendered "declares the Gold (Advanced) tier and earns Gold" and the verdict card always read "matches its declared tier". A false PASS on the artifact third parties are shown; the terminal had been honest about the same bytes all along. ADR 0030 decided this two months earlier and the guard landed only in `tier-report.mjs`. **FOUR PLAN ITEMS WERE UNBUILDABLE AS WRITTEN** and are corrected in the R2 plan so they cannot be re-executed: the evals premise was stale (7 instances existed; the real gap was `covers.skill`), "and G3 grades them" is unsatisfiable (`G3`s skill branch is a no-op by design), F5 Delta 2s "record via the F2 path" is not expressible (F2 requires a pinned corpus target; an authoring run has none), and the named effort cells are not settable (the Agent tool has no effort parameter). **DEFERRED:** the defect-rich model triple (R-AQ-3) needs real dispatches and was not faked; `U5` is mathematically unpassable in French (reading 18, ADR candidate); three F2 runner defects (readings 20/22/23). **Marketplace re-pin STAGED, not executed** - AU-2 reverted to its default after the v1.7.0 instruction.
- **v1.7.0 (trust and craft) CUT (2026-07-26, this release branch):** R1 of the uplift program, authorized by maintainer instruction; R2-R4 remain pending. Six PRs: **#149** CI hardening (Dependabot, Node `[22.12.0, 24]` matrix, blocking `npm audit --audit-level=high`, concurrency group, npm cache, SHA-pinned third-party actions, c8 report-only, CodeQL; the no-validation-logic-in-YAML invariant held); **#156** H1 docs (comparison re-verified 2026-07-26, new `docs/reference/subagents.md`, QUICKSTART html example, E12 synced, template design references committed, STATUS cross-repo note, U6 message wording); **#158** H1 code (`scripts/check-readme-version.mjs` drift guard wired into `npm test`, `U12`/`U13` conditional `N/A`); **#157** SP1 the builder craft pass (ADR 0037); **#160** F2 the eval-run pipeline (backlog E11); **#161**/**#162** two CodeQL high-severity escaping fixes. H1.6 (branch prune) done by the orchestrator: 20 remote branches deleted, each verified against a MERGED PR rather than a tree diff (the three-dot diff is wrong for squash merges and the two-dot diff counts older file versions as insertions). **Spine 30 / Standard 0.12 unchanged; 442 -> 516 tests; gate Advanced 0/0 at every merge.** Release packet: `docs/internal/release-plans/plan_v1.7.0/`. **H1.9 REJECTED on evidence:** its premise ("`site/src/content/docs/catalog.md` is a gitignored build artifact") was false on every clause - the file is tracked since v1.0.0, untouched by the generator, a published route with a named sidebar section, and drift-guarded by a test that calls it hand-authored. Executing it deleted a public page, its route, its sidebar entry and its guard (site 77 -> 75 pages); reverted, with the evidence table recorded in the plan so it cannot be re-executed. **CodeQL earned its keep on day one**, finding two genuine high-severity `js/incomplete-sanitization` defects in independently-written code (`report-render.mjs` and `eval-run-aggregate.mjs`): escaping the pipe without escaping the backslash first let `\|` in model-authored text break out of a Markdown table cell. **Branch protection was updated** to require `validate (22.12.0)` and `validate (24)`; the matrix renames the job, so the old required `validate` context would never have reported and every future PR would have hung. **SHIPPED 2026-07-26 (main `8340a6f`):** release PR #163 (squash `8340a6f`, all five checks green) -> tag `v1.7.0` at `8340a6f` -> `release.yml` minted the GitHub release behind the version-consistency guard (live, **Latest**) -> **marketplace re-pin EXECUTED** (maintainer lifted AU-2 by explicit instruction this session; the default reverts to stage-do-not-execute): `agent-plugins` PR #55, entry sha `cd12e10` -> `8340a6f`, entry version 1.6.1 -> 1.7.0, registry `metadata.version` 1.39.0 -> **1.40.0**, `validate-registry` green -> install resolution smoke-verified (marketplace.json -> `8340a6f` -> `plugin.json` 1.7.0). The v1.6.1 re-pin was executed earlier the same session as `agent-plugins` PR #54 (registry 1.38.0 -> 1.39.0), which also backfilled a missing `[1.37.0]` registry CHANGELOG entry. **NEXT:** R2 (v1.8.0 "deep builders, measured advisory") remains PENDING the maintainer's go, as do R3 and R4. Seven Dependabot PRs are open and deliberately unmerged, including two major action bumps; they want review, not an unreviewed pre-release merge.
- **v1.6.1 (the trust patch) CUT (2026-07-25, this release branch):** the 2026-07-19 coupled portfolio audit (`_local/audit/2026-07-19_fable_pop/`, gitignored) pointed the shipped v1.6.0 gate at five real product-on-purpose repositories under both profiles and hand-verified every finding. It exposed the grader over-flagging valid Mermaid and an output layer that misled three independent assessors in one day. **ADR 0036 (calibrate U12 diagram grammar + U6 template slots)** fixes both. **U12:** `bracketsBalanced` was a generic character walk, correct for `flowchart` and wrong for the two diagram types that overload the bracket characters - `sequenceDiagram` async arrows (`-)`, `--)`) and `erDiagram` cardinality (`||--o{` and the `|o || }o }|` / `o| || o{ |{` family). It now consults a per-diagram-type syntax-token table, **scoped by diagram type** (a stray `-)` in a flowchart still fails) and applied **rescue-only** (a closer is ignored only when the stack is already empty), which preserves ADR 0032's one-directional-safety invariant: no previously-passing diagram can newly fail. **U6:** a link target carrying a substitution token (`{{docs_path}}/guide.md`, `{release-url}`) is a template slot, not link rot - this answers PSR-3's open "what marks template intent" question with the token at the point of use, deliberately NOT a filename convention or a frontmatter flag a third-party plugin would have to adopt. **Display pair (PSR-6/7, presentation only):** above-declared-tier findings now print under a header stating they cannot affect the grade or exit code, and a pinned plugin gets a one-line Standard-debt indicator (count + the version at which the debt comes due). **Measured on the audit corpus** (`--profile plain-plugin`): pm-skills **56 -> 43** errors, pm-skills-mcp **18 -> 14**, portfolio U12 **14 -> 3** - landing exactly on the real-defect counts the audit hand-verified independently, before the fix existed. TDD RED-first throughout; **418 -> 442 tests**, including four guards proving the U12 allowance does not leak outside the diagram type that owns it. No Standard implication: spine 30 / Standard 0.12 unchanged, both checks keep `objective` provenance, gate Advanced 0/0. Two directional trade-offs are stated explicitly in the ADR rather than left silent (an accepted false negative on `(x -)`, and one erDiagram input that previously passed by accident). **Deliberately NOT in this patch:** the report-masthead analog of the debt line (the renderer has golden snapshots and a larger surface; queued as a v1.7.0 rider), and the comment-only-mermaid exemption (rejected with rationale - those blocks genuinely fail to render). **SHIPPED 2026-07-25 (main `cd12e10`):** release PR #145 (squash `cd12e10`, both CI jobs green) -> tag `v1.6.1` at `cd12e10` -> `release.yml` minted the GitHub release behind the version-consistency guard (live, **Latest**). Release folder: `docs/internal/release-plans/plan_v1.6.1/`. **The marketplace re-pin is STAGED, NOT EXECUTED** (`plan_v1.6.1/repin-instructions.md`): `agent-plugins` entry sha `c2bcbe2` -> `cd12e10`, entry version 1.6.0 -> 1.6.1, registry `metadata.version` 1.38.0 -> 1.39.0. Until the maintainer applies it the marketplace keeps serving v1.6.0, which is a no-op, not an outage. Landed behind a self-run 4-lens adversarial review (false-PASS / false-FAIL / determinism / contract-fidelity) whose one real catch was an imprecise false-negative description in the ADR itself, corrected and then pinned by two characterization tests.
- **v1.6.0 (manifest completeness, made actionable) CUT (2026-06-14, this release-prep PR):** the v1.6.0 cut = **F1 + F4**, both merged to `main` before the bump. **F1 (PR #140, ADR 0035):** a new Universal check **U13 (`skill-registration`)** comparing the skills a plugin registers in its enumerating manifest (`library.json` `components.skills`, else `.claude-plugin/marketplace.json` `plugins[]`) against the skill dirs on disk - on disk but unregistered ships invisibly to installers; objective + portable (survives `--profile plain-plugin`), distinct from `U8`. **The first Standard growth since U10 retired: spine 29 -> 30, Standard 0.11 -> 0.12**, and the first live exercise of ADR 0027's warn-for-one-minor burndown (U13 ships as `warn` at 0.12, graduates to `error` at 0.13). Verified against deanpeters (49 on disk, 47 registered -> exactly 2 flagged). **F4 (PR #141, E12):** a per-check report glossary (section 11, from `REPORT_META`, zero model tokens), the new `docs/reference/universal-checks.md` Bronze reference page, and a sub-600px responsive pass. Both behind clean adversarial reviews (F1 fixed a name-fallback masking hole + finished a count sweep; F4 both lenses clean). 418 tests, gate Advanced 0/0. Release plan packet: `docs/internal/release-plans/plan_v1.6.0/`. **FULLY SHIPPED 2026-06-14 (main `c2bcbe2`):** version-bump PR #142 (squash `c2bcbe2`, CI green) -> tag `v1.6.0` at `c2bcbe2` -> `release.yml` minted the GitHub release behind the version-consistency guard (live, Latest) -> marketplace re-pinned (`agent-plugins` PR #34: entry sha `7b1ba92` -> `c2bcbe2`, entry version 1.5.2 -> 1.6.0, registry `metadata.version` 1.23.0 -> **1.24.0**; `validate-registry` green) -> install resolution smoke-verified (marketplace.json -> `c2bcbe2` -> `plugin.json` 1.6.0). ADR 0035 ratified Proposed -> Accepted in the bump. A Codex pre-release adversarial review was attempted but its runtime crashed twice (an infra failure on this Windows/MCP setup, no defect findings; it surfaced one stale-draft nit in the packet README, fixed); the F1 and F4 changes carried clean Claude 4-lens adversarial reviews and release hygiene was verified clean before the cut. **Spine 30 / Standard 0.12, version 1.6.0, 418 tests, gate Advanced 0/0. NEXT per the roadmap: F2 (the dependable eval-run pipeline, E11), then F3 (advisory quality measurement) and F5 (authoring token measurements) as continuous supporting work; the marketplace-scope evaluation concept is the likely next headline.**
- **Self-validating:** yes. `node scripts/check.mjs` exits 0 (0 errors, 0 warnings); `tier-report` prints Advanced with an empty burndown (satisfies universal + convergent + advanced); `npm test` = 388/388 (verified 2026-06-10).
- **v1.4.0 (the designed evaluation report, F2 / E1):** the `report-render` library renders the one `evaluate.mjs` object as a self-contained HTML page or a Markdown twin in five report types (conformance, migration, release, review, behavioral) via `--format=md|html` and `--report`. A pure presentation layer: no new spine check, no Standard change (spine stays 29, Standard 0.11). Phases A (#111), B (#112), C (#113) all merged to `main` behind clean 4-lens adversarial reviews (the Phase C review caught and the fix closed a critical advisory-merge hole). Public reference: `docs/reference/evaluation-reports.md`. **SHIPPED `v1.4.0` (2026-06-09):** version-bump PR #114; tag `v1.4.0` at commit `da6eded` (`release.yml` minted the GitHub release behind the version-consistency guard; live + Latest), and the marketplace re-pinned to it (`agent-plugins` PR #20, registry `metadata 1.12.0`, entry version 1.4.0); install resolution smoke-verified (marketplace.json -> `da6eded` -> `plugin.json` 1.4.0). No new spine check and no Standard bump: spine stays 29, Standard stays 0.11. Supersedes `v1.3.0`.
- **v1.4.1 (hardening patch over v1.4.0):** a Codex adversarial review of the report renderer surfaced three edge cases on the advisory + migration paths, all fixed: a malformed advisory (missing `findings`/`cases`/`summary`) renders instead of throwing; the Markdown twin HTML-escapes advisory stamps and finding text so an untrusted advisory file cannot inject markup into a `.md`; an invalid `--target-tier` is rejected (CLI exit 2 + `migrateReport` throws) instead of producing an empty plan. The focused re-review then completed the Markdown escaping (insights + behavioral summary were still raw in the MD twin). Defensive hardening only - the advisory layer still cannot move the deterministic grade or the gate exit code. 346 tests (was 341, +5 regression); gate Advanced 0/0; no spine or Standard change (spine 29, Standard 0.11). **SHIPPED `v1.4.1` (2026-06-09):** PR #116 (the Codex fixes + version bump + the re-review's Markdown-escaping completion); tag `v1.4.1` at commit `01a5fac` (`release.yml` minted the GitHub release behind the version-consistency guard; live + Latest), and the marketplace re-pinned to it (`agent-plugins` PR #21, registry `metadata 1.13.0`, entry version 1.4.1); install resolution smoke-verified (marketplace.json -> `01a5fac` -> `plugin.json` 1.4.1). Supersedes `v1.4.0`.
- **v1.5.0 (outward grading):** the first eval-target corpus run (the toolkit graded against real third-party plugins - `anthropics/skills`, `obra/superpowers`, `wshobson/agents`, `lst97/sub-agents`; artifacts in gitignored `_local/audit/anchor-runs/2026-06-09/`) drove two changes that make the gate legitimate to point at plugins it does not own: (1) a **`--profile <name>`** flag on `check.mjs` + `evaluate.mjs` (PR #118) to grade a third-party plugin under a chosen profile without writing `askit.config.json` into its tree (mirrors `--mode`; unknown profile exits 2); (2) **U2** (root `AGENTS.md` anatomy) and **U5** (description scorer) reclassified `objective`/`vendor-cited` -> **house** (ADR 0029, PR #119), so `plain-plugin` drops them, the report counts them as profile conformance, and a published verdict no longer clamps them. The default `askit-library` grade is UNCHANGED (both still fire), so no plugin's pass/fail moves; spine stays 29, Standard stays 0.11, no new check. Net effect: `anthropics/skills --profile plain-plugin` drops from 23 findings to 1 (the real U3 description-over-spec-cap defect). A new `house-provenance.test.mjs` invariant keeps the `plain-plugin` off-set == the house-provenance set. 358 tests, gate Advanced 0/0. Release doc: `docs/internal/release-plans/plan_v1.5.0/RELEASE-PLAN.md`. **SHIPPED `v1.5.0` (2026-06-09):** version-bump PR #120; tag `v1.5.0` at commit `6bd2daa` (`release.yml` minted the GitHub release behind the version-consistency guard; live + Latest), and the marketplace re-pinned to it (`agent-plugins` PR #23, registry `metadata 1.15.0`, entry version 1.5.0; the PR also backfilled the missing 1.13.0/1.14.0 registry CHANGELOG entries); install resolution smoke-verified (marketplace.json -> `6bd2daa` -> `plugin.json` 1.5.0). Supersedes `v1.4.1`. Deferred: the U5 scorer recalibration (ADR 0029 follow-up) and corpus batch 2.
- **v1.5.1 (the batch-2 calibration patch) FULLY SHIPPED 2026-06-10:** corpus batch 2 was RUN (the gate pointed at the official Anthropic plugin set + four community marketplaces under `--profile plain-plugin`; artifacts gitignored at `_local/audit/anchor-runs/2026-06-10/FINDINGS.md`), and the verified competitive comparison shipped. Three landed changes plus one new finding, all from that run; the default `askit-library` grade is unchanged throughout (spine stays 29, Standard stays 0.11), and the toolkit's own Gold grade stays Advanced 0/0. (1) **Comparison (#122):** a public comparison page (`docs/explanation/comparison.md`) + a primary-source-verified research corpus (nine tool profiles + methodology + gap analysis under `docs/internal/research/`) + backlog intake E4-E10. (2) **ADR 0030 (#123):** U6 (reference-links) strips fenced blocks + skips non-filesystem schemes; U11 (mcp-valid) treats a typed-http empty-url server as a managed-connector warning, not a tier error; `tier-report` no longer asserts an askit tier for a plugin that never declared one. (3) **ADR 0031 (Finding 4, branch `fix/name-display-label-warn`):** U3 (frontmatter-valid) format-checks `name` only when it equals the directory (a divergent name is a display label / namespaced command; U4 owns the divergence), and U4 (name-matches-dir) is advisory under `plain-plugin` while staying a hard error under the default ladder. Verified against hookify (1 error -> 0) and gsd (165 -> 1, the 1 a genuine parse defect that correctly stays); 9 new tests, 375 total; landed behind a 4-lens adversarial review (all SHIP, which added 3 coverage-hardening tests + corrected the ADR's corpus counts). (4) **Finding 5 (ADR 0032):** U6 now ignores inline-code spans (single and multi-backtick) and U12 skips non-live mermaid (pure `{{PLACEHOLDER}}` template slots and fence-aware HTML-commented blocks). Measured on the C1 corpus, 43 errors -> 12 (the corrected number; the prior "43 -> 1" over-generalized - the 12 survivors are a murkier example-link/output-path class, 1 a genuine link-rot defect, deliberately not auto-suppressed). +13 tests. (5) **Token-usage dossier (Q2):** `docs/reference/token-usage-estimates.md` - the deterministic-core-vs-model-assisted split, explicit model/effort vs quality tradeoffs, assumptions and caveats. **SHIPPED `v1.5.1` (2026-06-10, main HEAD `6f1d8b0`):** version 1.5.0 -> 1.5.1 across the 5 manifests (+ INDEX), CHANGELOG `[Unreleased]` -> `[1.5.1]`, RELEASE-NOTES `## 1.5.1`, STATUS; 388 tests, gate Advanced 0/0, spine 29 / Standard 0.11. Release PR #125 (squash `6f1d8b0`) -> tag `v1.5.1` at `6f1d8b0` -> `release.yml` minted the GitHub release (live + Latest) -> marketplace re-pinned (`agent-plugins` PR #29: entry sha `6bd2daa` -> `6f1d8b0`, entry version 1.5.0 -> 1.5.1, registry `metadata.version` 1.18.0 -> 1.19.0; also backfilled the missing 1.18.0 registry CHANGELOG entry; validate-registry green) -> install resolution smoke-verified (marketplace.json -> `6f1d8b0` -> plugin.json 1.5.1). Release doc `docs/internal/release-plans/plan_v1.5.1/`. The Finding 5 work landed behind three `codex:adversarial-review` passes (codex-cli 0.137.0 worked this time via MCP-tool fallback): round 1 fixed a real soundness hole (global comment-strip reaching inside live fences), round 2 fixed multi-backtick inline code + a loose fence closer, round 3 fixed the dossier reproducibility wording and DECLINED two theoretical 4+-backtick fence edges with documented rationale (unreachable in real mermaid; a full CommonMark tokenizer deferred as not worth core-logic regression risk).
- **Eval-run record seeded + advisory token ranges MEASURED (2026-06-10, PR #127, main `a3da4fa`):** the first advisory measurement batch - five model-assisted runs (three `--report=review`, two `--report=behavioral`) across Opus/high, Sonnet/medium, Haiku/low against `skills/askit-evaluate`, the `documentation-pipeline` corpus plugin (cc-skills `2b1b61c`), `skills/askit-build-skill`, and two Anthropic skills (mcp-builder, pdf @ `5754626`). The tracked historical record now lives at `docs/internal/eval-runs/` (README conventions + `eval-runs.md` batch log; raw advisory JSON + rendered HTML gitignored under `_local/audit/eval-runs/2026-06-10/`), and `docs/reference/token-usage-estimates.md` moved its advisory ranges from "not yet measured" to MEASURED (one advisory run = roughly 33k-67k subagent tokens; cost dominated by target size, quality by model tier; the predicted Opus>Sonnet>Haiku quality gradient observed live). Seven toolkit sensor readings filed in the record with dispositions, the headline ones: `evaluate.mjs` component scope silently ignores `--profile` (calibration candidate), the behavioral `evals/` convention has zero on-disk instances anywhere (doc/spec fix candidate), `agents/askit-reviewer.md` names `check.mjs` where targeted `evaluate.mjs` is right (doc fix candidate), `askit-build-skill` declares a chain its procedure never instructs (skill improvement candidate), plus new U5/U6 calibration data. Docs-only: spine 29 / Standard 0.11, version 1.5.1, 388 tests, gate Advanced 0/0.
- **Eval-run batch 2 + methodology + E11 intake (2026-06-10, PR #129, main `d2407ec`):** three more review advisory runs on new model x effort cells against three public PM skill libraries - `RefoundAI/lenny-skills` @ `280a57a` (86 skills, Opus/medium, 77k tok; portably CLEAN under plain-plugin), `deanpeters/Product-Manager-Skills` @ `70fb6c4` (49 skills, Sonnet/high, 103k tok, 11E/12W baseline), `phuryn/pm-skills` `pm-toolkit` @ `d384f0c` (Haiku/medium, 55k tok; all 9 marketplace plugins clean). Advisory range now MEASURED at 33k-103k. New `docs/internal/eval-runs/METHODOLOGY.md` codifies the observe -> verify-against-ground-truth -> calibrate-via-ADR+TDD path; its proof case is sensor reading 8: the Sonnet/high advisory mis-triaged all 11 deanpeters U6 errors as checker false positives (CommonMark containing-file resolution proves the links genuinely 404 - U6 was RIGHT; verify before calibrate). Readings 8-13 add: S7 one-to-many command orchestrators (Standard-evolution note), systematic multi-corpus U5 under-scoring (50/86 lenny descriptions at exactly 0.65 from one template stem - the U5 recalibration TDD set is now multi-corpus), cross-skill trigger-surface redundancy as the advisory layer's distinctive value-add, manifest-vs-disk skill drift as an objective NEW-CHECK candidate (needs an ADR), and Haiku's high-leverage low-to-medium effort step. Backlog E11 (dependable eval-run pipeline) intakes the manifest/runner/dispatch-contract/record automation. Docs-only: spine 29 / Standard 0.11, version 1.5.1, 388 tests, gate Advanced 0/0. **Follow-up same day (PR #131, main `f53e137`):** R9 = the record's first same-target cross-model A/B (Opus/high vs R8's Haiku/medium on phuryn `pm-toolkit`, 100,320 vs 54,691 tok): Opus/high found knowledge-content defects Haiku declared absent (a wrong US privacy statute "VIPA" - Virginia's is the VCDPA - in the compliance skill; an inverted AmE grammar rule; a command whose 10-point scorecard contradicts its mapped skill's 10) -> readings 14 (a cheaper tier's "verified" is not verification) and 15 (command-vs-skill enumerated-content divergence is mechanically lintable); the record schema now carries evaluation scope (component vs plugin) explicitly. Plus the maintainer-reported renderer defect fixed: report tables now scroll horizontally at <=900px instead of crushing (CSS-only, golden snapshots regenerated; fuller responsive pass + the per-check explanation glossary intaken as backlog E12). 388 tests, gate 0/0, 9 runs / 15 readings total.
- **U5 recalibration SHIPPED + the same-target model triple completed (2026-06-11, PRs #133 + the records PR):** ADR 0033 (recalibrate U5 description scorer) landed TDD-first off the multi-corpus evidence: verb inflections + corpus-evidenced verbs + "whenever"/"if the user" triggers + the GDPR-acronym penalty replaced by a hard TODO/TBD/FIXME placeholder penalty (the inverse defect: "TODO: write a description" scored 0.9). Measured: U5 warns 98 -> 18 across five corpora, survivors sampled as the intended catch; +7 tests (395), threshold/severity/provenance unchanged, gate 0/0. The model triple (R10 Sonnet/high 71,796 tok + R11 Haiku/high 63,646 tok, identical prompt to R9 Opus/high) produced readings 16-17: the knowledge-content boundary is categorical Haiku-vs-frontier (Haiku at HIGH effort confabulated a statute correction and asserted false consistency), Sonnet/high matched Opus/high at ~70% cost with two additional verified majors (the XYZ+S definition split + tailor-resume scope creep, both re-verified by hand), and the two frontier finding sets only partially overlap (a two-frontier panel beats either alone). 11 runs / 17 readings.
- **v1.5.2 (the eval-run patch) CUT (2026-06-12, this release branch):** the roadmap's P0 component-scope fix + P1 doc-fix batch shipped as **ADR 0034 (resolve profiles in component scope, PR #135, main `1020298`)**: `evaluate.mjs` component scope runs the same `loadConfig` + CLI-override + `resolveFindings` pipeline as plugin scope (the flag was validated then silently dropped - sensor reading 1), with the skill directory as the ONE root for findings and config so file-scoped suppressions waive the gate exit (Codex adversarial review round 1's real catch; round 2 added the missing plugin-scope exit-code assertion; the nearest-enclosing-plugin-root suggestion DECLINED with rationale in the ADR). Doc-fix batch closed readings 2/3/6/7 (askit-reviewer -> targeted `evaluate.mjs`; behavioral derive-from-description fallback documented; behavioral `summary` counter semantics defined; askit-build-skill explicit delegate step; components 0.1.1). TDD throughout: +6 tests incl. the new `anti/component-config/linkrot` fixture, **401 total**, gate Advanced 0/0. Cut as **v1.5.2** bundling ADR 0033 (U5 recalibration) + ADR 0034 + the doc batch + the eval-run record/methodology/dossier + the responsive-table fix; spine 29 / Standard 0.11 unchanged. Release doc `docs/internal/release-plans/plan_v1.5.2/`. **FULLY SHIPPED 2026-06-12:** release PR #136 (squash `7b1ba92`, CI green) -> tag `v1.5.2` at `7b1ba92` -> `release.yml` minted the GitHub release behind the version-consistency guard -> marketplace re-pinned (`agent-plugins` PR #32: entry sha `6f1d8b0` -> `7b1ba92`, entry version 1.5.1 -> 1.5.2, registry `metadata.version` 1.21.0 -> 1.22.0; also backfilled the missing [1.20.0]/[1.21.0] registry CHANGELOG entries from the two thinking-framework-skills re-pins; validate-registry green) -> install resolution smoke-verified (marketplace.json -> `7b1ba92` -> plugin.json 1.5.2). Roadmap P0 item 1 and P1 item 5 are CLOSED; next per the roadmap: the manifest-vs-disk drift check ADR (P0 item 2, the v1.6.0 headline), then E11 (the eval-run pipeline) paired with the advisory quality measurement and corpus batch 3.

## Prioritized roadmap (2026-06-11, maintainer + eval-run evidence)

> **Reconciled against the repo 2026-07-27.** The rows below were written 2026-06-11 and never revised while six weeks of releases landed, so four of them described shipped work as open and one made a claim the repo contradicted. Every row is now marked with what was verified and how. **Net effect: P1 and P2 are empty except one measurement.** The reconciliation itself is the finding - a roadmap is a document asserting facts about a repo, and nothing was checking it.
>
> | Row | Was | Verified | Evidence |
> |---|---|---|---|
> | 3 - E11 pipeline | open | **SHIPPED** (v1.7.0, F2) | `scripts/eval-run.mjs`, `scripts/lib/eval-run.mjs`, `scripts/lib/eval-run-aggregate.mjs` on disk; `corpus.json` carries 8 pinned-sha targets; relocation addendum delta log records it as landed in Release 1 |
> | 4 - advisory quality | open | **HALF** | harness shipped (`scripts/lib/advisory-score.mjs`, `tests/fixtures/anti/seeded-defects/`); the measurement never ran - zero seeded-defect rows in `eval-runs.md`. The open half is exactly backlog E13 |
> | 5 - doc-fix batch | open | **SHIPPED** (v1.5.2) | all four items present: `askit-reviewer` targets `evaluate.mjs`, the derive-fallback is documented in `askit-quality-grader`, `askit-build-skill` carries the delegate step |
> | 6 - E12 report UX | open, and asserted "Universal does not [exist]" | **SHIPPED** (v1.6.0, PR #141) | `docs/reference/universal-checks.md` created 2026-06-13 in commit `5ecdd89`; backlog E12 was already marked DONE and closed |
> | 7 - authoring token measurements | open | **SHIPPED** | `docs/reference/token-usage-estimates.md` carries "Authoring (`askit-build-*`): MEASURED, 2026-07-26" |
> | 8 - P3 carried | mixed | **corpus batch 3 RAN** (2026-07-27 batch recorded in `eval-runs.md`); marketplace scope still pending as R3; Gemini emitter genuinely unbuilt (only mention in the repo is a comment in `prefix.mjs`); E4-E10 still in the backlog; the Finding-5 murkier-residual decision is still open | |
> | PSR-4 / PSR-11 / PSR-12 | queued for the v1.7.0 H2 doc batch | **STILL OPEN** | each string appears only in this file and nowhere else in `docs/`, `skills/`, or `scripts/`; no `when_to_use` note, no orchestrator how-to, no embedded-skills note landed |
>
> **What is actually open after reconciliation:** E13 (run the defect-rich model triple) is the only remaining P1. P2 is empty. P3 is unchanged apart from batch 3. Plus E14 (U5 language ADR), E15 (three F2 runner defects), the three PSR doc riders, and the maintainer's go on R3 / R4.

1. ~~**P0 - Component-scope `--profile` fix** (reading 1): `evaluate.mjs` silently ignores `--profile` in component scope, so third-party single skills still get house checks.~~ **SHIPPED in v1.5.2** (ADR 0034 (resolve profiles in component scope), PR #135).
2. ~~**P0 - Manifest-vs-disk drift check, ADR first** (reading 12): objective new-check candidate that catches silent delivery failures.~~ **SHIPPED in v1.6.0 as U13 (`skill-registration`)** (ADR 0035 (manifest-vs-disk skill-registration completeness), PR #140); spine 29 -> 30, Standard 0.11 -> 0.12.
2b. **P0 - Outward-grading trust calibration** (2026-07-19 portfolio audit, PSR-1/2/3/6/7): **SHIPPED in v1.6.1** (ADR 0036 (U12 diagram grammar + U6 template slots)). U12 was false-positive on 79 percent of the portfolio's mermaid errors (sequence async arrows, ER cardinality); U6 flagged template-slot links; above-tier findings and pinned-Standard debt were invisible in the output.
3. ~~**P1 - E11 dependable eval-run pipeline** (backlog E11): pinned-sha target manifest + deterministic runner + advisory dispatch contract + record/aggregate automation. Multiplies everything below.~~ **SHIPPED in v1.7.0 as F2** (`scripts/eval-run.mjs` + `scripts/lib/eval-run.mjs` + `scripts/lib/eval-run-aggregate.mjs`; `corpus.json` carries 8 pinned-sha targets; corpus batch 3 was its first real shakedown and produced the three defects now filed as E15). Verified on disk 2026-07-27.
4. **P1 - Advisory quality measurement** (METHODOLOGY rigor items) - **HALF SHIPPED, and the open half is the last P1 standing.** ~~Seeded-defect advisory fixtures (planted issues + scoring key -> precision/recall per model x effort)~~ **SHIPPED** as F3 R-AQ-2: `scripts/lib/advisory-score.mjs` plus the `privacy-notice-toolkit` fixture and scoring key under `tests/fixtures/anti/seeded-defects/`. **STILL OPEN:** replicate the model triple on that structurally-defective target to test the Opus-vs-Sonnet parity claim. The harness and fixture both ship and are tested; the three real dispatches were never run, so `eval-runs.md` contains zero seeded-defect rows. Tracked as backlog E13. Do not simulate it - the entire value is that the number came from a real run.
5. ~~**P1 - Cheap doc-fix batch from readings 2/3/6/7:** askit-reviewer step 2 `check.mjs` -> targeted `evaluate.mjs`; document the behavioral derive-from-description fallback (the `evals/` convention has zero on-disk instances); define behavioral `summary` semantics; add the missing delegate step to askit-build-skill.~~ **SHIPPED in v1.5.2.** All four items verified present on disk 2026-07-27.
6. ~~**P2 - E12 report/template UX** (backlog E12): per-check explanation glossary (static from check metadata, zero model tokens) + a `docs/reference/universal-checks.md` page (the rubric's missing Bronze breakdown - Silver and Gold reference pages exist, Universal does not) + the fuller responsive pass.~~ **SHIPPED in v1.6.0** (commit `5ecdd89`, PR #141): all three deliverables landed, and backlog E12 was marked DONE and closed at the time. The parenthetical above was already false when this row was written - `docs/reference/universal-checks.md` has existed since 2026-06-13.
7. ~~**P2 - Authoring token measurements:** the dossier's last unmeasured range (`askit-build-*`).~~ **SHIPPED:** `docs/reference/token-usage-estimates.md` carries an "Authoring (`askit-build-*`): MEASURED, 2026-07-26" section. **P2 is now empty.**
8. **P3 - Carried:** marketplace-scope evaluation concept (the gate has plugin and component scopes only; a marketplace scope would validate marketplace.json, iterate member plugins, and check cross-plugin overlap - today handled manually per plugin) **- this is the uplift program's R3, pending the maintainer's go**; Finding-5 murkier-residual decision (ADR 0032 shipped the fix; the 12 deliberately-unsuppressed survivors are the open decision); ~~corpus batch 3~~ **RAN 2026-07-27** (four new third-party libraries, recorded in `eval-runs.md`, and the F2 runner's first real shakedown - it produced E15); Gemini emitter (genuinely unbuilt; the only mention anywhere in the repo is a forward-looking comment in `scripts/checks/prefix.mjs`); E4-E10 comparison backlog (still open in `enhancements.md`). **Next (as of 2026-07-25, post-v1.6.1):** every P0 row above is shipped. The open item is the maintainer's go-timing call on the four-release uplift program (v1.7.0 "trust and craft" through v1.10.0 "manage and studio"), whose packet sits at [`../execution/`](../execution/EXEC-SUMMARY.md) awaiting one word. The remaining audit riders that did NOT ride v1.6.1 are queued for its v1.7.0 H2 doc batch: the U5 `when_to_use` exclusion note (PSR-4), the orchestrator-commands-to-`_workflows/` how-to (PSR-11), and the embedded-skills cross-repo U6 pattern note (PSR-12); Gold-scale authoring automation (PSR-8/9) goes through the `askit-backlog` why-gate. Independent of that call: E11 (the pipeline), advisory quality measurement, corpus batch 3.

**Cross-repo dependency note:** The `agent-plugins` standards program may relocate `STANDARD.md` and the checker (`scripts/check.mjs`, `scripts/lib/`, `scripts/checks/`, `scripts/generators/`, `tier-report.mjs`) into `agent-plugins/standards/`. This repo plans around that move: new engine-adjacent code (the eval-run pipeline, craft-review helper) is built cleanly separable so only the `npm run check` seam repoints if the checker moves. If that program's PR-C ("askit re-adopt") fires mid-program, this program STOPS and reconciles against [`docs/internal/execution/relocation-addendum.md`](../execution/relocation-addendum.md) before continuing.

- **Installable:** yes - **SHIPPED `v1.2.0` (2026-06-06).** Tag `v1.2.0` at commit `c1ecd26` (`release.yml` minted the GitHub release behind the version-consistency guard; live + Latest), and the `product-on-purpose` marketplace re-pinned to it (`agent-plugins` PR #16, `metadata 1.8.0`, entry version 1.2.0); install resolution smoke-verified (marketplace.json -> `c1ecd26` -> `plugin.json` 1.2.0). v1.2.0 retires `U10` (no-dashes) for Standard `v0.11` and a 29-check spine. Supersedes `v1.1.0` (2026-06-03, tag `f3250c0`, registry PR #14) and `v1.0.0` (2026-06-02, registry PR #9). Install: `/plugin marketplace add product-on-purpose/agent-plugins` then `/plugin install agent-skills-toolkit@product-on-purpose`; an existing install updates from the marketplace. **SHIPPED `v1.3.0` (2026-06-06): the gate-evolution release (F1 standard-aware gate, ADR 0027, PR #107; F3 configurable gate, PR #108; version-bump PR #109). Tag `v1.3.0` at commit `d8279c2` (`release.yml` minted the GitHub release behind the version-consistency guard; live + Latest), and the marketplace re-pinned to it (`agent-plugins` PR #18, registry `metadata 1.10.0`, entry version 1.3.0); install resolution smoke-verified (marketplace.json -> `d8279c2` -> `plugin.json` 1.3.0). No new spine check and no Standard bump: spine stays 29, Standard stays 0.11. Supersedes `v1.2.0`.**
- **On disk:** 23 skills (the full v1 catalog: `askit-evaluate`; the `askit-build-*` family - skill, subagent, command, mcp, hook, chain-contract, agents-md, output-style, workflow, docs, samples, statusline, settings; governance/lifecycle - `askit-backlog`, `askit-decision`, `askit-release`, `askit-deprecate`; lifecycle/onboarding - `askit-migrate`, `askit-init-plugin`, `askit-init-marketplace`, `askit-template-manager`; advise - `askit-capability-advisor`), 7 subagents (Claude-only: `askit-skill-author`, `askit-evaluator`, `askit-quality-grader` (behavioral judge), `askit-reviewer`, `askit-explorer`, `askit-file-search`, `askit-file-ops`), 2 commands, a 29-check validation spine (Bronze U1-U9, U11-U12, Silver S1-S8, Gold G1-G10: `hook-documentation` G1, `self-hosting` G2, `library-regression` G3, `index-drift` G4, `release-notes` G5, `deprecation` G6, `docs-frontmatter` G7, `folder-readme` G8, `source-doc` G9, `docs-presence` G10; `mermaid-valid` is U12; tier inclusion is an unnumbered structural property, not a numbered check) + generators + `tier-report`/`evaluate`/`check`, 308 tests with golden/anti fixtures + a Codex ingestion round-trip. **v1.3.0 (gate evolution) adds: the standard-aware gate (`meta.since` on every check + a pinned-Standard error->warn downgrade, ADR 0027) and the configurable gate (an opt-in `askit.config.json` for per-rule severity, the `askit-library`/`plain-plugin`/`house-style` profiles, a suppressions baseline, a `provenance` tag per check + the real-issues/profile-conformance report split, and a published-verdict trust clamp). No new spine check: the spine stays 29 and the Standard stays 0.11.**
- **Docs site:** full family Astro site standard conformance (clauses 14.1-14.11; ADR 0026) landed on `main`, and the site is now the **generated Pattern S view** of public `docs/**` (ADR 0024 D2): `site/scripts/gen-docs-site.mjs` emits link-rewritten pages into the gitignored quadrant dirs read by the stock `docsLoader()`, generated at `astro.config.mjs` load, with `check-generated-untracked.mjs` (gitignore-invariant + untracked) and the two 14.11 guards run in CI.

- **ADR 0024 v1.1.0 docs build-out (P0-P6 COMPLETE on `main`):** the staged author-before-enforce program (`release-plans/plan_v1.1.0/`) took the spine 25 -> **30** (U12 + G7-G10) and the Standard to v0.10. All merged: P0 (no-dash hook, G1 non-vacuous; #88), P1 (Diataxis content + frontmatter taxonomy; #89), P2a (`docs-frontmatter` G7 + Standard v0.10; #90), P2b (generated Pattern S site; #91), the HTML evaluation-report templates + E1 (#92), the post-merge reconcile (#93), the P3-P6 spec packets (#94), P3 (`mermaid-valid` U12 + README diagrams; #95), P4 (`folder-readme` G8 + 37 folder READMEs; #96), P5 (`source-doc` G9 + docblocks; #97), and P6 (`docs-presence` G10 + STANDARD v0.10 completion + the `1.0.0 -> 1.1.0` bump). Each phase shipped one PR vs protected `main` with a recorded adversarial review. **Pre-tag adversarial-review fixes (MERGED, PR #99):** a whole-build-out 4-lens adversarial review (`v1.0.0...HEAD`) found one release blocker - `U12` mermaid-valid false-rejecting validly branded diagrams (a `%%{init}%%` directive / `%%` comment / `---` config block before the keyword) - plus seven confirmed lower defects (CRLF reference-style links, G8 fenced-example parsing, U8 version drift only WARN, G10 readdir-order dependence, the RELEASE-NOTES full-file fallback). All fixed with +10 regression tests (280 total, gate 0/0), adversarially re-verified (0 blocking), and merged before the tag; deferred items (the Standard-versioning policy, house-style configurability) are recorded in ADR 0027 (Proposed) + `release-plans/plan_v1.1.0/adversarial-review-resolutions.md`. **v1.1.0 is now FULLY SHIPPED (2026-06-03):** tag `v1.1.0` -> commit `f3250c0`, GitHub release live (Latest), marketplace re-pinned (`agent-plugins` PR #14, metadata 1.7.0), install resolution smoke-verified end to end. The ADR 0024 build-out is complete and released; no maintainer-gated steps remain. **Next (post-1.1.0, maintainer-gated):** ratify or revise ADR 0027 (Standard-versioning/compatibility policy) before any third party grades against the Standard.

## Scope decision (2026-05-30, maintainer)

- **Agents:** v1.0.0 targets **Claude (Code and Cowork, one plugin format) + Codex** at Gold. **Gemini is a named v1.x roadmap** target (additive emitter), not v1. (Audit decision 1.)
- **Components:** v1 covers the **full builder catalog** (all 27 functional areas), using the consolidated v1 verb taxonomy (`build-<type>` with `create`/`improve` modes + the generic `evaluate`), not 60 discrete skills. (Audit decision 2.) This is the maximal-scope choice; see R6 below.
- **Beginner UX:** a minimal on-ramp (install path + one tutorial + visuals) is pulled forward to the Silver preview. (Audit decision 3.)
- **Truly everything in v1 (ADR 0020 O-1):** the previously-deferred docs-site, full eval engine, benchmark, statusline, and deprecate are all in v1. Docs strategy is ADR 0021: +2 net-new consolidated skills (`askit-build-docs`, `askit-build-samples`) collapsing the catalog's 6-7 docs/sample micro-skills; an Astro Starlight site deployed to GitHub Pages (copied from pm-skills); a bounded example-threads pattern; a summary+detailed architecture/ADR convention.
- **Subagents (ADR 0020 O-2):** all `askit-` prefixed for future-proofing (Gemini v1.x ships subagents). Rename the 2 shipped subagents + add a per-component prefix check.

> **R6 (scope / burnout) is now the dominant risk.** Full-catalog v1 is large. Mitigations: the monotonic-tier model keeps the toolkit shippable at every phase exit; the public Silver preview ships early as a release valve; delivery is sequenced into waves (below); independent builders are parallelizable via worktree-isolated subagents.

## Phase progress (real decomposition)

| Phase | Scope | Status | Evidence |
|---|---|---|---|
| 0 | Repo + Standard seed | DONE | tag v0.1.0 |
| 1 | Validation spine | DONE | v0.2.0 |
| 2 | Authoring/assessment core (build-skill, evaluate, subagents) | DONE | v0.2.0 |
| 3A | Codex contract v0.8 + Silver conformance core (S1-S5) | DONE | `c79be5c` |
| 3B | Multi-agent emission spine (Codex manifest + S6 + round-trip) | DONE | `65ba9a8` (#49) |
| 3C-1 | Silver self-claim (tier convergent, v0.2.0) | DONE | `a672485` (#53) |
| 3C-2a | build-subagent + dogfood subagents (Claude-only) | DONE | `c2dca9b` (#63) |
| 3C-2b | build-command + builder-pattern.md | DONE | `6b9d419` (#74) |
| 3C-2c | build-mcp + sec 3.9 correction + component-level S6 | DONE | `v1-build` |
| 3C-2d | build-hook, build-chain-contract, build-agents-md, build-output-style, build-workflow | DONE | `v1-build` (10 skills) |
| 3 gate | Phase 3 (Convergent) builder set complete; Codex hardening applied | DONE | `f35bfaf`; next: `0.3` Silver preview (go-public decision) |
| 4 | Governance + lifecycle + advise + init-* + judgment subagents + full-catalog tail | DONE (catalog) | `v1-build`: full v1 catalog built (23 skills + 7 subagents). ADR 0023; Sub-phase A eval engine (G3 + Layer 2 + `askit-quality-grader`); B (build-docs + build-samples); C (init-plugin + init-marketplace); tail (statusline, settings, deprecate + G6, template-manager). Remaining: the live Astro site build + a Phase-4-close adversarial gate. |
| 5 | Gold G1-G7 + self-conformance + v1.0.0 | DONE (self-conformance) | toolkit declares `tier: advanced`; full Gold gate G1-G7 green (0/0), 188 tests; PR #75. Remaining: the `v1.0.0` release tag (maintainer-gated) |

## DoD burndown (full-catalog v1, consolidated taxonomy)

Status: `done` on disk | `designed` (design doc exists) | `pending`. Areas reference `builder-skills-catalog.md`.

| Area | v1 component (consolidated) | Type | Target | Status |
|---|---|---|---|---|
| 3 | `askit-build-skill` (create/improve) | skill | both | done |
| 3/16/17 | `askit-evaluate` (conformance core; behavioral/review modes pending) | skill | both | done (partial) |
| 5 | `askit-build-subagent` | skill | claude | done |
| 4 | `askit-build-command` | skill | claude | done |
| - | `askit-skill-author`, `askit-evaluator` (rename from unprefixed pending) | subagents | claude | done |
| 9 | `askit-build-mcp` (+ mcp-valid U11, component-level S6) | skill | both | done |
| 6 | `askit-build-hook` | skill | claude(+cx subset) | done |
| 7 | `askit-build-workflow` | skill | both | done |
| 8 | `askit-build-chain-contract` | skill | both | done |
| 10/24 | `askit-build-agents-md` | skill | both | done |
| 12 | `askit-build-output-style` | skill | claude-only | done |
| 13 | `askit-build-statusline` | skill | claude-only | done |
| 14 | `askit-build-settings` + permission advice | skill | both(subset) | done |
| 11 | references/assets (a build-skill / build-samples concern) | mode | both | done (covered by build-skill + build-samples) |
| 1 | `askit-init-marketplace` | skill | both | done |
| 2 | plugin scaffolding (folded into init-plugin) | skill | both | done (folded into `askit-init-plugin`) |
| 23 | `askit-init-plugin` (interview/questionnaire/hybrid) | skill | both | done |
| 18 | `askit-backlog` (intake/triage/prune) | skill | both | done |
| 24 | `askit-decision` (ADR/RFC) | skill | both | done |
| 16/20/26 | `askit-release` (gate/version/changelog/release-notes) | skill | both | done |
| 0 | `askit-migrate` (adopt foreign repo) | skill | both | done |
| X | `askit-capability-advisor` (resolved to a skill, not a subagent) | skill | both | done |
| 15 | `askit-build-docs` (readme/quickstart/tutorial/how-to/reference/glossary/faq/troubleshooting/architecture/site modes) | skill | both | done (site mode = the pinned Astro Starlight recipe; live site is a build-verified slice) |
| 19 | `askit-build-samples` (threads-aware golden/anti generator + drift validate) | skill | both | done |
| - | `askit-reviewer` | subagent | claude | done |
| - | `askit-quality-grader` | subagent | claude | done (rebuilt as the behavioral judge in eval engine Layer 2; ADR 0023 closed backlog N1) |
| - | `askit-explorer`, `askit-file-search`, `askit-file-ops` | subagents | claude | done |
| 17 | eval-harness + library-regression (behavioral eval layer) | spine+skill | both | done (Layer 1 G3 `library-regression` check + `evals/` format; Layer 2 `askit-evaluate` behavioral/review modes + `askit-quality-grader` judge) |
| 19 | (folded into `askit-build-samples`, above) | - | - | - |
| 21 | deprecation policy + status handling | skill+check | both | done (`askit-deprecate` + the G6 `deprecation` check) |
| 25 | template-manager | skill | both | done (`askit-template-manager`) |
| 15 | docs + docs-site -> `askit-build-docs` (above); changelog/notes -> `askit-release` modes | skill/mode | both | done |

Count: the full v1 catalog is on disk - 23 skills and all 7 planned subagents. The build areas above are all `done`; what remains for Phase 5 (Gold) is conformance work on the toolkit itself (declare `advanced`, close G1/G2/G4/G5) plus the live Astro site build, not new catalog components.

## Cross-cutting workstreams (added by the 2026-05-30 audit)

| Workstream | Status | Tracked in |
|---|---|---|
| Re-baseline plan (this file + RELEASE-PLAN v0.2) | in progress | audit/2026-05-30_audit-plan-v1.md |
| Repo hardening (LICENSE, version + dash checks, sec 3.9 fix, mermaid, governance files, CI matrix, security scanning, release automation) | in progress (P0 batch) | audit/2026-05-30_audit-repo.md |
| Update-ingestion / spec-sync (pinned upstream versions + scheduled drift check) | pending | RELEASE-PLAN v0.2 |
| Behavioral eval layer (triggering evals + LLM-judge) | pending | Phase 4/5 |
| Docs, examples, docs-site, beginner on-ramp (Diataxis incl. tutorials + QUICKSTART; Astro Starlight + Pages from pm-skills; `build-docs` + `build-samples`; example-threads; summary+detailed convention) | pending (on-ramp pulled to Silver) | ADR 0021 |
| Agentic execution layer (task specs keyed to reqIDs + @claude surface + worktree parallelism) | pending | RELEASE-PLAN v0.2 |

## Next action

**The full v1 catalog is built and adversarially gated (Phase 4 complete): 23 skills + 7 subagents, 21-check spine, gate green at Convergent, 173 tests.** Sub-phase A (eval engine: G3 `library-regression` + Layer 2 behavioral/review + `askit-quality-grader`), B (`askit-build-docs` incl. the Astro Starlight `site` recipe + `askit-build-samples`), C (`askit-init-plugin` with the asserted anatomy test + `askit-init-marketplace`), and the tail (`askit-build-statusline`, `askit-build-settings`, `askit-deprecate` + the G6 `deprecation` check, `askit-template-manager`) are all on `v1-build`. The Phase-4-close adversarial gate ran and its confirmed findings were fixed: the S8 `components-mirror` check (closing a sec-5.1 status-mirroring gap that opened a G6 escape hatch), the Codex marketplace command, a broken references/ link (+ U6 extended to scan references docs), and minors.

**PUBLIC as of 2026-06-01 (maintainer go):** the repo is public at Silver (the planned 0.x preview) at https://github.com/product-on-purpose/agent-skills-toolkit. `v1-build` was merged to `main` (now the default); `main` is branch-protected (PR required, the `validate` CI check required and strict, no force-push or deletion, linear history, conversation resolution; `enforce_admins: false` keeps an owner escape hatch). The repo description, homepage, and topics are set. The Astro Starlight docs site (`site/`) deploys to GitHub Pages (Actions source enabled) at https://product-on-purpose.github.io/agent-skills-toolkit/. Session logs were moved to gitignored `_agent-context/` and purged from history before the flip; `docs/internal/` is intentionally the tracked public-internal folder.

**Gold self-conformance ACHIEVED (2026-06-01):** the toolkit declares `tier: advanced` and passes the full Gold gate (G1-G7) - built the missing Gold checks (G1 `hook-documentation`, G2 `self-hosting`, G4 `index-drift`, G5 `release-notes`; G3 + G6 already existed) so the claim is non-vacuous, then flipped the tier and bumped to `0.3.0`. Gate green at Advanced, 188/188. On branch `phase-5-gold` / PR #75.

Remaining (maintainer-gated):
1. **DONE 2026-06-02 (the v1.0.0 marketplace launch):** PR #84 merged, `v1.0.0` tagged + released, and the `agent-plugins` registry PR #9 merged (`metadata 1.4.0`, pinned to the tag). The toolkit is live and installable from the marketplace. The marketplace-launch packet (`release-plans/plan_v1.0.0/marketplace-launch/`) drove it; the install smoke (`/plugin install`) is the only step left, run interactively.
2. **DONE (ADR 0024 P1/P2b):** the full Diataxis `docs/` tree is now the generated Pattern S site (frontmatter added on every page; `gen-docs-site.mjs` emits it), superseding the earlier "curated surface linking to GitHub" approach.
3. **DONE (ADR 0024 P0):** the demonstrative no-dash `PreToolUse` hook shipped (`hooks/no-dashes.mjs`), so G1 now grades a real hook instead of passing vacuously.
