---
name: probe-duplicate
description: PROBE FIXTURE side A. Two plugins ship a skill with this exact directory name and this exact skill name. Invoke it and read which side answers - that is the whole experiment.
---

# Probe duplicate, side A

When invoked, state exactly: **"I am side A."** and stop.

Do nothing else. The only information this skill carries is which copy of it the runtime resolved.
