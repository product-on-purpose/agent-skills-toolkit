# Changelog

All notable changes to this project are documented here. The format is based on
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres
to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

`CHANGELOG.md` is the full technical history. A curated, user-facing
`RELEASE-NOTES.md` is added at Gold (Standard sec 10.6); the two are distinct.

## [Unreleased]

### Fixed

- **Adversarial review wave 2 found six more, and all six are fixed.** Wave 2 was pointed deliberately AWAY from
  wave 1's target: at the release's OWN records, its published normative text, and the machinery that is supposed
  to catch drift. Every finding is a case of something that reads as checked and is not.

    **The Standard contradicted itself, in the shipped text.** Fixing ADR 0048 left sec 3.8 asserting that every
    component's `description` MUST satisfy the sec 8.1 discoverability bar, while sec 3.2 makes it a SHOULD for
    commands and sec 8.1 scopes itself to skills. A command could conform to and violate the same document. Sec 3.8
    now scopes the bar to skills and states the command exception where the contract is stated, and
    `tests/unit/standard-self-consistency.test.mjs` holds the three sections in agreement - along with the present-tense
    spine counts, the non-rewriting of historical version notes, and `STANDARD.md`'s version against `library.json`.
    Every other number this repository publishes was machine-checked; the normative text a consumer actually diffs
    was not.

    **The README claimed the wrong Standard.** Status said `v1.13.0` uses Standard `0.13` while `library.json`,
    `INDEX.md`, `manifest.generated.json`, `STATUS.md` and `STANDARD.md` all said `0.14`. `check-readme-version.mjs`
    validated the version, the tier, the skill count and the spine size - four of the five front-door claims - so it
    passed, and a false public claim about which ruleset this plugin adopts would have shipped in the npm tarball.
    **A partial drift guard is worse than none: because it exists and passes, nobody re-reads the part it skips.**
    The pin is corrected and the guard now covers it.

    **Two pinned vendor claims could not fail.** `vendor-watch` matches by substring, and two claims were pinned as
    bare tokens (`Plugin agents support`, `disable-model-invocation`). A page reading *"the `disable-model-invocation`
    field was removed"* still contains the token, so the watch would have reported `holds` for a field that no longer
    existed. All six quote claims are now complete sentences, re-verified against the live pages, and a **meaning-reversal
    table** in the test suite pins, for each one, a page that keeps its vocabulary and says the opposite - which the
    watch must report MISSING. A further test checks the reversals are honest (they must share content words with the
    claim), and it rejected the first attempt at one. A new fragment claim cannot be added without failing three tests.

    **The monthly watcher would have opened a new issue every month.** It deduplicated by filtering open issues on the
    label `vendor-watch`, which had never been provisioned on this repository; a label filter naming a nonexistent
    label matches nothing. The comment described the intent and nothing implemented it. Dedup is now on a stable body
    marker that cannot be missing, absent, or stripped during triage, the label is provisioned (and self-provisions if
    absent), and `concurrency` stops a manual dispatch racing the scheduled run.

    **The release-blocking preconditions were a checklist.** `RELEASE.md` asked a human to confirm *"`npm run
    vendor-watch` green, and its run under 30 days old"* - a line whose entire subject is that people forget to re-read
    things - and **no workflow ran it**. A tag, a GitHub release and an npm publish could all ship while a sentence this
    repository asserts as fact had been gone from the vendor's page for a year, with every automated signal green.
    `npm run release-ready` now runs the conformance gate, the README drift guard, the release-count guard and
    `vendor-watch`, reports all four, and exits 1; `release.yml` and `publish-npm.yml` both invoke it. Freshness is
    enforced by the run rather than recorded: a claim past the window is STALE, which is exit 1, which blocks.
    **The one override covers an unreachable vendor page and nothing else** - a third party's outage is not a fact
    about this repository, but a gone-or-stale claim is, and no reason string excuses publishing it.

    **The records described a pin that had grown twice.** `CHANGELOG.md` and `STATUS.md` both said the watch pins
    "six claims across two vendor pages"; it pins eight across three. True when written, and prose, so it rotted in
    the release notes a consumer reads first. Corrected, and now asserted by a test.

- **Adversarial review wave 1 found four defects in the checks this release added, and all four are fixed.** The round was pointed at the three new checks and their migration metadata specifically. It cleared what I most wanted attacked - **no defect was found in the `since`/`until` ceiling interaction or the activation-neutral reason strings** - and then found this:

  **`agents/` is scanned RECURSIVELY, and both agent listers were flat.** Claude Code states it verbatim: *"Plugin `agents/` directories are also scanned recursively"*, and *"a file at `agents/review/security.md` in plugin `my-plugin` registers as `my-plugin:review:security`"*. So `agents/review/_shadow.md` was a live subagent that bypassed **both `U15` and `U14`** - the exact bypass ADR 0045 and ADR 0046 exist to close, one directory deeper. Both listers now walk recursively and keep the subpath in the name, because a bare basename would collide `agents/security.md` with `agents/review/security.md`. A filename containing `:` is skipped, because the runtime refuses it and being stricter than the runtime is the opposite failure.

  **The probe was generalised past its evidence.** The recorded experiment tested `README.md` and `_README.md` in a FLAT directory; the invariant drawn from it claimed to describe the whole directory. Recursion and the colon rule are now separate pinned vendor claims rather than an assumption.

  **`U17` dropped source-less catalogue entries before partitioning**, so `[{source:"./skills/a"},{name:"plugin-b"}]` reported nothing while `U13` claimed the manifest and ignored that entry too, and marketplace scope declined the whole file. The entry was examined by **no scope** - precisely the routing hole `U17` exists to close, passing cleanly. An entry with no usable `source` is now a shape error.

  **`U16` told authors to delete a value `S4` was reading.** `S4` resolves `metadata?.chain ?? frontmatter?.chain`, and `??` falls through on `null` - so `metadata: { chain: null }` alongside a top-level `chain` leaves the top-level array LIVE. `U16` called it dead weight; following that advice silently removed chain-contract enforcement. `U16` now ASKS `S4` through one exported resolver instead of duplicating the rule, and the lost-value message says **move it, do not delete it**.

  **vendor-watch could exit 0 having verified nothing.** A probe-only document whose fetch failed, and an empty claims document, both reported success - so the scheduled workflow would open no issue for a run that confirmed nothing. Refusal now keys off **source-level** fetch failures rather than claim verdicts, which is what let a probe hide a failed fetch. A MISSING claim stays exit 1: the page was read and the sentence is gone, which is a finding, not a refusal.

  **Family blast radius: nothing moved**, isolated by measuring with the recursive listers present and absent and verifying which was loaded at each step.

  **Seven mutation checks, and one initially proved nothing** - reported rather than quietly rewritten. The colon-exclusion test used a filesystem fixture named `bad:name.md`, which **cannot exist on Windows**: NTFS reads the colon as the alternate-data-stream separator, so the write SUCCEEDS and the directory ends up holding a file called `bad`. The test was vacuous on the platform `validate-windows` runs. The rule is now an exported predicate tested directly.

### Added

- **vendor-watch: every vendor claim this repository asserts as fact is now pinned and re-checked on a schedule.** `STANDARD.md` and four checks cite Claude Code behaviour as fact. `U14` quotes a vendor sentence in every finding it emits; `U15` carries `vendor-cited` provenance because it rests on how the runtime discovers subagents; sec 3.2 explains itself by reference to how commands are invoked. **Each of those was a page somebody read once and a date they wrote down.**

  **What that cost, one day before this shipped:** ADR 0048 was ratified asserting that a command is not a skill, and Claude Code says *"Custom commands have been merged into skills."* A 2026-08-10 internal audit had **already found it** and graded `S7` a conceptual conflict. The evidence existed. Nothing was re-reading it.

  `docs/internal/vendor-watch/vendor-claims.json` pins eight claims (6 quote, 2 probe) across three vendor pages, each carrying a `dependsOn` list (what breaks) and an `onChange` instruction (what to do, which differs per claim: `U14`'s is asymmetric because a field REMOVED from the refused list is green-ward while a field ADDED is a Standard revision). `npm run vendor-watch` checks them; `.github/workflows/vendor-watch.yml` runs monthly and **opens an issue rather than editing anything**; and `npm run release-ready` **runs it inside `release.yml` and `publish-npm.yml`**, so a tag or a publish is blocked by a claim the vendor no longer makes rather than by someone remembering to look.

  **It pins CLAIMS, not pages.** A page hash would fire on every navigation and CSS change and be ignored within a month; what this repository depends on is specific sentences, so those are what is pinned and checked.

  **A claim is a `quote` or a `probe`, and the distinction is honest rather than cosmetic.** A quote must still appear on the page and is checked automatically. A probe is an empirical behaviour **no page states** - `U15`'s "Claude Code registers every `.md` under `agents/`" was established by installing a plugin and looking at what registered - so the watch reports its AGE and names its reproduction and **never claims to have verified it**.

  **Refusal outranks a clean result:** exit 2 when a page could not be read, because a watch that passes because it could not reach the page is worse than no watch. Exit 1 when a claim is gone or has aged past the 30-day window.

  **Write-incapable by construction**, like `standards-watch`: only `readFileSync` is imported and everything goes to stdout, with a test that fails the build if any write API appears in either module. A watcher that can amend the claim it watches turns a governed decision into a silent one - and deciding what a vendor change MEANS is an ADR, not a pin bump a scheduled job performs at 3am.

  Verified against the live pages on 2026-08-15: all four quote claims hold, both probes correctly reported as unconfirmable by fetch. Proved sensitive on all three axes before being trusted - a claim the vendor does not make reports MISSING and exits 1, a 60-day clock shift reports every claim stale and exits 1, and an unreachable source exits 2. One real bug in the matcher was caught by its own test: it folded curly quotes but not straight ones, so a page rendering a sentence with curly quotes would never have matched a claim written with straight ones.

### Changed

- **ADR 0048 is AMENDED IN PLACE: its decision stands, its premise was false.** The ADR asserted that a command is not a skill. Claude Code's own documentation says, verbatim: *"Custom commands have been merged into skills. A file at `.claude/commands/deploy.md` and a skill at `.claude/skills/deploy/SKILL.md` both create `/deploy` and work the same way"*, and the plugins reference describes `commands/` as holding *"skills as flat Markdown files"* (both read 2026-08-15).

  **Nothing shipped is reverted and no behaviour changes.** `U5`/`U6`/`U7` staying skills-only is still correct, and so is sec 3.2's demoted SHOULD - but for a better reason. The vendor exposes `disable-model-invocation` and `user-invocable` frontmatter, and a `commands/` file is a skill **whose invocation is user-controlled**: a model never loads it automatically, so its description performs no trigger matching. `STANDARD.md` sec 3.2 and the 0.14 version note carry the corrected justification.

  **The process failure is the part worth recording.** The 2026-08-10 internal audit had already found this and graded `S7` a CONFLICT (conceptual) - that finding is the only reason "commands-as-skills" was on the v1.14.0 list. The ADR answered a **vendor-alignment** question by **measuring description scores** and never opened the audit's evidence. The measurement was sound; it was answering something else.

  **`askit-standards-watch` pins four agentskills.io artifacts and zero vendor pages**, so every vendor claim in this repository - `U14`'s field list, `U15`'s discovery behaviour, this ADR's premise - rests on a person having checked a page once. Vendor-watch is the remaining v1.14.0 workstream and this is its justification. One positive: `U14`'s citation re-verified **verbatim**, including "For security reasons".

  Filed as **E44**, not fixed: if the property that matters is invocation control rather than component type, a `skills/` file declaring `disable-model-invocation: true` has no trigger surface either and `U5` scores it anyway. **Measured first: 0 of 2435 skills across seven pinned corpora and all six family members declare either field.** A check for a population of zero is not warranted; the entry carries its trigger condition.


- **Standard 0.13 to 0.14.** The version note records all seven decisions of the v1.14.0 ADR pack (ADRs 0046 to 0052) and, per sec 7.7, which of them a plugin adopts by re-pinning to 0.14 (`U15`, `U16`) and which are held for every pin below 0.15 (`U17`, the workflow mirror). The Universal checks are now `U1-U9`, `U11-U17`; the spine is **34**.

- **Sec 3.2: a command's description MUST is DEMOTED to a SHOULD (ADR 0048), and this is the diff to read if you track the Standard.** The rule said a command's description "MUST match the skill's triggering intent" - unenforceable as written, and **satisfied by 0 of 14 commands in the reference family, this toolkit's own two included.** Those two score 0.65 while their backing skills, of identical name and intent, score **1.00**; the entire gap is one literal token ("Use **to** audit" is not "Use **when**"). It splits into a MUST (non-empty, states what invoking does - enforced by `S7`) and a SHOULD (agrees with the backing skill's intent - real, and not machine-checkable).

  **Why the bar is wrong for the surface, not merely mistuned:** a skill is selected by a model matching its description, so the description IS its trigger surface; a command is selected by a person typing `/<name>`, and on Codex there is no command artifact at all, so the backing skill does the triggering. **On neither runtime does a command description perform trigger matching.** `templates/command.md`, `docs/how-to/build-a-command.md` and the `askit-build-command` skill all asked authors for a trigger sentence and are corrected; the how-to's sec 8.1 citation is what made two hand-written commands here score 0.65. `U5`, `U6` and `U7` keep their skills-only scope, and a test now fails if `U5` is widened to commands.

- **Sec 8.1 is scoped to skills, and its scorer declines rather than fails a language it cannot read (ADR 0049).** The scoping is stated because the bar had been read as applying to any component. The declining half is normative: tooling MUST NOT report a description as failing when it cannot read the description's language, because a low score is a claim about the description and only "not scored" is a claim the tool has evidence for.

- **Sec 12 records that a catalogue-level finding is never a numbered requirement on an individual plugin (ADR 0051), which CLOSES the question of graduating cross-member findings.** The criterion is the **unilateral-remedy test**: a marketplace finding may become a numbered requirement only if the member named in it can resolve it by editing its own repository alone, without reference to any other member and without editing the catalogue. Applied to all eight marketplace classes, exactly one passes, and it already graduated as `U14`. The code had been saying so and nobody had noticed: six of the seven attribute their finding to the catalogue's own manifest or to a path spanning two members. A test asserts `reqId: null` by iterating the **emitters**, so a ninth class carrying a reqId fails CI rather than being caught in review.

  **One condition would reopen it, for two of the seven only.** `skill-collision` and `command-collision` rest on components entering a shared runtime namespace: if a runtime namespaces by plugin they should be **retired** rather than graduated. The other five are unconditional, because no vendor change makes a member the owner of a catalogue's file.

### Added

- **`U17` (`catalogue-manifest-shape`), spine 33 to 34: a `.claude-plugin/marketplace.json` that no scope can read (ADR 0052). Warn-only at 0.14; gates at 0.15.** The disjointness rule between marketplace scope and `U13` is a clean partition of the WELL-FORMED cases and says nothing about the rest.

  **Malformed.** A present-but-unparseable manifest produced **zero findings from anybody**: marketplace scope declines a catalogue it cannot read, `U13` swallows the parse error by design, and the directory grades as a plugin with nothing said about the broken file. Both behaviours are individually correct and deliberate; their conjunction is the hole. `detectMarketplaceScope`'s docblock closed with the claim *"and the JSON problem surfaces there"* - which was not true when written, and now is.

  **Mixed.** `looksLikeMarketplaceOfSkills` returns true if **any** entry resolves under `skills/`, so a manifest mixing skill entries and plugin entries routes entirely to `U13` and its plugin entries are **never collection-graded**. Reproduced: a catalogue declaring one local skill and one sibling plugin reports `scope: plugin, members analysed: 0`, with a real plugin catalogued and invisible. E36 asked whether a mixed manifest is legal; the answer is **(c), it is itself a defect**, and the remediation is to split it into one manifest per kind.

  **It ships warn-only at 0.14 because a census found it PREVENTIVE rather than corrective.** Across the seven pinned corpora, all six family members and `agent-plugins`: **7 real manifests, 6 of-plugins, 1 of-skills, zero mixed, zero malformed.** Both branches were found by adversarial review of the routing logic, not by observing a target. The hole is real and silent, which is why the check ships; it does not warrant gate-failing severity in the minor that introduces it.

  Mechanically this makes `U17` **the first check in the spine to carry `since` and `until` simultaneously** - `since: "0.14"` plus finding-level `migration: { capAt: "warn", until: "0.15" }`. `since` alone would gate the moment a consumer adopts 0.14; `until` alone would expose a plugin pinned below 0.14 to a check that did not exist at its pin. ADR 0044 point 2 anticipated both binding at once and specified that the reported `due` is the maximum across them; this is its first live case, verified at pins 0.13, 0.14 and 0.15.

  **Neither `detectMarketplaceScope` nor `resolveRegistrationSource` changes.** The disjointness rule is correct; `U17` reports the cases the partition does not cover rather than extending it to cover them. The `underSkills` predicate is **extracted and shared** with the scope router rather than reimplemented - two copies of "does this resolve under `skills/`" is precisely how the mixed case became invisible.

  Standard sec 12 gains the one-manifest-one-kind rule, and (per ADR 0051) a statement that a catalogue-level finding is never a numbered requirement laid on an individual plugin.

- **The frontmatter vocabulary is OPEN, stated as a rule, and `U16` (`metadata-placement`) checks PLACEMENT instead. Spine 32 to 33 (ADR 0050).** Standard sec 3.8 listed the keys each component type carries and never said what an unknown key is; `U3` validated `name` and `description` and nothing else. So every other key was unconstrained in both directions: an author could add anything, and could put a Standard key anywhere.

  **Strictness was measured and rejected.** Across **2342 skills in thirteen sources** (seven pinned corpora plus all six family members), **44.9 percent** carry a top-level key the Standard does not name and **58.2 percent** carry an unnamed `metadata.*` key. Three family members sit at 100 percent on one axis or the other. The most common unnamed keys are entirely ordinary: `compatibility` (971), `metadata.author` (1158), `metadata.tags` (1154). And sec 3.7's own opening calls the agentskills.io `metadata` map **arbitrary**, so rejecting unknown keys inside it would put this Standard in contradiction with the spec it profiles. Sec 3.8 now says the vocabulary is open **as a rule**, because silence reads as an unfinished thought and invites the same proposal again.

  **The inverse condition is 0.9 percent and every instance is real.** `U16` reports a key sec 3.7 places under `metadata` that is declared at the **top level**, where nothing reads it. The live case: **all six shipped `critique-skills` skills declare `version` at the top level with no `metadata` block at all.** Sec 3.7 says `version` is REQUIRED on every component at every tier; nothing in the spine read `metadata.version`, so that member neither satisfied the rule nor was told, and graded **Convergent with 0 errors**. This is `U14`'s defect class one layer over: the author wrote a declaration, it is not where anything reads, and nothing says so.

  Two messages, because two situations: a key **only** at the top level is silently lost and the message names its destination; a key in **both** places is shadowed by the nested copy, which is read, and the top-level one is dead weight that can drift from it. `Object.hasOwn`, never a nullish test, so an explicit top-level `version: null` is still reported.

  **`U16` is `house` provenance and is therefore OFF under `plain-plugin`**, the honest third-party grading mode - sec 3.7's placement is this Standard's convention, not a vendor's. `U15` is `vendor-cited` and stays on, which is exactly the difference between a portable fact and a house rule. The `plain-plugin` invariant test enforces that the off-set equals the house-provenance set, and it caught the omission before this shipped.

  Measured on the family: **6 findings on `critique-skills`, held at `warn` by its 0.12 pin against the 0.14 introduction, Standard debt 1 to 7, and no verdict moves.** It gets a real bill of six one-line edits when it re-pins, which is the correct outcome.

  **A gap stays open knowingly:** nothing yet catches `version` being **absent** entirely, which sec 3.7 also requires. That check would fire on every component of every plugin that never adopted the convention, and it is deliberately not bundled here.

- **`U15` (`agents-dir-registerable`), spine 31 to 32: every `.md` under `agents/` must be a registered subagent (ADR 0046).** Claude Code discovers subagents by scanning `agents/` for `*.md` and registers **every file it finds**. A probe recorded in `folder-readme.mjs` proved it: a directory holding `real-agent.md`, `README.md`, `_README.md` and `README.txt` registered **three** subagents - `real-agent`, `README` and `_README`. The underscore prefix protects nothing; only the non-`.md` extension is skipped.

  Four Silver checks (`S2` prefix, `S3` components-index, `S4` chain-contract, `S8` components-mirror) read the plugin's REGISTRATION list, which excludes those files. **A fixture plugin earns Convergent with 0 errors while shipping `agents/_shadow.md` and `agents/README.md`** - an unprefixed name, no manifest entry, `version: 9.9.9`, `status: deprecated` - and no check reads either one.

  **The obvious fix was measured and rejected.** Pointing those four checks at the runtime list works, and against `agents/README.md` it emits *"subagent README must start with the plugin prefix"* and *"declare it in library.json components.subagents"*. **An author who follows either instruction produces the phantom subagent with no name and no description that the 2026-08-06 `G8` exemption exists to stop plugins creating.** A check whose remediation creates the defect is worse than the silence it replaces. There is also a structural objection: `S3` and `S8` are bidirectional mirrors, so widening the disk side redefines what the manifest is a mirror OF, and makes the phantom **legal** in the same stroke.

  `U15` instead makes the two lists provably equal, so the four checks are complete **without being modified**. Its remediation is shape-specific and neither branch says "register it": a folder `README.md` moves out of `agents/`; any other unregistered file either becomes a properly prefixed, declared subagent or moves out. Both branches state the runtime fact, because an author who does not know the file is loaded cannot choose between them. `vendor-cited`, so under ADR 0044's trust step a subject cannot weaken it about itself in `published-verdict` mode.

  **The `G8` exemption and `U15` are two halves of one rule, and the code now says so.** The exemption stopped `G8` CAUSING phantoms; it did nothing about the ones already shipped, and both this repository and `critique-skills` had one.

### Fixed

- **`S3` stopped claiming that a declared, on-disk agent file is not on disk (ADR 0046 point 5).** A second, opposite defect that the `U15` measurement found and nobody had filed. An author who honestly declares `agents/_shadow.md` in `components.subagents` was told `declares "_shadow" but it is not on disk under agents/`. **The file is on disk.** Taken with the bypass above, the gate rewarded **concealing** an unregistered agent file and punished **declaring** it.

  Only the "declared but not on disk" direction is corrected - it asks a question about the filesystem, so it must test the filesystem. The opposite direction deliberately still reads the registration list, because that direction is the tightening and `U15` owns it; widening it would be the rejected design above. A test pins the asymmetry so it is not later "fixed" as an oversight.

  Correcting a false statement can only ever **remove** a finding, so it ships with no migration window.

### Added

- **Workflows are a loaded component, and the components mirror finally covers them (ADR 0047).** A workflow is a first-class Convergent component in the Standard (sec 3.4), with a defined location and format, and the loader never knew about it. `loadPlugin()` built skills, subagents, agent docs, commands and MCP servers, and no workflows.

  **The visible consequence was a gate finding that stated something untrue about your filesystem.** `S7` (`command-contract`) read `ctx.workflows`, which was always `undefined`, so a command declaring `maps-to: <a real workflow>` was reported as *"maps-to X but no skill or workflow by that name exists on disk"* against a repository containing `_workflows/X.md`. The remediation it implies - delete the mapping, or rename the workflow into a skill - is destructive advice from a wrong premise. The source comment said `ctx.workflows arrives in a later phase`; it had not arrived for as long as the field had been read.

  `_workflows/*.md` is now loaded once, in the loader, excluding a folder `README.md` and `_`-prefixed control files. `S5` and `S4` are repointed at it, replacing two more private readings of the same directory - the repository had **three** different ideas of what a workflow file is, and they had already drifted (`S5`'s private reader included `README.md`). Building the list can only WITHDRAW a finding, so it ships with no migration window and moved nothing on any family member.

  **`S3` (`components-index`) gains the workflow half of the mirror**, both directions, so a plugin can no longer ship nine workflows, declare none, and be told nothing. This one can only ADD findings, so it is a subrule tightening and every finding carries its own migration metadata: **introduced at Standard 0.14, gate-failing at 0.15.** Measured both ways - without the window it takes `thinking-framework-skills` from Convergent to Universal on 9 new errors; with it, the same 9 findings resolve to warnings, land in Standard debt, and **no verdict moves**.

  Two second-order facts for anyone fixing a plugin against this: a `_workflows/` directory holding only a `README.md` no longer counts as "chaining is in use" for `S4`, which is the better answer; and in `pm-skills` the command names and workflow names do not correspond, so `commands/workflow-design-sprint.md` would declare `maps-to: design-sprint`. Building the list does not make those commands pass - it makes them able to.

### Changed

- **`U5` (description-score) now DECLINES to score a description whose language it cannot read, instead of failing it (ADR 0049).** `U5` awards 0.35 of a 1.00 score for matching an English trigger-phrase pattern, against a 0.70 threshold. A description that pattern cannot match therefore caps at **0.65 and cannot pass at any quality.** On a 349-skill French corpus the pattern fired on **0 of 346** parseable descriptions while **341 of them carried an explicit French trigger clause.** The finding `U5` emitted - "state what it does AND when to use it, with concrete trigger keywords" - was false of every clause of descriptions that did exactly that, in French.

  Before scoring, `U5` measures the description's **English function-word density** and emits nothing below a floor of **0.10**. No language-detection dependency, no per-language lexicon, and `scoreDescription` itself is unchanged: this is a decision about when the scorer should decline to produce a number, not a recalibration.

  **Measured on the French corpus: 346 findings become 3.** Median function-word density is **0.000** on that corpus and **0.233** on the largest English one, so the separation is not marginal. The floor came from a sensitivity sweep over **2068 descriptions in seven pinned corpora**, not a guess: at 0.10 the cost is **3 descriptions in 1376** English; at 0.15 it is 62, and the ones lost in between are legitimate keyword-dense technical English.

  **The three that still fail on that corpus are the honest residual** - French descriptions carrying enough English loanwords to cross the floor. This reduces a systematic defect to a rare one; it does not eliminate it.

  **A decline is not a pass and does not print like one.** `check.mjs` reports the not-scored count in its human output and as `descriptionsNotScored` in `--json`, computed outside the verdict path. Without it, a plugin whose descriptions are all unreadable would be indistinguishable from one whose descriptions are all good.

  **Nothing moves red-ward, at any pin, in any mode**, so this ships with no migration window: the change can only ever withdraw a finding. A test asserts that directly, over five description shapes.

  The backlog's own recommended alternative - a language-independent structural signal for "states an occasion" - was prototyped and **falsified**: it fires on **99.9 percent** of all 2068 descriptions, including 94.4 percent of Anthropic's own, so it cannot discriminate. A pluggable French lexicon was also prototyped and reached only 33.2 percent.

### Fixed

- **A plugin's restricted-field verdict depended on how it was graded, which is the one thing ADR 0045 exists to prevent.** v1.13.0's own changelog states that the vendor field list "live[s] in one module both scopes import, so a plugin's verdict cannot depend on whether it was graded on its own or as a catalogue member." **That was false as shipped.** Sharing the field list was necessary and not sufficient: the two scopes must also apply it to the same **agents**, and they did not. `U14` was moved to `ctx.agentDocs` (every `.md` a runtime loads from `agents/`) during v1.13.0's review, while the marketplace scope kept building each member from `ctx.subagents` (only what the plugin registers, excluding `README.md` and underscore-prefixed files).

  **The observable effect:** a plugin shipping `agents/_shadow.md` with `permissionMode` took a `U14` error when graded on its own and produced **no finding at all** when graded as a member of a catalogue. Same bytes, two answers.

  A member now carries **both** lists under their true names - `subagents` for what it registers, `agentDocs` for what a runtime loads - mirroring the loader's own vocabulary, and the `A6` reading uses `agentDocs`. `subagents` keeps its existing meaning, so nothing reading the collection report changes.

  **Three tests were blind to this, each correctly, and that is the part worth keeping.** The `A6` unit test hand-builds its `members` array and so never reaches the real member build. The verdict-parity test compares tier, errors, warnings and exit code, and `A6` is a scope-local `warn` carrying no `reqId`, so it never enters a verdict. And ADR 0045's own "cross-scope parity assertion" compares **field lists**, which are identical on both sides of this defect. The new guard is end-to-end: one fixture directory graded both ways, comparing the restricted-field **findings**. It was proved capable of failing against each half of the fix independently.

  No verdict moves on any of the six family members. ADR 0045 is **not amended** - its decision was correct and its implementation diverged from it a release later - but it now carries a dated correction recording what the guarantee actually required.

## [1.13.0] - 2026-08-13

### Upgrade

- **Nothing in this release takes effect for your plugin until you raise `standard` in your own `library.json`.** If you change nothing, your grade does not change. The full cost of adopting 0.13, with remediation per item, is [Adopting Standard 0.13](docs/reference/adopting-standard-0-13.md).

  **Adopting 0.13 makes four things effective:** `U13` (register every skill you ship on disk, and ship every skill you register), `S4` (a string-shaped chain declaration is held to the same bar as an array), `U14` (a plugin-shipped agent may not declare `hooks`, `mcpServers` or `permissionMode`), and validation of the new optional `library.json` `selfValidation` enum.

  **If you have no `standard` pin at all, none of this protects you.** An unpinned or unparseable pin grades against the current ruleset immediately - ADR 0027's existing back-compat rule, since a plugin that never declared which contract it adopted cannot be graded against the one it adopted. Add the line.

  **Price the move before committing to it:** `npx agent-skills-toolkit . --strict` grades you against the newest spine without changing your pin, so every finding that appears only under `--strict` is one adoption would make real.

- **Regenerate your `INDEX.md`.** `gen-index` previously wrote `Self-validating: node scripts/check.mjs` into every index it generated, including for plugins that do not vendor this toolkit's gate and therefore have no such file. Run `npx agent-skills-toolkit gen-index . --write` (or `node scripts/generators/gen-index.mjs . --write` if you do vendor it). **Until Standard 0.14, an `INDEX.md` matching the old rendering exactly is reported as a `warn`, not an error**, so no plugin is gated on a defect this toolkit caused. If your plugin genuinely does vendor the gate, declare `"selfValidation": "vendored"` in `library.json` first.

- **Two paths can now fail where they passed, and neither needs a pin change.** `--strict` ignores your pin by definition and now ignores the ceilings too. And in `published-verdict` mode a setting the graded SUBJECT wrote about itself can no longer weaken an objective or vendor-cited finding; a subject may still be stricter, and grader-supplied settings are honoured in full.

### Added

- **Standard 0.13, and a new reference page saying what adopting it costs.** [`docs/reference/adopting-standard-0-13.md`](docs/reference/adopting-standard-0-13.md) states, in one place, the four things that become effective when you raise your own `standard` pin - `U13` (register every skill you ship), `S4` (a string-shaped chain declaration is held to the same bar as an array), `U14` (plugin-shipped agents may not declare `hooks`, `mcpServers` or `permissionMode`), and the new `library.json` `selfValidation` enum - together with how to price the move before committing to it (`npx agent-skills-toolkit . --strict` shows exactly the findings adoption would make real).

  **It leads with the case that has no migration window at all: an UNPINNED plugin.** If `library.json` has no `standard`, or its value is not a `MAJOR.MINOR` string, every requirement applies immediately, including this release's. That is not new in 0.13 and it is not an oversight - a plugin that never declared which contract it adopted cannot be graded against the one it adopted - but it had never been said plainly on a page a consumer reads, and a guarantee whose exceptions are buried is not a guarantee. The page also names the two things that change WITHOUT a pin change: `--strict` (which ignores your pin by definition, and now ignores the ceilings too) and `published-verdict` mode.

- **`library.json` gains an optional `selfValidation` field**, a closed enum of `"vendored"` or `"npx"` where absent means `"npx"`. It selects which self-validation command a generated `INDEX.md` names. Registered in the Standard's sec 5.1 normative field schema, because enforcing a field in `U1` while leaving it out of 5.1 would have Standard 0.13 contradicting its own implementation.


- **`U14` - restricted fields on plugin-shipped agents (ADR 0045), spine 30 to 31.** Claude Code refuses `hooks`, `mcpServers` and `permissionMode` on an agent shipped inside a plugin, in its own words **"for security reasons"**. The field is refused rather than ignored and nothing tells the author, so an agent carrying one has configured something that is simply not in effect. v1.12.0 detected this across the members of a catalogue and not when a single plugin is graded on its own, which is how almost everyone runs the gate.

  The vendor quote was **re-verified against the live page** before the code was written; both sentences matched verbatim. The field list and citation live in one module both scopes import, so a plugin's verdict cannot depend on whether it was graded on its own or as a catalogue member.

### Changed

- **Three version-gating mechanisms became ONE ceiling, applied after your configuration resolves (ADR 0044).** The repository had three ways to gate on the Standard version and only one of them read the pin. ADR 0027's `since` downgrade ran as a PRE-pass over the raw findings, which put it *before* configuration resolved - so a consumer's own `rules.X = "error"` was applied afterwards and simply beat it (**E26**). ADR 0041's migration cap was unconditional, so it could never graduate anything. And `U13`'s scheduled graduation lived in a hand-edited constant whose own comment conceded the gap.

  All three are replaced by a single severity ceiling computed from `(pinned, since, migration.until)` and applied LAST inside `resolveFindings`. `since` governs an **introduction** and `until` governs a **tightening**: two inputs to one ceiling, not two mechanisms. It never raises, and every comparison is by severity RANK rather than lexically - lexical `min("error","warn")` is `"error"`, which would have inverted the ceiling into a floor.

  **New public `ceiling` field**, always present, `null` whenever no constraint BINDS. Binding-only rather than version-active: where config had already lowered a finding, a constraint changes nothing, and reporting it would tell every debt consumer the pin is holding back a finding the unchanged config keeps a warning either way. A version condition that changes no outcome is not debt.

  The legacy `--json` fields (`downgraded`, `pinned`, `since`) are **deprecated for one minor** and each is specified independently rather than as a triple, which is self-contradictory for an `until`-only ceiling. `since` is emitted only when an introduction participates, because a tightening does not change when a check was introduced.

- **Configuration now carries WHO CHOSE IT, and a published verdict can no longer be weakened by its subject (E38).** Every resolved setting is grader-owned, subject-owned, or default. In `published-verdict` mode, a subject-owned setting can no longer lower an objective or vendor-cited finding - it is discarded and the finding returns to the severity a grader-selected rubric would give it. A subject can still be **stricter** about itself, and grader-supplied settings are honoured in full.

  **This deliberately reverses a guarantee this codebase published.** `resolve-config.mjs` stated that "turning the mode on can never flip a passing gate to failing"; it now can. A guarantee that protects the subject is the wrong guarantee in the one mode built to publish a verdict *about* the subject. The guarantee was stated in five places and all five moved together, because a fix that resolves only inside our own source leaves the surface other people actually read.

  Suppression is cleared independently of severity: a gate needs `error` AND `!suppressed`, so a step that only raised severity would leave a subject-owned waiver intact and still publish green.

- **`dispositions` is now an ordered, first-match partition that SUMS to the finding count**, plus a new orthogonal `trustActions`. The buckets previously overlapped - a live non-house error reduced by config was counted in both `realIssues` and `profileConformance` - so "the buckets sum" was never true of the old code either, which is exactly what every consumer assumed they could do. `profileConformance` and `warns` both shrink. `clampNotice` is deprecated for one minor and populated only where the old semantics remain literally true.

- **Two scheduled graduations discharged (`U13`, `S4`), and the mechanism that scheduled one of them could not have delivered it.** Both checks now emit their TARGET severity and encode no migration state themselves. `chain-contract.mjs` emitted `warn` on both string-derived branches under a `warn` ceiling, so lifting the ceiling at 0.13 would have produced a warning: **removing a ceiling cannot promote anything**, and `S4` would have graduated in name only. ADR 0041 is amended in place to record that.


- **`publish-npm.yml` now publishes via npm trusted publishing (OIDC), with no stored credential of any kind.** v1.12.0's live publish attempt failed `ENEEDAUTH` because the `npm-publish` environment holds no secrets, and the fix is not to add one. npm is actively restricting 2FA-bypassing tokens for direct publishing (the deprecation notice appears in that very run's log), so a token is a path to a closing door; OIDC has the runner mint a short-lived identity token that npm verifies against a publisher registered once on npmjs.com.

  **The `NODE_AUTH_TOKEN` fallback is removed rather than kept as a safety net.** A fallback that silently succeeds by a different mechanism is how a release gets published without the provenance everyone assumes it has - the same silent-substitution failure this repository has corrected twice elsewhere. With no fallback, an unregistered publisher fails loudly with an auth error, which is a diagnosis rather than a mystery.

  **`--provenance` is no longer passed, and its absence is not an omission:** npm generates provenance attestations automatically for a public package published from a public repository via trusted publishing. Passing the flag would imply the flag is what produces it.

  **The version floors OIDC requires are now asserted instead of inherited.** Trusted publishing needs npm >= 11.5.1 and Node >= 22.14.0. `.nvmrc` pins Node 24, comfortably over its floor, but the runner's npm was whatever that Node happened to bundle, and early 24.x releases shipped npm 11.3/11.4. The floor is installed explicitly and both versions are checked, so a runner image change becomes a named failure at that step rather than an authentication error three steps later that reads like the publisher was never registered.

  Two stale header comments describing the retired token design were corrected in the same change, and the standing requirement about scoping `NPM_TOKEN` to an environment secret was **deleted rather than restated**, because there is no longer a token path to misconfigure.

  **Exercised for real on 2026-08-12, not merely wired up.** With the trusted publisher registered against `product-on-purpose` / `agent-skills-toolkit` / `publish-npm.yml` / environment `npm-publish`, a `dry_run: true` dispatch packed cleanly and a `dry_run: false` dispatch published **1.12.1** to the registry, closing a gap where two releases were tagged, GitHub-released and marketplace re-pinned while npm consumers stayed on 1.11.1. The published tarball carries an automatic SLSA provenance attestation and a registry signature. Note for anyone reading the dry run as a rehearsal of the whole path: it is not, by design. Authentication happens only in the `publish` job, which the dry-run path never enters, so a dry run cannot tell you whether the publisher is registered - only the live run can. **1.12.0 was deliberately not published**, because it carried the three high-severity defects 1.12.1 fixed and an npm publish is irreversible after 72 hours.


### Verified

- **Codex round-trip (Q-E gate) run for this tag against Codex CLI 0.144.5**, 2026-08-14: the emitted
  `.codex-plugin/plugin.json` was installed into a throwaway local marketplace against the real CLI and
  the skills were confirmed INGESTED, not merely listed. The distinction is the whole point of the test:
  a manifest can appear in a listing while the runtime loads nothing from it. The last recorded
  verification was against 0.135.0, so this re-confirms emission against a newer CLI.
- **Seven adversarial review rounds**, each one reviewing the code the PREVIOUS round's fixes produced,
  because that code is otherwise unreviewed. v1.12.0 shipped after a single round and needed v1.12.1 for
  four more defects, every one inside round-one fix code. Every fix in this release carries a test that
  was proved capable of failing, by reverting the fix and confirming the test goes red.
### Fixed

- **The shell-probe timing tests no longer fail because the machine is busy, and no longer leave stray processes behind (E37).** In plain terms: two tests were timing how long a piece of cleanup code took, using stopwatch limits that a loaded computer could exceed even when the code was working perfectly. On the maintainer's workstation that produced a permanent "1 test failing", which in turn blocked `npm run release-counts` - the check that a release's stated test numbers are true - so **a release could not complete its own verification protocol on that machine.** Both tests now measure against limits derived from the code's own declared timing contract instead of copied numbers, and both clean up the processes they deliberately strand.

  **The numbers, because a timing fix is only as good as its margin.** A correct run of the stuck-helper case takes 3.3 to 3.8 seconds idle and was recorded at 5331 ms on a loaded workstation, against an assertion bar of exactly 5000 ms - which was also the stand-in helper's own lifetime, so one number meant both "did not wait for the helper" and "waited, and the helper died". The helper's lifetime moved to 60 seconds and the guard derives from it, so the two outcomes are now 30 seconds apart. `KILL_GRACE_MS` is exported from the test module as a timing contract rather than duplicated as a literal, and the hanging-candidate case asserts `timeoutMs + KILL_GRACE_MS + slack` instead of three copied numbers that could drift from the values they track.

  **Both cases were proven able to still fail.** A timing test that cannot fail is worse than one that flakes, so each was re-run with its own defect deliberately reintroduced: removing the `unref` that keeps a stuck kill-helper from anchoring the event loop fails the first case, and making the supervisor overshoot its declared bound fails the second - with a control run confirming the second fails for that reason and not for the scaffolding around it.

  **Process hygiene, measured rather than assumed (E32).** The cases that substitute or sabotage the kill helper were each stranding one shell process per invocation, about five per run of the file, which is where this workstation's accumulated orphans were coming from. They now reap what they strand, addressed by a marker scoped to the owning process so a parallel test file's work is never touched, and a full suite run leaves **zero**. The underlying supervisor defect (E32) remains deliberately deferred: a surviving candidate is reported as residue rather than failing the run, because failing on a known-deferred defect would put the flakiness straight back.


- **`gen-index` stops inferring whether a plugin vendors this toolkit's gate, and reads a declaration instead (E35).** The self-validation line was emitted unconditionally, so a plugin that consumes this toolkit rather than vendoring it received `Self-validating: node scripts/check.mjs` - a command nothing installs for them, shipped inside their own repository over their signature. Found by executing this repository's own published npm instruction from a clean directory, which is the third time consumer-position execution has found something no amount of reading found.

  **This is the fourth design for one line, and the three rejected ones each have a committed fixture that kills them**: `existsSync` on `scripts/check.mjs` (a generic path - a consumer with an unrelated file there is told to run someone else's program), the `G2` self-hosting condition (which regex-matches workflow YAML, so a CI that merely *mentions* the gate satisfies it), and the plugin's own `library.json` name (authored data, not authenticated identity). Each felt like identity and none of them was.

  **`G4` carries the migration so nobody is gated on our defect.** An `INDEX.md` matching the pre-v1.13.0 rendering EXACTLY is capped at `warn` until Standard 0.14; every other form of drift stays a hard error, so strictness is unchanged for every case except the one this toolkit caused. Measured across all six family members: `product-lifecycle-templates` holds Advanced with one capped warning, where the uncapped fix dropped it to Convergent - the regression that got this reverted inside v1.12.0.

## [1.12.1] - 2026-08-12

v1.12.0 shipped after **one** adversarial review round. This patch exists because a second round was run afterwards, on the observation that **the code written in response to round 1 had never itself been reviewed** - four fixes, one of them to the parity harness that had just started gating every pull request. Round 2 returned four findings, three high, plus two defects found by hand while writing its prompt. Every one is in code round 1 caused to exist. No check was added, removed or tightened; spine 30, Standard 0.12, and no plugin-scope or component-scope verdict moves.

### Fixed

- **The parity exception fingerprint was matching the wrong thing.** v1.12.0 required each `PARITY_EXCEPTIONS` entry to carry a `matches` regex, then tested it against the validator's **entire combined output**. `templates/seed-plugin` has exactly one authorized diagnostic, its missing `author`. Had it acquired a **second, unrelated** defect, the combined output would still contain the word "author", so the whole result stayed excused - hiding a new defect in the scaffold template every newly created plugin is generated from, on a harness that now gates.

  Fixed by measuring the real output shape rather than assuming it: `claude plugin validate --strict` reports each problem on a `❯`-prefixed line, so `extractDiagnostics` splits on that marker and `allDiagnosticsMatch` requires **every** diagnostic to match; one unmatched diagnostic gates. Where no marker is present (the skills-ref validator, the reduced-fidelity fallback, or a future format change) the whole detail is treated as one diagnostic - the previous behavior - because turning a required check red over a cosmetic vendor change is the alarm-fatigue hazard ADR 0042 names against itself. A `g`-flagged fingerprint would separately have advanced `lastIndex` between diagnostics and produced alternating results inside a gating predicate; the flag is now stripped before the repeated test.

- **A wrong discovered checkout could still be graded in a member's place.** Three defects, and the middle one is a lesson in incremental hardening. The suffix comparison went through three forms and the first two were both wrong: `a.endsWith(b)` accepted `notgithub.com/owner/name` for `github.com/owner/name`; adding a path boundary closed that and **still** accepted `evil.example/github.com/owner/name`, because a hostile path prefix reproduces the declared path at a boundary that looks legitimate. There is no safe prefix allowance, so there is now none - `normalizeRemote` reduces both sides to `host/path` (scheme, credentials, `.git`, trailing slash and explicit port removed) and the comparison is exact. A genuine mirror is what the `askit.marketplace.json` mapping is for.

  Separately, candidates were raced rather than ranked: a same-named plugin with no git metadata could not be disproved, so it was accepted on sight and shadowed the correct checkout in a later search root. A **confirmed** identity now always wins, whatever order it was found in. And `git-subdir` members were permanently unverifiable, since their plugin directory sits below the repository root and `<member>/.git` never exists - the remote is now read from the repository root via a bounded upward walk.

  A member matched by name whose identity cannot be confirmed either way is still **graded** - refusing would break legitimate vendored copies and extracted tarballs - but the collection now raises a **warning** saying so, so a green can no longer rest silently on an assumption about which directory was read.

- **The scope guard was four markers short and could still move a plugin's verdict.** v1.12.0 checked `library.json`, `skills/`, `agents/` and `commands/`. A plugin carrying `AGENTS.md`, a native `plugin.json`, and only hook or MCP components would still have been re-scoped to a catalogue and skipped its plugin checks entirely - exactly the verdict movement that release's governing invariant forbids, left open by a list written too quickly. The guard now covers `library.json`, both native plugin manifests, `.mcp.json`, and every component directory (`skills`, `agents`, `commands`, `hooks`, `workflows`, `output-styles`, `themes`, `monitors`), and a test walks every one of them rather than trusting the list stays current.

  `AGENTS.md` stays excluded, on a checked rather than assumed basis: the family marketplace this scope exists for carries `AGENTS.md` and a `marketplace.json` and nothing else, verified on disk, so including it would make the scope undetectable on its own motivating case.

- **The public red-versus-not-red contract was stale.** `docs/reference/marketplace-scope.md` and this changelog both said, unconditionally, that any existing directory which is not a plugin is an unresolvable entry and reds the collection. v1.12.0's own resolver fix had already made that false for *discovered* candidates, which are passed over rather than blamed. The explicit-versus-guessed distinction, and the identity check, are now documented where operators read them.

### Added

- **E37** (the shell-probe timing test has no margin and fails under local load) and a measured update to **E32**. `resolve-bash.test.mjs` began failing during this work; it is not caused by these changes, and the first hypothesis - accumulated orphaned processes interfering - was **disproved by measurement** (clearing them changed nothing, 0 of 4 runs passing before and after). The real cause is a wall-clock budget with no margin: the process must exit before the stuck helper's 5000 ms lifetime and measured 5331 ms on a loaded machine. It passes `validate-windows` on clean CI runners. E37 carries an explicit warning not to "fix" it by widening the threshold, since the threshold IS the helper's lifetime and raising it removes everything the test discriminates.

  Separately, E32's own escalation condition ("evidence that orphans accumulate across a normal development session") is now **met on evidence**: this workstation carried **68 orphaned `bash` processes** in run-shaped clusters spanning eight days, and four consecutive suite runs took the count from **63 to 85**, about five leaked per run under ordinary use. The other half of that condition, interference with a later run, was investigated and did not hold. Both halves are recorded so the next reader inherits the measurement rather than the suspicion.

## [1.12.0] - 2026-08-12

The gate learned a third scope. It could grade one plugin or one skill; it can now grade a **catalogue**, which is where a whole class of defect lives that no per-plugin run can see. **1004 tests, 0 failures**, gate Advanced 0/0. Spine unchanged at 30 checks, Standard unchanged at 0.12, and no existing plugin's verdict moves.

### Added

- **Marketplace scope (ADR 0039), the third evaluation branch.** `evaluate()` now detects a directory carrying a `.claude-plugin/marketplace.json` whose entries catalogue member **plugins**, and grades the collection. Every member is graded at **its own** declared tier and **its own** Standard pin - exactly as it would be graded alone - and the collection is red if any member fails **its own** claim. That aggregation rule (self-consistency worst-member) invents no tier expectation for anybody, which is the failure ADR 0038 forbids at aggregate scale, and offers no threshold a maintainer could tune until the collection turned green.

  **The scope and `U13` (skill-registration) are disjoint by construction, not by convention.** `resolveRegistrationSource` claims a `marketplace.json` exactly when at least one entry's source resolves under `skills/` (its `set.size > 0` guard); this scope's detector is the logical inverse of that same guard, expressed against the same `skillNameFromPath` helper rather than a second copy of the rule, and a test asserts that exactly one of the two ever claims a given manifest.

  **Two failures wear the word "unresolved" and only one of them is a red**, which falls out of combining ADR 0039's question 1 (grade local checkouts) with its question 2 (an unresolved member reds), and would otherwise red a collection merely because the operator had not cloned every member. An **unresolvable entry** - no source, a malformed source, or an **explicitly named** location (a local-path source, or an operator-supplied mapping) that does not exist or is not a plugin - is a defect in the catalogue and reds it, because an installer following that entry receives nothing. A member **absent from this machine**, or one whose only same-named candidates turn out to be a different repository, is a gap in the environment reading the artifact, is reported `not-graded`, and never reds; the verdict line carries the coverage count unconditionally so the gap is stated rather than inferred. The asymmetry is deliberate: a location the catalogue or the operator *named* is a claim whose failure is a defect, while a location this scope *guessed* from a repository basename is a hypothesis, and a failed hypothesis is absence.

  Six deterministic finding classes move the collection verdict: manifest shape, entry resolvability, duplicate catalogue names, cross-member skill-directory collision, cross-member command-name collision, and registry-versus-member version agreement. Three further analyses - cross-member trigger-surface overlap, command-versus-skill divergence, and content lineage between members - are namespaced under `advisory` and can never reach the verdict or the exit code.

  **Every finding carries `reqId: null`.** ADR 0039 question 3 chose scope-local findings over numbered spine checks so the contract every existing plugin is held to does not tighten in the same release that adds the capability to see between members. The tests enforce this rather than trusting it: `tests/unit/registry-sync.test.mjs` asserts the spine is exactly 30 and that `provenanceByReq()` covers every registered check, so a null-`reqId` module in `CHECKS` fails CI by construction.

- **The collection report, the sixth report type**, in terminal, JSON, Markdown and HTML. Every member row carries the registry pin sha, the registry entry version, the graded sha and a divergence marker **unconditionally, including when they agree** - a report that showed them only on disagreement would teach a reader to assume agreement from silence. Each row also carries that member's **Standard debt**, the count of findings that are warnings only because they postdate its own pin, which is what makes green-by-an-old-pin visible rather than flattering. The renderer is factored per report type at the dispatch point rather than threaded through the plugin view-model, so the five existing golden snapshots are byte-identical in this release.

- **Three new marketplace source kinds and the `renames` field**, accepted by both `askit-init-marketplace` and the new scope: `npm`, `archive` (which **requires** a `sha256`), and `git-subdir`. Accepting a kind means classifying and reporting it correctly, including reporting honestly that it cannot be resolved to a local checkout; it does not mean fetching it, since remote fetch-at-sha stays deferred. An `archive` with no digest is rejected as a broken entry rather than accepted as unpinned: an archive with no digest is an unverifiable download, and accepting it would let a catalogue advertise integrity it does not have. `renames` records the names an entry previously shipped under, and a former name colliding with a live name, or with another entry's former name, is an error - it is a duplicate name one step back in time.

- **A6, restricted fields on plugin-shipped agents**, read across every member of a catalogue. Claude Code states it plainly: "For security reasons, `hooks`, `mcpServers`, and `permissionMode` are not supported for plugin-shipped agents" ([plugins reference](https://code.claude.com/docs/en/plugins-reference), read 2026-08-12). An author who writes one of these believes they have configured something the runtime refuses. **This ships as a catalogue-level warning rather than a numbered plugin-scope check, deliberately and with the reason recorded:** a new tier requirement is a Standard 0.12-to-0.13 bump under ADR 0027, and Standard 0.13 also graduates `U13` to `error` and ADR 0041's chain-migration cap. Tightening the contract for every existing plugin as a side effect of a marketplace feature is the wrong trade, so the detection ships where it could ship without it and the graduation is filed as **E33** against the cut that owns it. **E34** separately asks the prior question of whether any cross-member finding belongs on the spine at all, since a plugin cannot unilaterally fix a collision with a sibling it does not know about.

- **Two public pages:** [marketplace scope](docs/reference/marketplace-scope.md), the durable reference for the new scope, and [the family registry](docs/reference/family-registry.md), a dated, reproducible collection report over the six-member family catalogue. The registry page is stamped as a snapshot of local checkouts on a date, with the reproduction command, precisely so it is not read as a live published-truth registry.

### Changed

- **The validator-parity harness is GATING** (`PARITY_MODE = "gating"`), discharging the flip ADR 0042 scheduled. That ADR replaced a release number with an evidence condition - "the mechanism completing at least one real release cycle in actual CI with no new undocumented disagreement left unresolved at the point of the flip - not a fixed calendar date" - and v1.11.0 and v1.11.1 both completed with the `validator-parity` job green on GitHub-hosted runners. The single known disagreement (`templates/seed-plugin` versus `claude plugin validate --strict`) carries its ADR 0043 exception entry and is annotated, not hidden. **One consequence is accepted knowingly:** under gating, `metadataParityUnavailableResult()` fails closed, so a run where `uvx` cannot be installed reds a required check rather than printing a line nobody reads. That is the intended behavior for a harness built because silence hid a defect for two releases. The harness's own summary lines now read in the present tense ("BLOCKS this run") rather than the conditional future, and the banner distinguishes the shipped default from a `--mode=` override, because a job that describes enforcement as pending while it is on is the same misreport in miniature.

- **`looksLikePlugin` moved from a private function in `scripts/evaluate.mjs` into `scripts/lib/load-plugin.mjs`.** Marketplace scope has to ask the same shape question of every member, and importing it back out of `evaluate.mjs` would be a cycle. One copy means a member and a directly-graded plugin cannot be judged by two drifting definitions of "is this a plugin".

- **`docs/internal/RELEASE.md` gained the npm publish step.** The gate has been a published package since v1.11.0 and the release checklist did not mention it, so a release could be tagged, GitHub-released and marketplace re-pinned while npm consumers silently stayed behind. The entry records the two constraints that are not negotiable: 2FA is enforced on the account, so a local publish needs a human at the prompt, and `--provenance` cannot be produced from a laptop.

### Found, and deliberately NOT fixed here

- **A generated `INDEX.md` tells consumers to run a command they do not have (backlog E35).** `gen-index` emits `Self-validating: node scripts/check.mjs` unconditionally, into every plugin it generates an index for. For a plugin that consumes this toolkit rather than vendoring it, that path resolves to nothing - and unlike a wrong instruction in our own documentation, this one ships **inside the consumer's own repository, over their signature**. Same defect class as the `G4` remediation failure recorded on 2026-08-10, one layer deeper, and found the same way: `npm i -D agent-skills-toolkit` into a clean directory against the live registry, then regenerating a scaffolded plugin's index from the consumer's position rather than from inside the tree where it happens to work.

  **The one-line fix was written, tested, and reverted inside this cut, and the reason is worth more than the fix.** The justification for shipping it was that "the only plugins whose output changes are the ones the line was wrong for, and every one of them is already in `G4` drift from the v1.10.0 generator fix, so one regeneration closes both." That reasoning is false, and one measurement falsified it: `product-lifecycle-templates` graded **Advanced, 0 errors, 0 warnings** before the change and took a `G4` index-drift error immediately after, because it has no `scripts/check.mjs` and the expected index moved underneath its committed one. This release's governing invariant is that no existing verdict moves, and that moved one from green to red on a live marketplace member. A migration-bearing generator change belongs in a release that schedules a migration; **E35** carries it to the Standard 0.13 cut with an `### Upgrade` section, the way v1.10.1 handled the previous wave. A test asserts the current, known-wrong behavior on purpose so it cannot be silently re-fixed without meeting that entry.

### Fixed

- **`docs/internal/RELEASE-HISTORY.md` was two releases stale.** v1.11.0 and v1.11.1 shipped without an entry, so the narrative record stopped at v1.10.1 while the repository was at v1.11.1. Both are backfilled, and the "Where we are now" line now names the drift it is itself prone to.

## [1.11.1] - 2026-08-11

One fix, found by a maintainer running v1.11.0's own published publish instructions and hitting a wall CI cannot reach. **948 tests, 0 failures**, gate Advanced 0/0. No behavior change to the gate, no Standard movement, nothing a plugin is graded by moves.

### Fixed

- **`npm publish` was impossible from the default Windows shell, and the failure message pointed at the wrong thing.** `tests/unit/action-run-step.test.mjs` executes the Action's real `run:` script by shelling out with `execFileSync("bash", ...)`, resolving `bash` **through PATH**. On Windows that resolution depends on the invoking shell: from Git Bash it finds Git's bash, which shares the Windows filesystem, and all seven tests pass; from PowerShell it finds `C:\Windows\System32\bash.exe`, which is the **WSL launcher**. WSL does not inherit Windows environment variables unless `WSLENV` says so, so the `RUNNER_TEMP` the test carefully sets **vanishes crossing the boundary**, `"$RUNNER_TEMP/askit-gate.json"` collapses to `/askit-gate.json` at the WSL root, and the script dies with `Permission denied` before producing any output.
  Because `prepublishOnly` runs `npm test`, that made **publishing impossible from PowerShell on any machine with WSL installed**, which is the default shell on Windows and an extremely common configuration. The gate refused to publish a package that was itself fine.
- **The misleading failure is fixed too, and it cost more than the defect.** The seven failures reported `expected 'none', got undefined`, which reads as a grading disagreement and sent the investigation through Node versions, `npm ci` timing and test flakiness before the buried `Permission denied` line identified the shell. Since the outputs bridge writes `tier` unconditionally before any branch in the step, an undefined `tier` can only mean the script died early: that case now throws immediately, naming the bash binary used, the exit status, and the captured stderr.
- **The fix asks each candidate shell to prove itself rather than judging it by its path.** The first attempt rejected WSL by matching the path string against `System32`, `Sysnative` and `SysWOW64`, and the pre-release review found that insufficient within one round. On this machine `%LOCALAPPDATA%\Microsoft\WindowsApps\bash.exe` is a **symlink to `wsl.exe`**, is on `PATH`, and contains none of those three names; explicit Git for Windows candidates were meanwhile accepted on `existsSync` alone and never reached the check at all. A denylist of known-bad paths is unbounded and permanently one alias behind. **The path string was a proxy for the property that actually matters**, which is whether a shell shares this filesystem and inherits this environment, so the resolver tests that property directly instead. Every candidate goes through the same probe, explicit locations and `PATH` fallbacks alike; the lexical match survives only as a cheap pre-filter that can reject early and can never grant acceptance. On POSIX, plain `bash` is unchanged.

  **The guarantee, stated exactly and no more strongly, because two further review rounds broke two earlier attempts to state it:**

  > A candidate is accepted only if, within a bounded timeout, it exits with code 0 after correctly echoing back a randomly-named environment variable this process set **and** correctly writing a randomly-named token to a file path this process gave it, with `WSLENV` stripped from the environment it runs in. That closes every concrete bypass found across both review rounds (`existsSync`-based trust, lexical path matching, output-only checks, and `WSLENV`-based forwarding of guessed names), but it is an empirical, bounded-time behavioral test, not a formal proof of isolation: a future candidate that genuinely satisfies that same observable contract within the timeout window would still be accepted, because at that point it is no longer distinguishable, from here, from a bash that actually shares this process's environment and filesystem.

  An earlier draft of this entry claimed the probe was "immune to symlinks, junctions, reparse points and launcher paths nobody has invented yet". Round 2 of the review showed that was false while the variable **names** were fixed, since `WSLENV` forwards named variables across the boundary with path translation. The sentence above is quoted from the implementation rather than paraphrased, because this is the fourth consecutive release in which a control's scope was described more strongly than the code enforced it.
- **The probe accepted candidates that had failed.** `execFileSync` throws on a nonzero exit or timeout, but the catch preserved whatever output had arrived while the pass decision never consulted the error, so a shell that echoed both tokens and *then* exited 7, or hung, returned `pass: true` and was selected for a real invocation that has no timeout at all. Proven live before fixing: the nonzero case returned `pass: true`, and the hang returned `pass: true` after 6014ms, which also confirmed `execFileSync`'s `timeout` is not a real bound because it waits for the child's pipes to close. Acceptance now requires a clean zero exit with no error and no timeout, under a supervisor with its own timer. The probe directory is removed on every path including the forced-kill one, asserted by diffing the temp directory before and after.

  **What the supervisor guarantees, and what it does not, quoted from the implementation:**

  > On rejection, this process reliably stops waiting - on the candidate and on any helper this file spawned to try to kill it - and exits within a bounded time, verified directly under adversarial conditions including a kill helper that itself starts and never reports back. It does not guarantee the candidate's own OS process is actually terminated, or that anything short of an explicit confirmed success means more than "this process stopped waiting and moved on."

  A third review round found the earlier version could **hang** rather than fail: `killProcessTree` discarded every `taskkill` failure and the hard-stop only resolved the Promise, never unreferencing the child or destroying its stdio, so a surviving descendant held Node's event loop open. Measured: the hard-stop resolved at 323ms while Node did not exit until the child died at 2046ms, and an unbounded child would have held it forever. Since `npm test` gates `npm publish`, that converted a publish failure into a publish hang. The kill is now asynchronous so it cannot block its own timer, failure is checked rather than swallowed, `child.kill()` runs as a fallback from both the failure path and the hard-stop, and a short grace window precedes `unref()` because unreferencing in the same tick as the kill let the target survive in roughly one trial in eight.
- **A residual is documented rather than engineered away, deliberately.** Verification surfaced that an orphaned `bash.exe` can survive even a kill Node reports as a clean `SIGKILL` exit, traced to MSYS process-tree representation on Windows rather than to the kill sequencing. Closing that fully needs OS-level containment, a Windows Job Object or equivalent, which is the right mechanism for a production process supervisor and over-engineering inside a test helper whose only job is rejecting an unsuitable shell. **The harm named by the review, a hung `npm test` and therefore a hung publish, is fixed and verified; what remains is an occasional orphaned background process, which is hygiene rather than a block.** Recorded as backlog E32 rather than chased, three review rounds into a one-line patch.
- **A second defect surfaced while proving the first.** `existsSync` returns **false** for that App Execution Alias even though `execFileSync` spawns it happily, so the existence gate that supposedly protected explicit candidates would have reported a working WSL launcher as absent. Trusting `existsSync` to mean "this is a usable executable" was wrong in both directions at once.
- **It fails loudly rather than skipping.** If no suitable bash exists, the file throws at import naming what was searched. A test that quietly no-ops when it cannot find its own shell reports green while testing nothing, which is precisely how this survived: **CI can never catch it**, because every ubuntu runner has a real `/bin/bash`. It took a maintainer on Windows running the documented command.
- **Surveyed rather than spot-fixed:** all 23 files under `tests/` that spawn a subprocess were checked. Every other one invokes `process.execPath` or `git`, both unambiguous. This was the only file trusting a bare `bash` through PATH.

### Note on numbering

A test-harness portability fix. No source file outside `tests/` changed, so this is a PATCH.

## [1.11.0] - 2026-08-11

Reach. The project had finished building a grade worth trusting and the grade was consumed nowhere outside this repository. Everything here makes it reachable: installable, `npx`-runnable, emitted in the formats CI systems read, wrapped in an Action, and reported on a badge computed at a known sha. Spine stays **30 checks**, Standard stays **v0.12**, and **no plugin's tier or exit code moves**. **939 tests, 0 failures**, gate Advanced 0/0. Release packet: `docs/internal/release-plans/plan_v1.11.0/`.

The governing rule for the whole release: **every output added is a pure serialization of data the gate already computes.** No new computation entered the gate path. Where a field could not be filled from what the gate already knew, it is not emitted and the gap is documented, which is why there is no invented line number anywhere in this release.

### Added

- **The gate is now installable and `npx`-runnable.** `package.json` flips to public with a single bin, an intentional `exports` surface, and a `files` **allowlist**. The allowlist matters more than it sounds: without one, publishing ships the entire monorepo, including `site/`, `tests/`, every fixture and all of `docs/`. It was derived by computing the actual import closure from the three entry points rather than by judgment, which is how six `scripts/lib/` modules with no consumer among the shipped entry points got excluded without guessing. The published tarball is **60 files**. Four maintainer-only tools are deliberately excluded because they read paths relative to **this** repository, including a default scoring key that points into `tests/fixtures/`: shipping them half-working inside someone else's `node_modules` is worse than not shipping them. **Proven from the consumer's position**, not asserted: the tarball was installed into a clean directory outside this checkout, pointed at a fixture plugin copied to a second directory so nothing could resolve back here, and it graded that plugin, exercised the lazily-imported renderers, and returned exit 1 on a non-plugin target. That last part is what proves the allowlist is complete; a file listing cannot.
- **A publish workflow that cannot fire by accident.** `workflow_dispatch` only, no tag trigger, because publishing to a public registry is a one-way door. The two modes are **structurally distinct rather than a flag on one code path**: the string `npm publish` does not appear anywhere in the dry-run path, so a mistyped flag cannot turn a preview into a release. Version agreement against the tag is checked first, mirroring the guard already in `release.yml`. **The maintainer still runs the single publish command.** The workflow exists to make that one command safe and repeatable.
- **The package name decision, recorded because it is a security question rather than a taste question.** The package and its single bin are both `agent-skills-toolkit`. The tempting short alias `askit` was rejected: that package already exists on npm and belongs to an unrelated author, so shipping it as a bin name would mean `npx askit` from a clean environment fetching and executing **their** code while any failure looked like ours. Four saved keystrokes is not worth an execution-confusion footgun, and the reasoning is recorded in the bin itself so a future maintainer does not helpfully add it back.
- **SARIF 2.1.0 output** (`--sarif`), so findings land in the GitHub Security tab. One deduplicated rule per registered check, one result per finding. **Provenance rides as a rule property**, which is backlog E9's actual point: a consumer can filter to portable objective failures only, and that is the single dimension where this toolkit stands alone against the field. **Suppressed findings are not dropped.** They appear at their real level carrying SARIF's own `suppressions` property with `kind: "external"`, because the waiver comes from `askit.config.json` rather than an inline source comment. A SARIF file that silently omits what was suppressed is how a clean Security tab gets manufactured, and this one refuses to do that.
- **GitHub Actions annotations** (`--gha`), with correct escaping of `%`, carriage returns, newlines, colons and commas per the workflow-command spec, so findings appear inline on a pull request diff.
- **`--json` on `check.mjs`**, emitting a **gate-only** object rather than `evaluate.mjs`'s report. That distinction is deliberate: `evaluate`'s object carries `byRule` and `dispositions`, which are analysis the gate does not compute, and producing them here would have been new computation in the gate path. When `--json` is set, stdout carries only JSON, so a consumer can pipe it.
- **A published GitHub Action** wrapping the gate, with the plugin path, profile, annotation and SARIF options as inputs, and the earned tier plus error and warning counts as outputs so a workflow can branch on them. **It does not `npm install` this package**, which is what makes it work before the package is ever published: `uses:` checks this repository's own tree out at the pinned ref and runs the committed `scripts/check.mjs` directly. All decision logic lives in a portable Node script rather than in YAML, per the standing invariant that a workflow holds no validation logic.
- **A CI-generated, sha-pinned tier badge.** The badge JSON is a shields.io endpoint carrying the tier, the **graded sha**, the Standard pin and the date, so a reader can tell what was graded and when. This retires a defect class permanently rather than fixing an instance: the previous badge was hand-maintained, and this repository's own front-door claims went stale for two releases without anything noticing. **The trade-off is recorded rather than hidden:** the Pages deploy trigger was widened from three path filters to every push to `main`, specifically so a code-only push refreshes the badge, at the cost of a full site rebuild on every push. A badge that silently fails to update is strictly worse than no badge, because it is a stale claim with a machine's authority behind it.
- **`scripts/check-parity.mjs`, the validator-parity harness, and ADR 0042 (validator parity is report-only and checks parsed values).** It runs `claude plugin validate --strict` on the repository and the seed template, and the `skills-ref` reference validator across every skill. **It asserts on parsed values, not exit codes**, and that requirement is the whole reason it is worth building: ADR 0040 records that `agentskills validate` reported "Valid skill" for all 24 skills while `metadata.chain` was being silently mangled, because the validator never inspects `metadata` contents. A harness that shelled out and checked the status code would have reported this repository green throughout that entire defect and reproduced the blind spot in CI while calling it coverage. It compares against the **actual reference implementation's parser** rather than a JavaScript reimplementation, which would drift from upstream and eventually certify parity against our own guess. **Demonstrated catching a seeded violation**, because a harness never seen to fail is not evidence: seeding a YAML list into a `metadata` value produced `[PASS]` from the vendor validator and `[MISMATCH]` from the parsed-values check, side by side. **Report-only for one release**, flipped to gating by a one-line change; the reason for waiting is narrow and honest, that the mechanism has only ever run locally and has not yet executed on a real runner. It also found genuine source-blob-versus-PyPI-release skew in all three `skills-ref` files, hand-investigated rather than reported as an alarming hash: a CRLF packaging artifact plus one real line difference.
- **A documented-exception path for parity disagreements, built because a real one appeared.** Each exception carries a target, a reason and the ADR that authorizes it, and a matching finding is **annotated rather than hidden**. An exception whose ADR reference does not resolve to a real file becomes its own finding, because a waiver justified by a decision record that does not exist is worse than no waiver. The summary distinguishes documented from undocumented disagreements, so only the latter would ever block once gating starts. This was originally deferred as speculative; it moved forward because leaving the one real exception rendering as a bare unexplained failure on every run is the alarm-fatigue defect, and it would have broken CI the day anyone enabled gating.

### Changed

- **Provenance is now visible in all five outputs**: the terminal, JSON, the SARIF rule property, a Markdown column and an HTML pill (backlog E23). It was already stamped on every resolved finding, so this is serialization rather than plumbing. The point is that a reader scanning findings can now tell a portable objective failure from a house-convention one without leaving the output.
- **The Bronze scaffold emits a minimal native manifest**, and ADR 0043 (Bronze scaffold defaults a minimal native manifest) records why. `claude plugin validate templates/seed-plugin --strict` failed with "No manifest found in directory" while the README promised a Bronze plugin is installable, and both could not be true. Since Codex 0.146.0 reads `.claude-plugin/*` directly, one minimal file now buys install recognition on **both** vendors, which is a premise move rather than a design error in the original tier split. **The Standard question is deliberately left open:** whether Bronze itself must *require* a native manifest would move every existing Bronze plugin's verdict and needs ADR 0027 warn-first treatment, so the tooling defaults it now and the spine decision waits for evidence.
- **A placeholder author was considered and rejected, and the acceptance criterion was corrected instead.** The vendor requires a real `author` object for `--strict`, and the obvious fix was `"author": {"name": "REPLACE - your name"}`. That is the same defect `U5` (description-score) already penalizes under ADR 0033, where a `TODO` that scores well is the scorer being fooled: a fabricated attribution that satisfies a validator is that with a different hat on. So the honest split ships instead. A template genuinely has no author, so it passes plain `validate` and **warns** under `--strict`, permanently and correctly. `askit-init-plugin`'s interview now asks for one and writes it into both manifests, so a scaffolded plugin passes `--strict`; declining emits **no** `author` key rather than an empty one. Both states were re-run from the consumer's position. The original criterion, inherited unqualified from an audit item, never said which artifact it meant.
- **`clampNotice` now reaches the Markdown and HTML reports** (backlog E28). The published-verdict clamp explains why a severity is not the one the consumer configured; the terminal showed it and the designed reports never had, so a consumer opened the shareable artifact and saw a severity that silently disagreed with their configuration. It is labelled distinctly from the warn-first migration cap, because they are different mechanisms and collapsing them into one generic note would be tidier and would destroy the information. Collected across **all** findings for a requirement rather than the lead's, which is the bug the equivalent `migrationNotice` fix had to be corrected for one release earlier.
- **Findings can carry an optional `line`.** SARIF emits a `region` only when one is present and a file-level location otherwise. Exactly one check populates it today, `U12` (mermaid-valid), chosen because it already computed the exact line and interpolated it into its own messages, so carrying it onto the finding was a one-line addition per call site rather than new scanning work. **Emitting `startLine: 1` to fill the field was explicitly rejected**: inventing a location is the one thing a deterministic gate exists not to do. Retrofitting the remaining checks is a separate release.

### Note on numbering

New capability, no new spine check, no Standard change, and nothing that moves an existing plugin's verdict. That is a MINOR.

## [1.10.1] - 2026-08-11

A trust patch. No new capability, no new check, no Standard movement, and nothing a third party is graded by moves: spine stays **30 checks**, Standard stays **v0.12**, tier stays Advanced. Its job is making the repository's own claims true again, and converting four of them from things somebody has to remember into things a machine checks. **743 tests, 0 failures** (was 613), gate Advanced 0/0. Release packet: `docs/internal/release-plans/plan_v1.10.1/`, including the adversarial review record: **four rounds, twelve findings**, most of them introduced by this release. Pass counts are not quoted because the platform-specific halves of the argv coverage skip on the opposite platform by design: the total is the same everywhere, the skip distribution is not.

### Fixed
- **The `chain` migration traded a loud failure for a silent corruption, and the silent one is worse.** PR #204 moved the delegation chain from a top-level `chain:` key to `metadata.chain` because the agentskills.io vocabulary forbids unknown top-level fields. That cleared the rejection. It did not clear the problem. The specification defines `metadata` as "a map from string keys to string values", and the reference implementation enforces that by **coercion rather than rejection**: `skills_ref/parser.py` runs `{str(k): str(v) for k, v in metadata["metadata"].items()}` over the whole namespace. A YAML list therefore survives as a Python list repr. Measured on the shipped file: `metadata.chain` came back as the **string** `"['askit-skill-author', 'askit-reviewer']"`. `agentskills validate` reported "Valid skill" for all 24 skills throughout, because `validator.py` checks that top-level keys are in `ALLOWED_FIELDS` and never inspects `metadata` contents at all. A pass that happens because nothing looked is the same defect class as the phantom `agents/README.md` subagent and the self-concealing index drift, both found one release earlier. The declaration is now a comma-separated string in `askit-build-skill` (0.1.3), `askit-evaluate` (0.1.3) and `agents/askit-skill-author.md` (0.1.2), and it round-trips through the reference parser unchanged. `S4` (chain contracts) gained a `parseChainDeclaration()` that reads the string form (splitting on commas or whitespace), the array form, and the legacy top-level key, with `metadata.chain` still winning and no merge. **The one behavior that moves is warn-first, and the pre-release adversarial review is why.** A string-form declaration previously fell through an `Array.isArray()` guard and was silently treated as no declaration at all, invisible to both the orphan check and the chaining-in-use signal. Teaching `S4` to read it therefore makes a check newly able to fire, and a plugin with a scalar `chain: some-agent` and no contract file would have gone from passing to erroring purely by upgrading a **patch**. Per ADR 0027 (Standard versioning and compatibility policy) and ADR 0041 (warn-first string-shaped chain declarations), any finding arising from a **string-shaped** declaration is a `warn` in this release and graduates to `error` at Standard 0.13; array-shaped and legacy declarations keep their existing `error` severity untouched. Severity is decided by the value's **shape**, not by which key it was read from. Because `gateExitFromFindings` counts errors only, **no plugin's tier and no plugin's exit code moves.** Proved positively rather than by a green gate: removing `askit-reviewer` from the real contract made the orphan finding fire against the migrated string-form declaration, then the contract was restored. This repository's own gate can never test that claim on its own, since its contract permits every edge it declares, so the coverage lives in a dedicated `chain-string-no-contract` anti-fixture.
- **`library.json` disagreed with five components about their own version, and the count is the point.** It entered this release as a two-component finding, because two components were what PR #204 happened to touch. Comparing all 33 registered components against their own frontmatter found **five**, in two directions from two separate causes: `askit-build-skill` and `askit-evaluate` (manifest 0.1.0, frontmatter 0.1.2) plus `askit-skill-author`, `askit-reviewer` and `askit-quality-grader` (manifest 0.1.0, frontmatter 0.1.1). `S8` (components-mirror) mirrors `status` and `tier` and has never compared `version`. An ungated field does not drift where somebody looked. All five are corrected, and `tests/unit/component-version-mirror.test.mjs` now fails the build on any disagreement, treating an unreadable component version as a failure rather than a skip. That guard is **repo-local**, in the family of `scripts/check-readme-version.mjs`: it carries no Standard implication and moves no third-party verdict. Whether `S8` should require this of everyone is recorded as backlog E24, ADR-gated under ADR 0027 (Standard versioning and compatibility policy), because tightening a check third parties are graded by is a different act from fixing our own records.
- **The release checklist promised a README check that was only half built.** `docs/internal/RELEASE.md` describes itself as "the committed, one-to-one mirror of the automated release-readiness gate" where "CI enforces every item so nothing here depends on anyone remembering it", and lists "README Status matches the declared tier + version (drift = error)". Only the shields.io badge was ever compared. The `## Status` section's own version, skill count and spine size were unguarded prose, which is how a front-door claim goes stale for two releases without anything noticing. `scripts/check-readme-version.mjs` now also reads that section and fails on a version that disagrees with `library.json`, a skill count that disagrees with the registered components, or a spine size that disagrees with the live check registry, reporting every disagreement in one run instead of one per invocation. It caught the v1.10.1 bump immediately: `README.md:379` carried a stale version the badge check would have passed. A missing `## Status` section is a hard failure rather than a skip, and the section is located by splitting on the heading rather than by a lookahead for the next one, because the first implementation would have silently stopped checking if `## Status` ever became the last section in the file. **The tier half of that same promise was still missing, and round 3 of the review caught it:** the guard checked version, skill count and spine size while its own docblock cited the tier contract as its justification, so the README could claim a different Bronze, Silver or Gold grade than `library.json` declares, or drop its tier claim entirely, and the guard still reported green. It now reads `library.json.tier` and requires a matching Status claim, failing in both directions and failing on a missing claim. **Round 4 then found that the first version of this check accepted a contradiction:** it tested whether the claim contained either expected synonym anywhere, so with `tier: advanced` the string `Advanced (Silver)` passed because it contains `Advanced`. The guard added to prevent public tier drift would have greenlit a README naming two grades in one line. The rule is now "must name the declared tier and must name no other", which rejects the contradiction while still accepting a reasonable single-token claim such as a bare `Gold`. **Round 5 then found the bypass one layer down:** the rule was implemented with `statusBody.match(...)`, which returns only the **first** match, so a Status section carrying two `**Tier**` lines passed on the strength of the first while the contradictory second was never inspected. Every tier claim is now collected with `matchAll` and **exactly one** is required, since a section describing present state has no business stating two grades. The neighbouring skill-count and spine scans had the identical first-match-only shape and were fixed at the same time rather than left one line below the hole just closed, which is the mistake that produced two earlier findings in this release; those two take a deliberately different rule, requiring every occurrence to **agree** rather than requiring exactly one, because neither has a single canonical bullet and a legitimate second correct mention is possible. The Bronze/Silver/Gold mapping was private to `report-render.mjs` rather than shared; it moved to `scripts/lib/tier.mjs`, and **round 4 found one more copy still standing**: `scripts/generators/gen-index.mjs` kept its own `TIER_LABEL`, so `G4` (generated-docs drift) output could diverge from the reports and the README guard while the changelog claimed a single source. The two mappings turned out to be identical in shape and values, so the local copy is removed and `INDEX.md` regenerates byte-identically. One mapping now, with a generator test asserting no local redefinition rather than a sentence asserting it.
- **Argv paths were passed through unnormalized, and one report echoed the raw spelling.** Every CLI entry point took a filesystem path from `argv` and applied no normalization: `check.mjs`, `evaluate.mjs`, `tier-report.mjs`, `standards-watch.mjs`, `eval-run.mjs`, and the three generators, covering path-valued flags as well as positionals. `docs/how-to/troubleshoot-the-gate.md` documented the consequence as a caveat readers had to remember, and `tests/unit/eval-run.test.mjs` carried a comment reading "a backslash path once graded nothing and printed a clean pass". **Stated precisely, because the caveat overstated it:** on native Windows the entry points already resolved backslash paths correctly, since Node's win32 `path.join` accepts either separator. The severe grades-nothing mode is cross-platform, a Windows-spelled path handed to a POSIX Node process, and the one reproducible same-host defect was `evaluate.mjs` echoing the un-normalized spelling into its JSON report's `target` field. All of it is closed by `normalizeArgPath` in `scripts/lib/fs-utils.mjs`, which is **guarded on the path separator**: on POSIX a backslash is a legal filename character, so normalizing unconditionally would be the identical defect pointed the other way. **It also does not trim, and an earlier draft did.** The pre-release adversarial review caught that leading and trailing spaces are legal in a POSIX filename, so `/srv/plugin ` and `/srv/plugin` are two different directories, and three callers of this function write files (`gen-index`, `gen-manifest` and `sync-agents-md` in `--write` mode). Trimming would have silently emitted generated files into a sibling directory the caller never named, which is a strictly worse outcome than reading the wrong tree, and the first draft's tests had pinned that behavior in place. The separator conversion is now the only transformation applied, with an integration test that builds two real roots differing only by a trailing space and proves a `--write` run touches only the one it was given. The separator is a parameter so both branches are exercised deterministically on any host rather than one of them being skipped by whichever runner happens to execute the suite. `resolvePosix` in the eval-run pipeline deliberately keeps swapping unconditionally, because it normalizes a clone path from the tracked corpus manifest that may be authored on one machine and read on another, and both functions now carry cross-referencing docblocks so neither gets collapsed into the other later.
- **`emitPin` inherited a `repoHeadSha` it never verified.** The re-pin proposal is built by spreading the previous `verified` block, so the prior commit sha survived into a document whose every blob SHA had just been refreshed around it. This re-pin's proposal offered `38a2ff82`, the value recorded on 2026-07-27, while upstream HEAD was `69ef37e9`, and it sat beside a `by` field reading the literal string `"unrecorded"`. `upstream-pin.json` exists so a reviewer can check every value by hand, offline, without trusting the tool, so a stale fact inside `verified` defeats the file's purpose. `emitPin` now **drops** a verification fact the caller did not supply rather than inheriting one. The documented human-review step is what caught this, which is the process working; the fix is because a correct outcome that depends on someone looking is not yet a fix. **The same wrong rule was applied to a second field, and fixing the first did not prompt a look at the second:** the pre-release adversarial review found that `lastUpstreamCommit` behaves identically and worse. `emitPin` refreshes each artifact's `blobSha` and `surface` while leaving its commit provenance untouched, so a re-pinned artifact names the commit that produced the **previous** bytes. The committed pin proved it, carrying blob `d9a2db099d90` beside commit `6868401b` from 2026-05-16 while ADR 0040, written in the same change, states the change arrived in `217be548` on 2026-08-04. Both fields now follow one rule: provenance for an artifact whose bytes moved is dropped unless the caller supplies a replacement through the new `artifactCommits` option, and an artifact whose bytes did not move keeps its provenance because that fact is still true. The committed pin is corrected against the upstream API. Recorded as backlog E25, now closed apart from one noted residual: the CLI does not yet look up commit metadata, so a re-pin drops the field and a reviewer restores it, which is a manual step but an honest one.
- **The two flagship skills declared their delegation chain in a top-level `chain:` frontmatter field the agentskills.io vocabulary does not allow.** The reference validator's allowed top-level keys are `allowed-tools`, `compatibility`, `description`, `license`, `metadata`, `name`; `metadata` is the sanctioned extension namespace, and `chain:` at the top level is not on that list. `askit-build-skill` and `askit-evaluate` both carried it there, so the spec's reference validator rejected them while this toolkit's own gate passed them clean. The field now lives under `metadata.chain` in both `SKILL.md` files, each bumped from `metadata.version` 0.1.1 to 0.1.2, and in `agents/askit-skill-author.md`. The `S4` chain-contract check reads the new location, preferring `metadata.chain`, and keeps reading the legacy top-level `chain:` as a fallback so no third-party plugin's existing S4 verdict moves. The authoring docs now teach `metadata.chain` as the location, with a one-line note that the top-level form predates Standard vocabulary alignment and is still read for compatibility. **Superseded within this same release, before it ever shipped:** the list form under `metadata` turned out to be silently mangled by the reference implementation, so the value became a comma-separated string and both skills went to 0.1.3. See the first entry in this section. This entry is kept rather than merged because the intermediate state is the evidence for why the second fix was needed, and because a reader tracing `metadata.chain` through the git history will land here first.
- **v1.10.0's `gen-index` fix put consuming plugins into `G4` drift and nothing told them.** The fix was correct and its consequence was recorded nowhere a consumer would see it: v1.10.0 made the two boilerplate `INDEX.md` sections filter by `existsSync` instead of asserting this toolkit's own layout, so any plugin whose index was generated by the previous generator now disagrees with the current one and earns a `G4` error on upgrade. `RELEASE-NOTES.md` gains the `### Upgrade` section v1.10.0 shipped without, the only recent release missing one. It states the expected error, the regeneration command, and the three facts that stop it being read as a regression: the error is the check finally being able to see a wrong index rather than a new defect, the diff is small and subtractive, and a plugin shipping every artifact renders byte-identically and sees no drift at all. **Measured, not asserted:** `thinking-framework-skills` v0.13.0 declares `tier: advanced` and grades **Convergent at 1E / 128W** today, its single error is `G4`, and its committed `INDEX.md` links four paths it does not have (`STANDARD.md`, `docs/internal/decisions/`, `docs/internal/backlog/`, `docs/internal/STATUS.md`). A dry-run regeneration removes exactly two lines and changes nothing else.
- **The `G4` remediation told consumers to run a command they do not have.** `docs/how-to/troubleshoot-the-gate.md` carried `G4` as a one-line entry whose fix was `node scripts/generators/gen-index.mjs . --write`, which resolves only inside this toolkit. Nothing installs the generators into a consuming plugin, and the marketplace member currently failing over `G4` has no `scripts/generators/` at all. `G4` is promoted from the brief list to a full section beside `G3`, splitting the inside-the-toolkit form from the consumer form, showing the diff-before-write sequence, and naming two traps the repo has hit before: the plugin root is **positional** rather than a flag, and a Windows backslash path is silently read as a different directory.

### Added
- **`npm run release-counts`, because a number this release documented going stale went stale four more times while it was being documented.** The changelog published `647 tests` when the truth was `667`; `docs/internal/STATUS.md` carried the pre-release `613`; the release packet claimed `673` against `682` one paragraph after stating it had been rebaselined against HEAD. Three of those were corrected by hand and the defect came back, each time found by an adversarial review round rather than by anyone re-reading. `scripts/check-release-counts.mjs` runs the suite, reads the authoritative total from the TAP summary, and fails on any disagreeing count in the newest changelog section, in the `Tests` row of `STATUS.md`, or anywhere in the current release packet, deriving the version from `library.json` rather than hardcoding it. It compares **totals and failures only, never pass counts**, because the argv coverage skips its Windows-only half on POSIX and its POSIX-only half on Windows, so the same total reports different passes on each platform. It is wired to the release gate and deliberately **not** to `npm test`: running the suite from inside the suite is circular, and doubling test time on every push to guard a once-per-release number is the wrong trade. Its detection shape is narrow on purpose and the limit is documented rather than assumed away: a count phrased without an adjacent failures figure slips through, and a looser pattern would fire on this release's own historical narration every run, which teaches maintainers to ignore it. The release packet also stopped restating the count at all, because deleting a hand-maintained copy beats checking one. **This is the release's own thesis demonstrated on itself the hard way:** the defect recurred four times while everyone involved knew about it, was documenting it, and was looking for it, and it stopped when a script started checking.
- **A `windows-latest` CI job.** `.github/workflows/ci.yml` gained `validate-windows`, running `npm ci`, the full suite and the conformance gate on Node 24, so the platform whose path handling this release fixed is actually exercised rather than reasoned about. It is a separate job rather than a matrix entry on the existing `validate`, deliberately: renaming that job once before silently broke the required-status-check contract and left every subsequent pull request hanging. **`validate-windows` is a new required-check candidate and branch protection does not pick it up automatically.** Until a maintainer adds it, a green pull request implies Windows coverage it does not have.
- **A recorded first-party validator parity baseline.** `docs/internal/release-plans/plan_v1.10.1/validator-parity-baseline.md` records what the vendors' own validators say about this repository at this tag, with tool versions, the prior baseline for the delta, and the reproduction commands. Measured live: `claude plugin validate .` passes in both default and `--strict` modes; all 24 skills pass `skills-ref` 0.1.1, up from 22 of 24 before the `chain` migration; and `claude plugin validate templates/seed-plugin --strict` **fails** with "No manifest found in directory", recorded as a known open tension rather than quietly fixed, because the Standard places native manifests at Silver while the README claims Bronze installability and deciding which moves is a Standard question. Until now the evidence for the portability claim was that our own gate said so.
- **A finding-level migration cap, so a warn-first guarantee survives severity resolution.** Round 2 of the pre-release adversarial review found that the warn-first fix from round 1 did not actually hold: `runGate` applies `askit.config.json` per-rule overrides **after** a check emits its severity, so a plugin configured with `rules: { "S4": "error" }` had the new warning escalated straight back to a failing error, reproduced at `exitCode 1`. **The hole is older and wider than this release.** `applyStandardDowngrade`, the ADR 0027 (Standard versioning and compatibility policy) pinned-Standard downgrade, also runs before `resolveFindings`, which means every warn-first migration this repository has performed is config-escalatable; `U13` (skill-registration) is the live instance, shipping as `warn` at Standard 0.12 today. A finding may now carry `migration: { capAt, until, reason }`, applied last in `resolveFindings` after profile, per-rule override, suppression and the published-verdict clamp. It is a **ceiling and never a floor**, implemented as a rank comparison so `off` sits below `warn` structurally: a capped finding is only ever lowered, a suppressed one stays suppressed and does not reappear in the warn count, and a consumer can always still silence it. The cap is not silent either: a `migrationNotice` reaches the terminal, the JSON, and both the Markdown and HTML reports, so someone who set an override and got a warning is told why rather than watching their configuration appear to be ignored. **That last part took two rounds to get right.** Round 3 found the notice reaching the terminal and JSON only, while the designed renderers projected `lead.message` and dropped it, which made the claim above false in exactly the artifact a third party is most likely to read. Round 4 then found that round 3's fix copied the notice from the **lead** finding alone, so a real result carrying an array-shaped orphan error alongside a capped string-shaped warning made the error the lead and lost the warning's notice again: the fix worked only when the capped finding happened to sort first. All unique notices for a requirement are now collected and rendered, deduplicated. Fixing it surfaced a pre-existing instance of the same gap: `clampNotice`, the published-verdict clamp's equivalent explanation, has **never** reached either renderer. That one is deliberately left alone and filed as backlog E28, because it is an untouched code path and a release already three review rounds deep is the wrong place to add an unreviewed behavior change. `U13` is deliberately untouched here and recorded as backlog E26; lowering a severity is always safe under ADR 0027, so that work is bounded by scope rather than by policy.
- **ADR 0041 (warn-first string-shaped chain declarations), Accepted.** Records the one behavior change in this patch as a scheduled tightening rather than an unversioned one, with its graduation to `error` at Standard 0.13. It states the counter-argument rather than omitting it: there is a real case that this is not a tightening but a check that was blind and now sees, which is exactly how v1.10.0 shipped the `G4` (generated-docs drift) fix, as a MINOR with an upgrade note that newly failed a marketplace member at its declared tier, on the reasoning that "the error is the check finally being able to see a wrong index rather than a new defect." The ADR distinguishes this case on MINOR-versus-PATCH grounds: a patch that moves a verdict is not a patch. It also records **how this got through**: the release plan asserted a no-verdict-moves invariant while the implementation carried an explicitly authorized carve-out against it, and the claim went unverified against the orphan and missing-contract paths because this repository's own gate structurally cannot exercise them.
- **ADR 0040 (re-pin agentskills.io after an editorial metadata clarification), Accepted.** The first watch run since 2026-07-27 returned `VERDICT: material-change`, flagging that the specification's `metadata` field constraint had changed to "a map from string keys to string values". Reading the diff rather than trusting the classification found the opposite of a tightening. The specification changed by exactly two hunks across 245 lines, and the parenthetical the upstream added to its summary table **already existed verbatim in the pinned revision**, in a `#### metadata field` subsection that is byte-identical between the two. The upstream synced a table to prose it had already published. Its own commit agrees: `217be548`, "Merge pull request #479 from jonathanhefner/issue-474-**clarify**-metadata". The second unclassified delta, a new sentence under `## Optional directories`, adds no directory and removes none, so there is no coverage gap either. Decision: **re-pin only**. No check changes, no Standard bump, no ADR 0027 burndown, because there is no new requirement to burn down, and spending that budget on an editorial sync would move third-party verdicts for a rule nobody added. The decision also records two limits found along the way: the watcher's `material` class means "structurally decidable", never "important", which is why an ADR is where the judgment happens; and passing `agentskills validate` is weaker evidence than it reads as, because the validator never inspects `metadata` contents, so the parity harness scoped for the next release must assert on **parsed values** and not only on exit codes or it will reproduce this blind spot in CI and call it coverage.
- **A release packet, reviving a practice that lapsed after v1.9.0.** `docs/internal/release-plans/plan_v1.10.1/` carries the README, the item-by-item plan with acceptance criteria and an explicit out-of-scope list, and the parity baseline. v1.10.0 shipped without one.
- **A second way into the documentation, routed by question instead of by reader.**

### Changed
- **`docs/internal/STATUS.md` calls itself the single live source of truth and had become a 151-line accretive log.** It carried its own indictment: a reconciliation note added 2026-07-27 records that four roadmap rows described shipped work as open, and concludes "a roadmap is a document asserting facts about a repo, and nothing was checking it." Rewritten to 104 lines of current state, open set, and forward roadmap. Deleted: the per-release paragraphs for v1.4.0 through v1.9.0 (duplicated in `CHANGELOG.md` and `RELEASE-HISTORY.md`), the `Phase progress` and `DoD burndown` tables describing a build that finished at v1.0.0 with every row reading `done`, the 2026-05-30 scope decision, and a `Next action` section frozen at 2026-06-02. The facts in its Current state block are deliberately one per line so a currency check can read them.
- **`docs/internal/RELEASE-HISTORY.md` was frozen at 1.9.0.** Its "Where we are now" footer had missed v1.10.0 entirely. Both v1.10.0 and v1.10.1 sections added and the footer brought current. The footer now states plainly that nothing checks it and a maintainer updates it by hand, which is the honest reading rather than an aspiration.
- **`docs/internal/execution/EXEC-SUMMARY.md` carried pre-renumbering release labels.** Marketplace scope is now targeted at v1.12.0 and the manage-and-studio work is deferred indefinitely; a dated note records the renumbering so nobody re-derives it from the commit history.
- **The upstream pin moved to the current agentskills.io revision.** `docs/specification.mdx` re-pinned from `20cf9f6b6723` to `d9a2db099d90` with `verified.date` 2026-08-11; the three `skills-ref` blobs are unchanged. `npm run standards-watch` now reports `VERDICT: unchanged`, so the next non-clean run means something. The pin had been 15 days stale. New public page `docs/explanation/document-map.md`. `reading-paths.md` routes by who you are and does that job well, but a reader who arrives holding a specific question ("what actually backs a tier claim?", "what does a conditional check mean?", "why should I believe the positioning?") had no page that answered it without first picking a persona. The map opens with the five jobs a document here does - normative, verification, legitimacy, qualifier, instructional - because knowing which job a page does tells a reader what kind of answer to expect and what it will not give them. Five question groups follow, each row pairing a question in the reader's own words with the one document that answers it. It defers to `INDEX.md` for components and to `reading-paths.md` for persona routes rather than restating either, so there is nothing to drift. Public documents only: `docs/internal/` is named as existing, with its conclusions traced to the published pages that carry them (`comparison.md` for the research, `validation-and-improvement.md` for the evaluation runs, `limitations.md` for the known-weak spots), so a reader can follow the chain without the map pretending working notes are documentation. The page closes by saying plainly that the folder inventories and site routes are checked mechanically but its own descriptions are not, so a wrong row is a bug in the map rather than in the document it points at. `reading-paths.md` gains one reference-shelf row pointing here.

## [1.10.0] - 2026-08-07

### Added
- **ADR 0039 (marketplace-scope evaluation), Proposed.** The gate resolves plugin and component scopes only, so a catalogue is graded by a person looping over members and three defect classes stay structurally invisible: cross-member skill and command collisions, a catalogue entry that resolves to nothing while every member that did resolve grades green, and a registry entry version disagreeing with the member's own manifest. The ADR settles the three questions that shape every downstream change. **Which tree a run grades:** the local checkout, with the registry pin sha, entry version, graded sha and a divergence marker mandatory in the report, and remote fetch-at-sha deferred rather than rejected. **How members aggregate:** self-consistency worst-member, so each member is graded at its own declared tier and its own Standard pin and the collection reds if any member fails its own claim, because the family marketplace is heterogeneous on both axes and a uniform rule would grade a Universal member against a Gold expectation it never made. **Whether anything becomes a spine check:** no, everything ships as scope-local deterministic findings, so the capability lands without retightening the 30-check contract every existing plugin is held to; graduation is deferred to its own ADR with the warn-first burndown `U13` established. No code merges until this is Accepted; three open questions are listed for ratification.
- **The toolkit now states what it cannot do.** New public page `docs/explanation/limitations.md`, written because a quality tool that never states its own boundary is asking to be trusted further than it has earned. It collects, in one place and with the evidence: the gate checks structure and never content (deliberate, Design Principle 3); the advisory layer is measured rather than guaranteed, with the E13 recall figures showing 0.38 to 0.62 at three tiers, so a clean review is not evidence a plugin is clean; `U5` is mathematically unpassable in a language its English trigger pattern does not know (0 of 346 on a French corpus); `G3`'s skill branch is a documented no-op; `U6` never scans commands or subagents; nothing resolves `.claude-plugin/plugin.json` component paths; there is no marketplace scope; cross-agent support is deliberately asymmetric. Each limit is labelled deliberate, tracked, or not-built, so a reader can tell which will change.
- **A working practice for more than one plugin.** New public page `docs/how-to/manage-multiple-plugins.md`. The gate has plugin and component scopes only, so a catalogue is graded by looping over members, and the page is explicit about what that loop cannot see (cross-member skill and command name collisions, ambiguous routing across members, an unresolvable registry entry). Covers the plugin-versions-registry-points authority split, why `S2`'s prefix rule is the highest-leverage early decision in a portfolio, the end-to-end re-pin sequence with its smoke verification, how a Standard bump rolls across a portfolio via pins and `--strict`, and a one-repo-versus-many comparison. Two mermaid diagrams.
- **Nine operational FAQ entries.** `docs/explanation/faq.md` had nine questions, all defending design choices and none answering a user in trouble. Added the questions actually asked when something goes wrong: zero errors does not mean good; hundreds of errors on a third-party plugin means the wrong profile; a clean pass that read nothing is a Windows backslash path; checks that fire on deliberately non-live content; the non-English description cap; whether a Python plugin works; how to grade a whole marketplace; whether a new Standard check fails an existing plugin; and whether a new plugin should pin an older Standard.

### Fixed
- **`G8` required a folder README in `agents/`, and the runtime loads it as a subagent.** Claude Code discovers subagents by scanning `agents/` for `*.md` and loads every one it finds, so requiring a folder guide there made every conforming plugin register a phantom subagent with no name and no description. Established empirically: a probe plugin holding `real-agent.md`, `README.md`, `_README.md` and `README.txt` was loaded with `claude --plugin-dir` and asked to enumerate its subagents, and returned three - `real-agent`, `README` and `_README`. The underscore prefix does not protect a file; only the non-`.md` extension was skipped, and there is no warning or error. Both plugins that followed the rule shipped one: this toolkit, and `critique-skills`. `agents/` is removed from `FIXED_ROOTS`, joining the repo root and `templates/seed-plugin` as a documented exclusion, and this repo's own `agents/README.md` is deleted. The change removes a requirement without adding a prohibition, so a plugin still carrying the file mid-migration is not newly failed. **The lesson is in our own history:** `docs/internal/release-plans/plan_v1.1.0/P4-folder-readme/SPEC.md` records that adding `agents/README.md` already caused this once, and it was "fixed at the single enumeration point: `README.md` is excluded from component discovery everywhere". That fixed the toolkit's idea of what an agent is and left the runtime's untouched. The same mismatch is still live and unprobed for `commands/`, recorded in the backlog. **612 tests.**
- **`gen-index`'s two boilerplate sections described this toolkit, not the plugin being indexed.** Every other part of `renderIndex` is data-driven from `ctx.library` and component frontmatter; `## Manifests` and `## Documentation and governance` were fixed string lists of the toolkit's own repo layout, rendered verbatim for every consuming plugin. Any plugin that ships no `.codex-plugin/`, no `manifest.generated.json`, no `templates/`, no `agents/_chain-permitted.yaml`, or no `docs/internal/STATUS.md` (which is to say any plugin below Advanced, and any Claude-only plugin) received an `INDEX.md` asserting paths that are not in its tree. The failure was self-concealing: `index-drift` (G4) compares a plugin's on-disk INDEX against this same generator, so the dangling links matched the expectation and passed the drift check forever. Reported by `critique-skills`, which had **seven dangling links out of twenty-four** and had to carry a local post-processing filter to strip them, which then put it in permanent G4 drift for being correct. Both sections are now row tables filtered by `existsSync` against the plugin root, with per-fragment filtering inside composite rows, so a row keeps only the artifacts that exist and a section with no surviving row drops its heading entirely. Fragment text carries no trailing period and a row joins survivors with `"; "`, so **a plugin that does ship every artifact renders byte-identically to the previous fixed strings**: no existing `INDEX.md` changes unless it was asserting something that is not there. Verified against the toolkit's own INDEX (byte-identical) and against `critique-skills` (all seven phantom rows gone). **610 tests.**
- **`SKIP_DIRS` covered the Node ecosystem's scratch directories and not Python's.** The shared set (`scripts/lib/fs-utils.mjs`) listed `node_modules`, `dist`, and `.astro` but no Python equivalent, so `G8` folder-readme walked into bytecode and tool caches and reported them as under-listed inventory children. A plugin is graded against a set that is complete for one language and not another, which makes the finding the toolkit's fault rather than the plugin's: `critique-skills` was reported **three of its five above-tier findings** for `__pycache__` directories alone, and the same would hit every Python-bearing plugin in the family. The set now covers both ecosystems, grouped by category (version control, dependency directories, build output, tool caches, scratch) so the next gap is visible rather than latent: added `__pycache__`, `.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `.tox`, `.venv`, and `venv`. Separately, `S6` source-doc kept its **own private copy** of `SKIP_DIRS`, a leftover from the v0.11 consolidation that moved the set into `fs-utils.mjs` for `U12` and `G8` only; it would have silently stopped matching the shared set the moment either was edited, and since `G9` walks `.py` as well as `.mjs`, a vendored virtualenv under a scope root would have been graded for docblocks. It now imports the shared set, and a new test asserts no repo-wide content scanner redefines `SKIP_DIRS` locally. **607 tests.**
- **Three ADRs named symbols that were never written.** v1.9.0 added `## Implementation sites` to every decision record so a decision names what carries it, found by grep rather than recall. The retrofit was itself written from recall, and three of the fourteen sections pointed at nothing: ADR 0027 (Standard versioning and compatibility policy) attributed `applyStandardDowngrade` to the wrong file and described a `standardGate` wrapper that does not exist, ADR 0032 (calibrate U6 inline code and U12 non-live mermaid) named `stripInlineCode` where the function is `stripCode`, and ADR 0033 (recalibrate U5 description scorer) named `ACTION_VERBS` and `WHEN_PATTERNS` where the regexes are `ACTION` and `WHEN`. All three corrected against source, each carrying a dated correction note. ADR 0027 also gains the site it was missing: `templates/seed-plugin/library.json`, the omission that let the seed pin sit four Standard minors stale. `tests/unit/adr-implementation-sites.test.mjs` now asserts that every symbol named in an Implementation sites section resolves to real source under `scripts/`, `tests/`, `hooks/`, or `site/scripts/`. It asserts existence only, never completeness, which remains the judgment call the v1.9.0 session deliberately declined to automate. **603 tests.**
- **The seed scaffold pinned a stale Standard.** `templates/seed-plugin/library.json` declared `"standard": "0.8"`, written once during v1.0.0 (commit `e06a377`) and never touched while the Standard moved 0.8 to 0.12. That pin is copied verbatim into every plugin `askit-init-plugin` creates, and per ADR 0027 (Standard versioning and compatibility policy) the gate downgrades error to warn for any check introduced after a plugin's pin. A plugin scaffolded today would therefore have been born silently opted out of six checks: `U12` (mermaid-valid), `U13` (skill-registration), and `G7` through `G10` (docs-frontmatter, folder-readme, source-doc, docs-presence). Honoring an old pin is correct for an existing plugin and wrong for a new one, which has no legacy to protect. The seed and the Bronze tutorial now pin `0.12`, and a new invariant in `tests/unit/init-anatomy.test.mjs` asserts the seed pin equals the toolkit's own declared Standard, so the next Standard bump that forgets the template fails the suite instead of shipping. No behavior change to the gate; the seed still grades Universal with 0 errors. **602 tests.**

## [1.9.0] - 2026-07-27

Two governance capabilities, both aimed at the same failure: a rule that exists and is not carried to every place it applies. Outward, the toolkit can now tell when the upstream spec has moved. Inward, every decision record names what implements it. Spine stays **30 checks**, Standard stays **v0.12**. **601 tests**, gate Advanced 0/0.

### Added
- **`askit-standards-watch` (skill 24).** `STANDARD.md` section 6 states, normatively, that where agentskills.io evolves the Universal tier MUST track it. Nothing implemented it, and no upstream version was pinned anywhere, so "has the spec changed?" was not an answerable question. It now is. The pin unit is a **git blob SHA-1 per normative artifact**, not a repo HEAD or a version string: the upstream publishes no tags or releases, and its HEAD moves on unrelated commits while `specification.mdx` sat unchanged from 2026-05-16 to today. Blob SHAs are reproducible with `git hash-object` by a reviewer who does not trust the tool.
- **`## Implementation sites` on every ADR.** The template, `askit-decision`, and 14 retrofitted decision records now name the exact files and functions that carry each decision, found by grep rather than recall. This addresses the failure behind three defects in two days: ADR 0030's guard implemented in one of two renderers, ADR 0034's flag validated in one of two scopes, and one escaping mistake made twice independently.
- **`scripts/lib/md-escape.mjs`.** One shared Markdown table-cell escape.

### Changed
- **The report's HTML path no longer renders a null declared tier.** ADR 0038 fixed five sites; the retrofit's grep found four more in the HTML exec paragraph, masthead, ID strip, and metadata section, which were still emitting a blank tier name and a literal `null (null)`. **The discipline found this on its first use, inside the very ADR that named the pattern.**
- **All three Markdown cell escapes now share one primitive.** CodeQL found the same `js/incomplete-sanitization` defect a fourth and fifth time in new code, written by a third independent author. When the first two were fixed the note read "worth revisiting if a third appears" - a third appeared, so the opportunity is removed rather than the bug corrected again.

### Note on numbering
A new skill is a MINOR, so this takes v1.9.0. The uplift program had reserved that number for marketplace scope, which becomes **v1.10.0**; manage-and-studio becomes **v1.11.0**. Renumbering an internal plan is cheaper than misnumbering a public release.

### Added
- **`askit-standards-watch` and the upstream pin.** `STANDARD.md` sec 6 has said since v0.1 that "where agentskills.io evolves, the Universal tier MUST track it", and nothing implemented it: no upstream revision was recorded anywhere, so "has the spec changed?" was not an answerable question. The new pin at `docs/internal/standards-watch/upstream-pin.json` records it, content-addressed by git blob SHA-1 because the upstream publishes no version and cuts no tags or releases (verified against the repository API). Four artifacts are watched: the specification prose and the three `skills-ref` sources sec 6 points at.
- **`npm run standards-watch`,** the deterministic detector behind the skill. It fetches each pinned artifact, extracts a structural surface from the specification (the frontmatter field table, the component inventory, a body hash per section), and splits every delta into three classes: **material** (a field or directory added, removed, or re-constrained; a section added or removed) decided structurally with no judgment applied, **needs a human read** (a prose body moved, or the reference implementation changed) which it locates and deliberately refuses to classify, and **cosmetic** (bytes moved, surface identical) which it filters. It refuses with exit 2 rather than reporting a clean run when its anchors are gone. Each delta resolves to the checks it lands on by parsing `docs/reference/universal-checks.md` and joining the check registry at run time, so that mapping cannot drift.
- **A worked example built from a real upstream change.** Upstream commit `6868401` (2026-05-16) reworded one prose line about the `name` charset while leaving the frontmatter table identical, so a table-only differ would have seen nothing. `skills/askit-standards-watch/examples/pin-historical-2026-05-15.json` reproduces the detection today.

### Changed
- **The watcher proposes and cannot amend, and that is enforced rather than intended.** `scripts/lib/standards-watch.mjs` and `scripts/standards-watch.mjs` import only `readFileSync` from `node:fs` and emit the report, the ADR skeleton, and the proposed re-pin to stdout; a unit test fails the build if any filesystem write API or a `child_process` import appears in either file. Re-pinning lands as a reviewed file change beside the ADR that motivated it, never as a side effect of a run.

## [1.8.0] - 2026-07-26

Deep builders, measured advisory. The thirteen builder skills stop being scaffolders and start being teachers: every one now ships working examples, where before there were none at all. The advisory layer stops being an impression and becomes a number, with a scoring harness whose central rule is that a confident wrong answer scores worse than an honest "I am not sure". Four new third-party libraries were graded, which is where most of this release's findings came from. Spine stays **30 checks** and the Standard stays **v0.12**. **561 tests**, gate Advanced 0/0.

### Added
- **Working examples for all thirteen builders (SP2b).** 25 example documents and 7 runnable artifacts: 3 goldens plus a deliberate anti-example for each of the four complex builders (`askit-build-skill`, `-workflow`, `-mcp`, `-hook`), and a golden for each of the other nine. Before this release **not one builder shipped a single working example**. Every runnable artifact was executed during review, not just written: three MCP servers driven through real protocol handshakes, four hook scripts run against real payloads, and the formatter proven convergent by a byte-identical second run.
- **Craft-depth reference docs for the four complex builders (SP2a).** The stubs were 22, 32, 36 and 41 lines. They are now real craft documents: the patterns, the anti-patterns, worked reasoning for the judgment calls, and the *why* behind each Standard section they cite. Every claim is grounded in a recorded sensor reading, an ADR, or the actual behavior of the check module, read from source.
- **The seeded-defect fixture and scoring key (F3 R-AQ-1).** A gate-clean plugin carrying nine known qualitative defects, plus three bait entries that are *not* defects, and a key precise enough to classify a finding against. Gate-clean is the load-bearing property: a fixture the deterministic gate catches would measure the gate instead of the judgment layer.
- **The advisory precision-and-recall harness (F3 R-AQ-2), `scripts/lib/advisory-score.mjs`.** Scores an advisory result against the key and computes precision and recall per model-and-effort cell. It dispatches no model, proven three ways including a `fetch` spy and a nonexistent-path run. Reproducible: the same inputs twice yield an identical partition, byte-identical CLI output, and no timestamp in the result object.
- **`covers.skill` triggering eval sets** for `askit-evaluate`, `askit-build-skill`, and `askit-migrate` - the one eval-set form that had never existed on disk. 24 cases each, built around the near-miss boundaries that actually confuse routing.
- **Corpus batch 3:** four new third-party libraries graded on the F2 runner, its first real exercise at scale. Sensor readings 18 to 25 filed with dispositions.

### Changed
- **Authoring token costs are MEASURED, not estimated (F5).** Six real authoring runs, measured while the builders authored the examples above. A golden-plus-anti set for one component type runs roughly 100k to 175k tokens. Four caveats are recorded with the numbers, because a cost figure quoted without them will be wrong: the effort cells are not the ones planned (the Agent tool has no effort parameter), one row covers its first round only and is deliberately not extrapolated, orchestration cost is in no row, and a batched row is not a unit price.
- **Both defective templates fixed.** `templates/mcp.json` and `templates/hooks.json` each pointed at a file that has never existed. Both now use the repo's own `REPLACE-` placeholder convention. The MCP defect is preserved verbatim as the teaching anti-example, so the mistake that shipped becomes the lesson.
- **`askit-build-hook`'s craft doc now states that `additionalContext` is never a top-level key.** It nests under `hookSpecificOutput` beside a matching `hookEventName`. A bare top-level key is structurally valid JSON that the runtime silently discards, so the hook exits 0 and looks exactly like success. Two golden examples hit this during authoring; verified against the vendor reference.

### Fixed
- **The conformance report no longer invents a declared tier (ADR 0038).** It fell back to the *earned* tier when `library.json` carried none, so a plugin that declared nothing rendered "declares the Gold (Advanced) tier and earns Gold" - and, since earned then trivially equalled "declared", the verdict card always read "matches its declared tier". A **false PASS on the artifact third parties are shown**, while the terminal gate said the honest thing about the same bytes. Found by corpus batch 3 on a real target where the report claimed "10 of 10 satisfied" and the same bytes under the default ladder returned seven errors, every one of them present in the report's own JSON. ADR 0030 had decided this two months earlier; the guard landed in `tier-report.mjs` and was never mirrored into the renderer.
- **An unreachable score in a craft doc.** `authoring-guide.md` claimed a sample description scores 0.20; measured, it is 0.00, and 0.20 is arithmetically unreachable. Found by the builder measuring every score its examples cite.

### Added
- **Working examples for all thirteen builder skills (SP2b).** Every builder now ships an `examples/` folder under the samples convention (Standard sec 7.2), where before none of them shipped a single one. The four complex builders get the full set of three golden examples plus one anti-example each: `askit-build-skill` (a bounded single-purpose skill, a multi-section skill with a real `references/` split, a delegating skill wired through `chain:` plus the contract plus a `G3` chain eval; anti = the vague description, and the measured 1.00 that is worse), `askit-build-workflow` (linear, branching, hook-participating; anti = the orphan-step trap, with both checks traced to show exactly why the gate is silent), `askit-build-mcp` (stdio, http including the managed-connector variant, and a resources server; anti = the nonexistent server file), and `askit-build-hook` (a PreToolUse guard, a PostToolUse formatter, a Stop notifier; anti = the exit-2-discards-your-JSON trap). Every remaining builder ships one golden. The examples are not hand-written prose: each is the recorded output of a metered `askit-build-*` run, and those runs are the F5 authoring-cost measurements.
- **The MCP and hook examples ship runnable artifacts, not just configuration.** Three dependency-free JSON-RPC 2.0 MCP servers (stdio, HTTP, and a resources server, all speaking `protocolVersion` `2025-06-18`) and four hook scripts, each launched and each carrying its real transcript. This is the authoring rule the MCP craft doc states, made concrete: a golden example is one you have actually run.

### Fixed
- **Two scaffold templates pointed at files that never existed.** `templates/mcp.json` shipped `"args": ["./mcp/example-server.mjs"]` with no `mcp/` directory anywhere in the tree, which is two defects in one line: a bare relative path that resolves against the agent's working directory rather than the plugin, and a target that does not exist. `templates/hooks.json` had the identical class of defect, registering `${CLAUDE_PLUGIN_ROOT}/hooks/example-guard.mjs` against a `hooks/` folder that has only ever held `no-dashes.mjs`. Both now carry explicit `REPLACE-` slots in the repository's own template placeholder convention, the MCP path uses `${CLAUDE_PLUGIN_ROOT}`, and the hook command runs through `node` with the path quoted so an install directory with spaces still works. Neither defect was ever a gate failure and neither could have been: `U11` proves a stdio server declares a non-empty `command` string, and `G1` proves a registration is well-shaped, but no deterministic, model-free, cross-platform check can resolve an arbitrary launch command. The MCP defect is deliberately preserved as the `askit-build-mcp` anti-example, so the mistake that shipped is now the lesson.
- **The `askit-build-skill` authoring guide quoted a score the scorer cannot produce.** Its worked example said `Helps with resumes and CVs.` scores 0.20; measured, it scores 0.00, and 0.20 is unreachable because the real-words signal needs eight words and the string has five. Found by measuring every score the examples cite rather than trusting them.

## [1.7.0] - 2026-07-26

Trust and craft. Every stale surface on the front page is corrected and mechanically guarded against drifting again, the skill builder learns to review craft and not just conformance, the improve loop becomes a command instead of a hand procedure, and the CI system grows the hardening a public repo should have had all along. The spine stays **30 checks** and the Standard stays **v0.12**: nothing here changes what conformance requires. **516 tests**, gate Advanced 0/0.

### Added
- **The builder craft pass: an optional improve-mode phase 2 for `askit-build-skill` (ADR 0037).** After the deterministic gate is clean, improve mode can now dispatch the `askit-reviewer` subagent against a written rubric (`skills/askit-build-skill/references/skill-craft-rubric.md`: description and trigger quality, instruction clarity, example depth including golden and anti-example presence, reference structure, token economy), render the findings through the existing `--report=review` advisory path as a durable Markdown or HTML artifact, and offer to apply only the mechanical subset with explicit consent. It answers the question the gate cannot: not "does this skill obey the rules" but "is it any good as a teacher". The gap is real and measured in the fixture that ships with it, which passes the gate 0/0 while carrying a description that scores 1.00 on U5 with a trigger naming nothing a user would type, plus link rot in `examples/`, which U6 does not scan.
- **`scripts/lib/craft-review.mjs` - the SAFE/JUDGMENT partition contract.** SAFE is a CLOSED allowlist of three mechanical categories (`broken-link`, `formatting`, `missing-frontmatter-field`), each requiring a bounded single-line fix descriptor; everything else, including any category the allowlist does not name, is JUDGMENT. Every rule fails toward "do not touch": an unknown category, a missing fix, a mismatched fix kind, a multi-line or over-length substitution (which is how an instruction rewrite would otherwise be laundered through `formatting`), a fix target that is absolute or escapes the skill directory, and any frontmatter field outside a closed bookkeeping set (`description` and `name` are deliberately excluded because they carry meaning). The applier writes only on `consent === true`, re-classifies every finding it is handed so a JUDGMENT finding cannot be smuggled past it, and refuses an absent or ambiguous literal rather than guessing.
- **31 tests**, including the two-direction invariant proof that a craft finding cannot move the gate exit code: a hostile craft advisory cannot fail a subject the gate passes, and a clean one cannot pass a subject the gate fails.
- `agents/_chain-permitted.yaml` permits `askit-build-skill` -> `askit-reviewer`, with `evals/build-skill-to-reviewer.eval.json` covering the new chain edge (G3), including the negative case that a red gate does not dispatch the reviewer.
- **The eval-run pipeline (backlog E11).** An eval run is now a command instead of a hand procedure: a tracked pinned-sha corpus manifest (`docs/internal/eval-runs/corpus.json`), a deterministic runner (`scripts/eval-run.mjs`) that refuses a drifted, empty, or dirty tree before it grades anything, reusable advisory dispatch templates for review and behavioral mode, and an aggregator that appends the run rows to the tracked record and widens the measured range in `docs/reference/token-usage-estimates.md`. The runner makes no model call and reaches the gate only through the `npm run check` script seam. Every coverage bound it applies (a `--limit`, a collection-scale sampling instruction) is stated in the run output and carried into the record, so a sampled batch cannot later read as full coverage.
- **`scripts/check-readme-version.mjs` - the README drift guard (H1.1).** Asserts the README version badge equals `library.json` `version`, and runs as the first step of `npm test`, so a stale front page now fails CI rather than shipping. It reads the version by JSON key, not by line position, so a manifest reformat cannot make it false-fire. This is PRD metric M7: README drift becomes impossible silently rather than merely discouraged.
- **`docs/reference/subagents.md` - the subagent reference page.** All seven subagents finally documented (purpose, parent skill, chain permissions), sourced from `agents/_chain-permitted.yaml`. It states the real shape rather than an idealized one: four of the seven have a declared chain edge and three have none, because only two skills declare a `chain:` at all, and `S4` validates declared edges rather than requiring every subagent to have one.
- **CI hardening (the [CI evolution plan](docs/internal/execution/05-ci-plan.md), changes 1-8).** Dependabot across three ecosystems with minor and patch grouped; a Node `[22.12.0, 24]` matrix on `validate` so the declared `engines` floor is proven rather than claimed; a blocking `npm audit --audit-level=high`; a concurrency group that cancels superseded runs; npm caching on `validate` and `release`; SHA-pinned third-party actions; c8 coverage as a report-only step; and CodeQL static analysis. The standing invariant held: no validation logic lives in YAML, so every check remains a `node scripts/` invocation a contributor can reproduce locally.

### Changed
- **`U12` (`mermaid-valid`) and `U13` (`skill-registration`) now render `N/A` rather than `PASS` when their precondition is absent (H1.8).** A plugin with no mermaid diagrams was reported as passing the diagram check; a vacuous pass reported as PASS overstates conformance. Both are now conditional, and the guard is tested in both directions: a subject that *does* have diagrams or *does* have an enumerating manifest still grades normally and never renders `N/A`.
- **The README front page is true again (H1.1).** The version badge read `1.2.0` and the check count read `29`; both were three releases stale. Corrected, and now guarded.
- **`docs/explanation/comparison.md` re-verified (H1.2).** The stale caveat about the description scorer "currently being recalibrated" is retired (ADR 0033 shipped that on 2026-06-11), every competitor claim was re-checked against primary sources on 2026-07-26, and the not-yet-published `/evaluation-reports/` showcase route now says so. The "What we are not claiming" section is kept, with its caveat corrected rather than deleted: we still make no claim to grade description quality better than the field, because we have not measured it.
- **`U6` (`reference-links`) findings now say what a link resolves against.** The message carries "resolves relative to the containing file", closing the ambiguity that led an AI review to mislabel eleven real broken links as checker false positives (eval-run sensor reading 8).

### Fixed
- **Markdown cell escaping: backslashes are escaped before pipes (CodeQL `js/incomplete-sanitization`, high).** Two independent instances, in `scripts/lib/report-render.mjs` and `scripts/lib/eval-run-aggregate.mjs`. Escaping the pipe without escaping the backslash first is self-defeating: untrusted text containing the two characters `\|` became `\\|`, which Markdown reads as one literal backslash followed by a *bare* pipe, so the value escaped its table cell and added a column. Both paths carry model-authored content (advisory finding messages; `advisory.model` and `advisory.result` in a run record), so both were reachable. The regression tests count pipes the way Markdown does - a pipe is escaped only when the backslash run before it is odd - rather than textually stripping `\|`, which is the same mistake as the bug.
- **Path separators are normalized before resolution, not after, in the eval-run pipeline.** On Windows `path.resolve()` understands backslashes so either order works; on POSIX a backslash is a legal filename character, so resolving a backslash-written absolute path produced a plausible-looking wrong path. Caught by the new Linux CI matrix leg, which is the first thing the matrix paid for.

## [1.6.1] - 2026-07-25

The trust patch. Grading five real third-party-shaped plugin repositories in the 2026-07-19 portfolio audit exposed the grader over-flagging valid Mermaid notation, and three independent assessors misreading the gate's own output in a single day. Both are fixed here. Nothing the Standard requires has changed: the spine stays **30 checks**, the Standard stays **v0.12**, both calibrated checks keep `objective` provenance, and the toolkit still self-grades Gold (Advanced 0/0, **442 tests**).

### Fixed
- **`U12` (`mermaid-valid`) no longer misreads Mermaid grammar as unbalanced brackets (ADR 0036).** `bracketsBalanced` was a generic character walk, which is correct for `flowchart` and wrong for two diagram types that overload the bracket characters: `sequenceDiagram` async message arrows (`-)` solid, `--)` dotted, where `)` is an arrowhead glyph) and `erDiagram` crow's-foot cardinality (`||--o{` and the `|o || }o }|` / `o| || o{ |{` family, where `{` and `}` are cardinality glyphs). The check now consults a per-diagram-type syntax-token table, **scoped by diagram type** so a stray `-)` in a flowchart is still the unmatched paren it looks like, and applied **rescue-only** (a closer is ignored only when the stack is already empty) so no previously-passing diagram can newly fail. Measured on the audit corpus: portfolio `U12` errors **14 -> 3**, with the 11 removed confirmed false positives.
- **`U6` (`reference-links`) no longer treats a template slot as link rot (ADR 0036).** A link whose target carries a substitution token (`{{docs_path}}/guide.md`, `{release-url}`) is filled in by a generator and has nothing on disk to resolve. This is the U6 half of the rule ADR 0032 gave `U12` for pure `{{...}}` diagram bodies, and it answers "what marks template intent" with the token at the point of use - no filename convention and no frontmatter flag a graded third-party plugin would have to adopt.
- **Corpus verification.** Re-grading the five audited repositories under `--profile plain-plugin` takes pm-skills **56 -> 43** errors and pm-skills-mcp **18 -> 14**, landing exactly on the real-defect counts the audit had hand-verified independently, before this fix existed.

### Changed
- **The gate sections above-declared-tier findings.** Findings above a plugin's declared tier cannot affect its grade or exit code, but printed in the same undifferentiated `[error]` stream ahead of a "0 error(s)" summary. They now print under an explicit header saying so, after the findings that do count.
- **The gate states pinned-Standard debt.** A plugin pinned to an older Standard printed "Tier: Advanced (no blockers detected)" with exit 0 while carrying findings that all become gate-failing the moment it re-pins. A one-line indicator now names how many findings the pin is holding back and the Standard version at which they come due.

### Added
- `docs/internal/decisions/0036-calibrate-u12-diagram-grammar-and-u6-template-slots.md` - the calibration decision, its measured before/after, and the two accepted directional trade-offs stated explicitly rather than left silent.
- 24 tests (**418 -> 442**), including four scoping guards proving the `U12` allowance does not leak outside the diagram type that owns it, and `tests/unit/gate-display.test.mjs` covering the presentation split.

## [1.6.0] - 2026-06-14

Manifest completeness, made actionable. The first growth of the Standard since the v1.2.0 relaxation: a new Universal check catches a plugin that ships skills it never registered (invisible to installers), and the evaluation report is made actionable with a per-check glossary and the missing Bronze reference page. The Standard moves **0.11 -> 0.12** and the spine **29 -> 30**; per the warn-for-one-minor burndown the new check ships as a warning (gating nobody) before it becomes a gate-failing error at 0.13, so no previously-passing plugin newly fails on the bump. The toolkit still self-grades Gold (Advanced 0/0, 418 tests).

### Added
- **U13 (`skill-registration`) - manifest-vs-disk completeness (ADR 0035).** A new Universal/Bronze check that compares the skills a plugin registers in its enumerating manifest (`library.json` `components.skills`, or a `.claude-plugin/marketplace.json` `plugins[]` catalog) against the skill directories on disk. A skill on disk but unregistered ships invisibly to installers; a registered skill with no directory cannot be delivered. It is objective and portable (it survives `--profile plain-plugin`), and distinct from `U8` (`manifest-drift`, which compares the generated native manifests to `library.json`). Introduced at Standard v0.12 and, per sec 7.7, shipped as a `warn` for the 0.12 minor - the first requirement to exercise the warn-for-one-minor burndown the v1.3.0 standard-aware gate introduced - graduating to a gate-failing `error` at v0.13.
- **Per-check glossary in the evaluation report (backlog E12).** Every report (HTML and Markdown) now carries a consolidated glossary (report section 11) explaining what each spine check verifies, in one line, sourced from the report's static metadata (zero model tokens). It covers the passing and not-applicable rows the evidence ledger leaves unexplained, so a reader who has never read the Standard can still act on a grade.
- **`docs/reference/universal-checks.md` - the Bronze reference page.** The Universal floor (`U1-U9`, `U11-U13`) now has the dedicated reference page the Silver and Gold tiers already had, including U13 and its burndown, linked from the conformance-and-tiers explanation.
- **The v1.6.0 release plan packet (`docs/internal/release-plans/plan_v1.6.0/`).** A PROGRAM-PLAN plus per-feature SPEC and IMPL-PLAN for the five efforts (F1 manifest completeness and F4 report UX shipped here; F2/F3/F5 are continuous supporting work), restoring the full specs-and-implementation-plans rigor.

### Changed
- **The Standard grows to v0.12 and the spine to 30 checks** (`U1-U9`, `U11-U13`, `S1-S8`, `G1-G10`). This is the first Standard growth since `U10` (`no-dashes`) was retired in v0.11; the warn-for-one-minor burndown means the new requirement is a warning for one minor, so no previously-passing plugin newly fails. The toolkit pins `0.12` and grades at full strength.
- **Evaluation reports are legible below 600px** (a single-column rail and content grids; the glossary table wraps instead of scrolling), completing the responsive work the `<=900px` table-scroll fix began.

## [1.5.2] - 2026-06-12

The eval-run patch: the first release whose every change came out of the new historical evaluation-run record and its observe -> verify-against-ground-truth -> calibrate loop (eleven recorded advisory runs, seventeen sensor readings). Two calibrations (the U5 description-scorer recalibration and the component-scope `--profile` fix), a doc-fix batch aligning the advisory delegates with reality, the eval-run record and methodology themselves, the measured token dossier, and a responsive-table render fix. No requirement changes: a plugin graded the default way scores the same on pass/fail (U5 warns get quieter and more honest), the spine stays **29**, the Standard stays **0.11**, and the toolkit still self-grades Gold (Advanced 0/0, 401 tests).

### Added
- **Historical evaluation-run record (`docs/internal/eval-runs/`).** The tracked log of every model-assisted evaluation run (the deterministic gate is free and reproducible, so it is never logged): one row per run with context, pinned target, report type, model, instruction-directed effort, measured tokens, wall-clock, output reference, and the toolkit findings the run surfaced. Seeded with two measurement batches (eight runs): batch 1 - five advisory runs (three review, two behavioral) across Opus/high, Sonnet/medium, and Haiku/low against a toolkit skill, a community corpus plugin, and two Anthropic skills; batch 2 - three review runs on new cells (Opus/medium, Sonnet/high, Haiku/medium) against three public PM skill libraries (RefoundAI/lenny-skills, deanpeters/Product-Manager-Skills, phuryn/pm-skills), plus the record's first same-target cross-model comparisons, completed as a full model triple (Opus, Sonnet, and Haiku at identical high effort on the same plugin). The batches filed seventeen sensor readings on the evaluator itself, the headline ones: a silently-ignored `--profile` in component scope, the aspirational `evals/` convention, a `check.mjs`-vs-`evaluate.mjs` delegate inconsistency, systematic multi-corpus U5 under-scoring evidence, a chain-vs-procedure gap in `askit-build-skill`, a manifest-vs-disk skill-drift new-check candidate, the proof case that advisory triage must be ground-truth-verified before it becomes calibration input (a high-effort advisory mis-triaged 11 real U6 link defects as checker false positives), and the A/B proof that a cheaper tier's veracity claims are not verification (knowledge-content errors only the Opus/high run caught).
- **Eval-run methodology (`docs/internal/eval-runs/METHODOLOGY.md`).** The observe -> verify-against-ground-truth -> calibrate-via-ADR+TDD promotion path for sensor readings, the field-by-field rationale for the record schema, the current structured-qualitative quality assessment and its named rigor gaps (seeded-defect advisory fixtures, same-target cross-model A/B, adversarial verification of calibration-relevant findings), and the documentation map for where each artifact lives.
- **Backlog E11 - dependable eval-run pipeline.** Intake for making the run lifecycle reproducible end to end: a tracked pinned-sha target manifest, a deterministic runner for the free half, a documented dispatch contract for the advisory half, and record/aggregate automation.

### Changed
- **Token-usage dossier advisory ranges are now MEASURED (`docs/reference/token-usage-estimates.md`).** Eleven measured advisory rows replace the provisional "not yet measured" advisory ranges (one run lands in roughly 33k-103k total tokens), with four measured lessons: total cost is dominated by target size rather than model tier, the model/effort quality gradient matched the predicted ordering, effort moves tool use and wall-clock more than raw tokens (with the Haiku low-to-medium step the highest-leverage quality buy), and - from the completed same-target model triple - the model-tier boundary on knowledge-content review is categorical: Opus/high and Sonnet/high both caught the target's factual/legal errors (Sonnet at ~70% of the Opus cost, with two additional verified majors), while Haiku at medium AND high effort could not (it declared "no veracity gaps", then at high effort confabulated a correction); a cheaper tier's "verified" is not verification, and a two-frontier panel beats either model alone. The authoring (`askit-build-*`) ranges remain unmeasured.

### Changed (calibrations)
- **U5 (description-score) recalibrated against the eval-run corpus (ADR 0033).** The scorer now tracks "would this description trigger correctly" instead of "did it use the scorer's exact word list": the action lexicon accepts verb inflections (creating / created) and the action verbs real high-quality descriptions use (draft, review, diagnose, "Help users [verb]", and others, each corpus-cited); the use-when trigger recognizes "whenever" and "if the user asks/mentions" phrasings; and the blanket all-caps penalty that dinged legitimate trigger acronyms (GDPR) is replaced by a hard penalty on placeholder tokens (TODO / TBD / FIXME), closing the inverse defect where an unfinished "TODO: write a description" scored 0.9. Threshold stays 0.7, severity stays warn, provenance stays house. Measured on five corpora: U5 warnings 98 -> 18, with the survivors verified as the intended catch (noun-led, trigger-less descriptions). +7 corpus-derived tests (395 total); gate Advanced 0/0; no spine or Standard change.

### Fixed
- **Report tables no longer crush on narrow viewports (`scripts/lib/report-render.mjs`).** At widths of 900px and below, the HTML report's table cards scroll horizontally (with a 560px table minimum) instead of compressing every column; golden render snapshots regenerated (CSS-only diff). The fuller responsive pass and a per-check explanation glossary are intaken as backlog E12 (report-template responsiveness and glossary).
- **`--profile` / `--mode` are honored in component scope (ADR 0034, #135).** `evaluate.mjs` on a single skill directory validated `--profile` at the CLI and then silently dropped it - a third-party single skill graded under `plain-plugin` still got the house checks, and `report.profile` was `undefined` (eval-run sensor reading 1). Component scope now runs the same config resolution as plugin scope (file config, then CLI overrides, per-rule override wins) and records `profile` / `mode` on the report; the skill directory is the one root for both the finding paths and the config, so a file-scoped suppression in a skill-dir `askit.config.json` matches and waives the gate exit (the Codex review round-1 catch). Default component verdicts are unchanged (the default profile is the `askit-library` identity). +6 tests (401 total).
- **The advisory delegates' docs now match the code (#135).** Four one-file fixes from eval-run sensor readings 2, 3, 6, and 7: `askit-reviewer` runs the targeted `evaluate.mjs <target> --json` instead of the whole-plugin `check.mjs` (which grades a bare skill directory as an empty plugin); the behavioral mode documents its derive-from-description fallback in `askit-evaluate` and `askit-quality-grader` (the `evals/` convention is forward-looking - no published target ships one); the behavioral `summary` counters have fixed semantics in `docs/reference/evaluation-reports.md` (every case in exactly one counter; a false fire counts as `behaviorFail`); and `askit-build-skill` has an explicit delegate step for its declared `askit-skill-author` chain (the chain-vs-procedure gap a recorded behavioral run found as a real fail). Touched components bumped to 0.1.1.

## [1.5.1] - 2026-06-10

The calibration patch from the second eval-target corpus run (the gate pointed at the official Anthropic plugin set and four community marketplaces under `--profile plain-plugin`): grading calibrations that remove systematic false positives on well-built third-party plugins, plus the verified competitive comparison and a token-usage dossier. Every grading change is to HOW a check fires under outward grading, not to WHAT the Standard requires - a plugin graded under the default `askit-library` profile is unaffected, the toolkit's own Gold grade stays Advanced 0/0, and the spine stays **29** and the Standard stays **0.11**.

### Added
- **Verified competitive comparison (#122).** A public comparison page (`docs/explanation/comparison.md`) and a dated, primary-source-verified research corpus (nine tool profiles plus a methodology and gap analysis under `docs/internal/research/`) positioning the toolkit against other skill and plugin builders and validators. Honesty-guarded: no unsourced quality claim, and the U5 description-scorer caveat is stated explicitly.
- **Token-usage dossier (`docs/reference/token-usage-estimates.md`).** A public reference on what an evaluation costs in model tokens, built on the two-regime split: the deterministic core (the gate, the tier report, and every `evaluate` render except the two advisory reports) is **zero model tokens** and reproducible, and only the optional model-assisted layer (review and behavioral advisory, `askit-build-*` authoring) has model and effort variance - which structurally cannot move the grade. Explicit on the model (Opus and Sonnet and Haiku) and effort vs quality tradeoff, with assumptions, caveats, and the measured data we have.

### Changed
- **U6 (reference-links) and U11 (mcp-valid) calibrated; the plain-plugin tier label corrected (ADR 0030, #123).** U6 strips fenced code blocks before scanning and skips non-filesystem URI schemes (`computer:`, `file:`, `ftp:`, `ws:`/`wss:`, `tel:`, `data:`, `javascript:`, protocol-relative `//`), so an illustrative link inside a fenced example or a non-relative scheme is no longer a dangling-reference error (matching the in-repo `gen-docs-site.mjs` handling). U11 treats a typed `http` server with an empty or absent url as the host-resolved managed-connector pattern (a warning, not a tier-blocking error), the way Anthropic's knowledge-work plugins ship gmail and google-calendar; a genuinely underspecified server still errors. And `tier-report` no longer asserts an askit tier for a plugin that never declared one - it reads "objective checks pass (no askit tier declared)" instead of "Advanced". Both checks keep `objective` provenance and still catch real defects.
- **U3 (frontmatter-valid) and U4 (name-matches-dir) calibrated for display-label and namespaced names (ADR 0031, #124).** A third-party plugin that uses the skill `name` as a human display label ("Writing Hookify Rules") or a namespaced command ("gsd:add-backlog") was double-flagged - U3 on the kebab format AND U4 on the name-vs-directory mismatch, on the same field (165 errors on gsd alone). U3 now format-checks `name` only when it equals the directory (a divergent name is a label; U4 owns the divergence), and U4 is advisory (a warning) under the `plain-plugin` profile while staying a hard error under the default `askit-library` ladder. A malformed canonical name (non-kebab and equal to its directory) still errors, a genuine parse defect still errors, and both checks keep `vendor-cited` provenance; the house-provenance invariant is unaffected because U4 is downgraded to `warn`, not turned off.
- **U6 (reference-links) ignores inline code; U12 (mermaid-valid) skips non-live diagrams (ADR 0032).** From the C1 triage, under one principle - a check validates only LIVE content, not authored illustrations of syntax. U6 now strips single-backtick (and multi-backtick) `inline code` spans in addition to fenced blocks, so a link or capture regex written as inline code is not parsed as a dangling reference. U12 now skips a mermaid block whose body is purely a `{{TEMPLATE_PLACEHOLDER}}` slot (the generator substitutes the real diagram) and a block commented out inside `<!-- ... -->` (which does not render); the comment handling is fence-aware so a `<!-- -->`-looking span inside a LIVE diagram is still validated. Both keep `objective` provenance and still catch real defects (a genuine dangling link outside code; a malformed live diagram). Measured on the C1 corpus, the four plugins drop from 43 errors to 12 (the rest a murkier example-link class deliberately left flagged, one of them a genuine link-rot defect).

## [1.5.0] - 2026-06-09

The outward-grading release. The first eval-target corpus run (the toolkit graded against real third-party plugins for the first time) turned the gate from self-proving to legitimately pointable at plugins you do not own. Two changes ship from it; nothing changes for a plugin graded under the default profile, so the spine stays **29** and the Standard stays **0.11**.

### Added
- **`--profile <name>` on `check.mjs` and `evaluate.mjs`.** Grade a plugin you do not own under a chosen profile (e.g. `--profile plain-plugin`) without writing `askit.config.json` into its tree. It mirrors `--mode`: a CLI value that overrides the loaded config's profile through the same resolution path (an explicit per-rule override in a present config still wins), and an unknown profile is rejected with exit 2. This closes the main friction the eval-target corpus run surfaced: pointing the gate at a third-party plugin previously graded it against the full askit library ladder, drowning portable defects in non-portable scaffolding findings. No spine or Standard change (29 checks, Standard 0.11).

### Changed
- **`U2` (root `AGENTS.md` anatomy) and `U5` (description scorer) reclassified from portable to `house` provenance (ADR 0029).** The eval-target corpus run showed both fire on well-built official targets on convention rather than defect (Anthropic's own `skills` ships no root `AGENTS.md`; good `use-when` descriptions cluster at 0.65 under the U5 scorer's 0.7 bar). As house checks they are now turned off by the `plain-plugin` profile, counted as profile conformance rather than real issues, and never clamped in published-verdict mode (a consumer may opt out of an askit convention). Both still fire under the default `askit-library` profile, so no plugin's conformance grade changes; the spine stays **29** and the Standard stays **0.11**. A new `house-provenance.test.mjs` invariant asserts the `plain-plugin` off-set equals the house-provenance set so the two never drift.

## [1.4.1] - 2026-06-09

A hardening patch over `v1.4.0`, from a Codex adversarial review of the new report renderer. Three defensive fixes on the advisory and migration paths; no behavior change for valid input, and no spine or Standard change (spine stays **29**, Standard stays **0.11**).

### Fixed
- **A malformed advisory no longer crashes the renderer.** An advisory block missing its `findings` (review) or `cases` / `summary` (behavioral) is normalized to empty defaults in `deriveModel`, so `--report=review|behavioral` renders a clean report instead of throwing a `TypeError`. The advisory comes from a non-deterministic LLM layer, so a missing field is expected input, not a fault in the caller.
- **The Markdown twin escapes raw HTML in every advisory interpolation.** A hostile or hallucinated advisory `model` / `effort` / `date`, finding `message`, insight, or behavioral summary is escaped (angle brackets and table pipes; newlines collapsed) before it reaches the Markdown, so a raw `<script>` or `<img onerror=...>` in an advisory file cannot survive into a rendered `.md`. The review caught that the first cut escaped the stamps and table cells but left the insights list and behavioral summary raw; both are now escaped, so the Markdown twin matches the HTML renderer (which already escaped everywhere).
- **An invalid target tier is rejected, not silently empty.** `--target-tier` is validated at the CLI (exit 2 on an unknown tier), and `migrateReport` throws on a `targetTier` outside `TIER_ORDER` instead of returning an empty migration plan that would read as "already at the target."

### Notes
- All three are defensive hardening on untrusted or edge input; the central invariant is unchanged - the advisory layer still structurally cannot move the deterministic grade or the gate exit code. 346 tests (was 341, +5 regression tests); gate Advanced 0/0. Shipped behind a focused adversarial re-review that itself completed the Markdown escaping.

## [1.4.0] - 2026-06-09

The designed evaluation report (F2 / backlog E1): one pure renderer over the single `evaluate.mjs` report object emits a self-contained HTML page or a Markdown twin, in five report types, so a conformance result is consumable by a non-engineer and a PR reviewer, not only in a terminal. No new spine check and no Standard change: the spine stays **29** and the Standard stays **0.11**; the renderer is a presentation layer over the deterministic gate.

### Added
- **Designed conformance report (Phase A, #111).** `scripts/lib/report-render.mjs` (`renderMarkdown` / `renderHtml`) plus `scripts/lib/report-meta.mjs` (a per-reqId why-it-matters / fix-prompt / effort table) render the deterministic report object as a self-contained HTML page (inline CSS, one small inline script for the TOC scroll-spy / copy-prompt buttons / print, no external assets) or a Markdown twin, following a 10-section information architecture. `scripts/evaluate.mjs` gains `--format=md|html` and `--out`; terminal and `--json` are byte-for-byte unchanged. The renderer derives the spine and Standard version live, never hard-coded.
- **Migration and release-readiness reports (Phase B, #112).** `--report=migration --target-tier=<tier>` renders a staged gap-by-tier plan (`scripts/lib/migrate-report.mjs`); `--report=release` renders a deterministic go / no-go (`scripts/lib/release-report.mjs`: a clean gate AND version-consistent manifests AND a present `RELEASE-NOTES.md`, mirroring the `release.yml` guard). Both are pure decorators over the conformance object.
- **Review and behavioral advisory reports (Phase C, #113).** `--report=review|behavioral --advisory <file.json>` renders the two advisory reports, whose content is produced by an LLM layer (`askit-reviewer` / `askit-quality-grader`) and merged onto the conformance object by an allowlisting `applyAdvisory()`. The advisory layer is clearly labeled, carries a model/effort/date provenance stamp, and structurally cannot move the deterministic grade or the gate exit code.
- **Public reference.** `docs/reference/evaluation-reports.md` documents all five report types, both formats, and the deterministic/advisory boundary.

### Notes
- The renderer is a pure projection: it adds no finding, runs no model, and never changes the tier or the gate exit code (Design Principle 3 / ADR 0023). Golden snapshots lock both formats. Each phase shipped behind a 4-lens adversarial review; the Phase C review caught a critical advisory-merge hole (an unbounded spread could let a malformed advisory file overwrite the verdict) that the `applyAdvisory` allowlist and a hostile-advisory regression test closed. 341 tests; gate Advanced 0/0. No spine or Standard change: 29 checks, Standard 0.11.

## [1.3.0] - 2026-06-06

The gate-evolution release: the deterministic gate becomes Standard-version-aware (ADR 0027) and configurable like a real linter (per-rule severity, profiles, suppressions, provenance, a published-verdict mode). No new spine check and no tier requirement, so the spine stays **29** and the Standard stays **0.11**; an unconfigured plugin grades exactly as it did in 1.2.0.

### Added
- **Standard-aware gate (F1, ADR 0027).** Every check declares the Standard version it was introduced at (`meta.since`); the gate reads `library.json.standard` and downgrades any error from a check introduced AFTER the pinned version to a `warn` (surfaced, never gate-failing), so a plugin pinning an older Standard is graded against the ruleset it adopted rather than the newest spine. Pure version arithmetic in two new leaf modules (`scripts/lib/standard-version.mjs`, `scripts/lib/standard-gate.mjs`), wired through `check.mjs` / `tier-report.mjs` / `evaluate.mjs`. A `--strict` flag grades against the full live spine for authors validating the Standard itself. `STANDARD.md` gains sec 7.7 ("Standard versioning and compatibility"); ADR 0027 is Accepted.
- **Configurable gate (F3).** An optional `askit.config.json` at the plugin root configures grading without editing source: per-rule severity (`error` / `warn` / `off`), named **profiles** (`askit-library` the default, `plain-plugin` the portable-checks-only rubric, and an opt-in empty `house-style` slot), and a **suppressions** baseline (`reqId` + file glob + optional message substring, each with a required reason). Each check carries a **`provenance`** tag (`objective` / `vendor-cited` / `house`), and the report splits "real issues" (objective + vendor-cited) from "profile conformance" (house conventions + downgrades). A **`published-verdict`** mode (the `--mode` flag) clamps an objective/vendor-cited finding a subject tried to disable back up to a `warn`, so a published conformance verdict cannot be gamed. New modules: `scripts/lib/config.mjs`, `profiles.mjs`, `resolve-config.mjs`, `suppressions.mjs`; `docs/reference/gate-config.md` documents the schema, profiles, suppressions, and the clamp.

### Notes
- No new conformance requirement: the spine stays 29 (`U1-U9`, `U11-U12`, `S1-S8`, `G1-G10`) and the Standard stays `0.11`. Config is consumer-side and non-normative for conformance (`STANDARD.md` sec 7.7). 308 tests; gate Advanced 0/0; both features shipped behind a clean 4-lens adversarial review.

## [1.2.0] - 2026-06-06

Retire the `U10` (no-dashes) house-style check from the conformance spine (Standard `v0.10 -> v0.11`, spine `30 -> 29`), and add a refined Command Dashboard v2 evaluation-report sample template.

### Removed
- **`U10` (`no-dashes`) retired from the conformance spine; Standard `0.10 -> 0.11`.** The no-em-dash / no-en-dash rule was a stylistic house preference with no portability or vendor basis (agentskills.io, Claude Code, and Codex impose no such rule), so grading third-party plugins against it was out of scope for a skill and plugin standard. `scripts/checks/no-dashes.mjs` and `tests/unit/no-dashes.test.mjs` are removed; the shared `SKIP_DIRS` set moves to `scripts/lib/fs-utils.mjs` (where `U12` mermaid-valid and `G8` folder-readme now import it); the registry, the `registry-sync` spine count (`30 -> 29`), `STANDARD.md` (spine line + a v0.11 note), `library.json` `standard`, and the live docs are updated. The dash preference remains available as an **opt-in** author-time guard: the toolkit still ships the `PreToolUse` hook in `hooks/` (so Gold `G1` stays non-vacuous), and any maintainer-level global hook is unaffected. Spine **30 -> 29** (`U1-U9`, `U11-U12`, `S1-S8`, `G1-G10`).

### Added
- **Command Dashboard v2 evaluation-report sample template.** `docs/internal/template/evaluation-report--plugin--dashboard-v2.html`: the dashboard report variant with a real font-weight hierarchy on a system-font stack, wider non-clipping matrix cells, and text-wrap balancing; a self-contained sample with a hypothetical subject (#102).

## [1.1.0] - 2026-06-03

The ADR 0024 documentation-depth and discoverability build-out: five new deterministic checks taking the spine from 25 to **30** (`U1-U12` + `S1-S8` + `G1-G10`), the full dual-audience Diataxis docs set, the generated Pattern S docs site, the demonstrative hook that makes `G1` non-vacuous, and **Standard v0.10**.

### Added
- **`docs-presence` check (Gold `G10`), STANDARD v0.10 completion, and the `1.1.0` bump [ADR 0024 P6].** `scripts/checks/docs-presence.mjs` asserts three lifecycle-rigor facts: the four Diataxis quadrants are non-empty, every ADR (`docs/internal/decisions/NNNN-*.md`) carries a `## TL;DR`, and the `doc-role: architecture-overview` page resolvably links the `architecture-detailed` page (conditional on a `docs/` tree). `STANDARD.md` is completed to v0.10 (adds `U12` Universal and `G8`-`G10` Gold normative requirements, sec 4.2, the spine line `= 30`, the v0.10 history note); `gold-checks.md` gains the `G8`-`G10` rows; the `26` / `G1-G7` wording was swept to `30` / `G1-G10` across the README, AGENTS, the docs, and the site catalog; `library.json` and `package.json` bump `1.0.0 -> 1.1.0` (native manifests + `manifest.generated.json` + `INDEX.md` regenerated). An explicit `CHECKS.length === 30` and a reqId-uniqueness assertion guard the spine. Spine 29 -> **30**.
- **`source-doc` check (Gold `G9`) + four-field source docblocks [ADR 0024 P5].** Every hand-authored `*.mjs`/`*.js`/`*.py` under `scripts/`, `site/scripts/`, and `hooks/` carries a header docblock with the four fields (what it is / what it does / why / what uses it); `scripts/checks/source-doc.mjs` asserts their presence (never prose quality) in the first lines, accepting the lowercase, uppercase, and `@`-tag styles. Spine 28 -> 29.
- **`folder-readme` check (Gold `G8`) + folder-READMEs [ADR 0024 P4].** Every meaningful folder carries a `README.md` with a frontmatter `title` and an `## Inventory` whose listed children set-equal the folder's actual children; `scripts/checks/folder-readme.mjs` reports under-listed and phantom children. The repo root (the hero) and `templates/seed-plugin/` (the seed payload) are excluded. A latent bug was fixed: a folder `README.md` is no longer mis-enumerated as a component. Spine 27 -> 28.
- **`mermaid-valid` check (Bronze `U12`) + README hero diagrams [ADR 0024 P3].** Every fenced `mermaid` block in tracked `.md`/`.mdx` is structurally validated (a recognized diagram keyword, balanced brackets ignoring quotes, no tabs); the README gains four canonical diagrams (architecture, tier-climb, eval-boundary, build-evaluate-improve). Portable structural validation; `astro-mermaid` is the render-time second layer. Spine 26 -> 27.
- **Generated Pattern S docs site [ADR 0024 P2b].** The Astro Starlight site is now a generated view of the public `docs/**` tree: `site/scripts/gen-docs-site.mjs` emits each Diataxis quadrant into `site/src/content/docs/<quadrant>/` (read by the stock `docsLoader()`), rewriting intra-repo links so the rendered site has zero browser-broken links (a sibling doc becomes a base-absolute site route; a published directory a GitHub `tree` URL; everything else a GitHub `blob` URL). Generation runs at `astro.config.mjs` load, so the autogenerated sidebar's quadrant directories exist on any entrypoint. The generated quadrants are gitignored and rebuilt every build; `site/scripts/check-generated-untracked.mjs` (wired into `ci.yml`) asserts both that every emitted quadrant is gitignored and that no generated page is tracked; `docs/internal/` is never emitted. `deploy-pages.yml` now also triggers on `docs/**`. The `askit-build-docs` `site` mode, its recipe, and the two public docs were rewritten off the superseded in-place glob loader to this mechanism (R-SITE-1..5). 68 routes, clause-14.11 link/route guards green.
- **Diataxis documentation set and frontmatter taxonomy [ADR 0024 P1].** Authored the missing dual-audience content (root QUICKSTART, the `docs/tutorials/` quadrant, glossary, FAQ, troubleshooting, an architecture overview + detailed pair) and added the `title` / `description` / `audience` / `level` (+ optional `tags` / `doc-role`) taxonomy to every existing `docs/**` page, plus the `docs/reference/frontmatter-taxonomy.md` reference. Author-only (no new check; the `G7` enforcement is P2a).
- **`docs-frontmatter` check (Gold `G7`) and Standard v0.10 [ADR 0024 P2a].** Every published `docs/**` page (excluding `docs/internal/`) MUST carry the frontmatter taxonomy (`title`, `description` with no colon-space, `audience`, `level`, optional `tags` / `doc-role`); `scripts/checks/docs-frontmatter.mjs` enforces it (conditional, advanced tier) with golden/anti fixtures and tests. The Standard reclassifies the old `G7` tier-inclusion statement (now an unnumbered structural property) and assigns `G7` to this check; sec 8.4 pins the taxonomy. Spine 25 -> 26 (`U1-U11`, `S1-S8`, `G1-G7`); `library.json` `standard` 0.9 -> 0.10; native manifests and `INDEX.md` regenerated; the 25 / `G1-G6` wording was swept to 26 / `G1-G7` across the README, AGENTS, RELEASE-NOTES, the docs, and the site catalog. Gate green at Advanced (0/0).
- **Demonstrative hook (`hooks/`): the toolkit now ships one real hook, so Gold check `G1` (hook-documentation) grades a real artifact instead of passing vacuously.** A `PreToolUse` guard on `Write|Edit|NotebookEdit` (`hooks/no-dashes.mjs`) denies any payload whose `new_string` / `content` / `new_source` contains an em-dash (U+2014) or en-dash (U+2013), emitting an actionable substitution message (replace with ' - ' or restructure); clean or unreadable payloads are allowed (a hook bug never wedges the session). It enforces the Standard's `U10` no-dash rule at author-time. Documented per sec 3.5 in `hooks/README.md` (event/trigger/matcher/scope/failure) and covered by a `G3` regression eval (`evals/no-dashes-hook.eval.json`, `covers: { "hook": "PreToolUse" }`). The script references the banned characters by character code so it stays `U10`-clean itself. This is **P0** of the ADR 0024 / `plan_v1.1.0` build-out (it sits on `main` unreleased; it reaches installers only at the next release tag). P0 adds no new check, so the spine stays at 25; it makes the existing `G1` non-vacuous. Gate green at Advanced, 211 tests.

### Fixed
Pre-tag corrections from a 4-lens adversarial review of the v1.1.0 build-out (`v1.0.0...HEAD`); full ledger in `docs/internal/release-plans/plan_v1.1.0/adversarial-review-resolutions.md`.
- **`U12` mermaid-valid no longer false-rejects validly branded diagrams (the one release blocker).** A `mermaid` block that opens with a `%%{init: ...}%%` theming directive, a `%%` comment, or a `---` YAML config block now passes: the diagram-keyword test skips those leading constructs (as Mermaid itself does) before checking the type, and `%%` comments are stripped before bracket balancing so a stray bracket in comment prose no longer fails the block. Findings now cite the offending body line, not the fence opener. Committed golden diagrams exercise all three constructs so the repo's own U12 self-scan dogfoods them; +6 regression tests.
- **`gen-docs-site` rewrites reference-style link definitions on a CRLF checkout.** `rewriteBodyLinks` now splits on `/\r?\n/`, so a `[label]: ../path` definition is rewritten instead of being left as a browser-broken relative target on Windows-authored sources (same CRLF class as the earlier fence-trim fix); +1 `transform()` test.
- **`G8` folder-readme parses the inventory over fence-stripped text**, so a list item inside a fenced authoring example no longer masks real folder drift or injects a phantom (matching `G10`'s existing behavior); +2 tests.
- **`U8` manifest-drift version disagreement is now an `error` (name stays a warn).** The portable gate is now authoritative for the exact version invariant the release tag guard enforces, so a local green `node scripts/check.mjs .` can no longer hide a native-manifest version mismatch. (A tightening of an existing check, folded into the v0.10 release; see ADR 0027.)
- **`G10` architecture-pair resolution is order-independent.** A duplicate `doc-role: architecture-overview`/`-detailed` marker is now an explicit error instead of resolving by filesystem `readdir` order; the missing-`## TL;DR` message no longer overclaims "the 3-line decision summary"; +1 test.
- **`release.yml` fails the tag when a `## <version>` RELEASE-NOTES section is missing** (was a silent `cp` of the whole changelog as the release body); the stale `ci.yml` comment attributing site generation to a non-existent `prebuild` hook is corrected.
- Recorded **ADR 0027 (Proposed): a Standard-versioning and compatibility policy** (deferred for maintainer ratification) and a full resolutions ledger covering the deferred and refuted findings. 280 tests; gate Advanced 0/0.

## [1.0.0] - 2026-06-02

The first tagged Gold release: the toolkit becomes installable through the `product-on-purpose` marketplace. It folds in everything since `v0.2.0` - the full v1 builder catalog, the Gold self-conformance gate, and full Astro site conformance (clauses 14.1-14.11) - plus the launch changes below.

### Changed
- **Repositioned the README around the plugin lifecycle - start, grow, govern, and level up an advanced cross-agent plugin - instead of reading as a skill builder, and made the tier model scannable** (a six-column "at a glance" table plus tight per-tier cards in place of the dense inline-ID prose). The repositioning keeps an explicit beginner on-ramp alongside the advanced maintainer track, and softens the "zero-dependency" overclaim to the accurate "one runtime dependency, a YAML parser" (matching `package.json` and the docs site).
- **`library.json` now declares `standard: "0.9"`** (was `0.8`), adopting the v0.9 Standard the repo amended in `STANDARD.md` (runner Node baseline `>=22.12.0`; ADR 0025). Version bumped `0.3.0` -> `1.0.0` for the marketplace launch; the native manifests, `manifest.generated.json`, and `INDEX.md` were regenerated from `library.json` (the version source of truth).
- **Astro site toolchain and branding converged to clauses 14.6/14.8 and 14.2 (full 14.1-14.11 conformance).** CI (`ci.yml`, `deploy-pages.yml`) now resolves Node via `node-version-file: .nvmrc` so the committed pin is the single CI source of truth, instead of a hardcoded `node-version: "24"` in all three `setup-node` steps; `actions/checkout` and `actions/setup-node` move to `@v5` (the Pages actions were already `@v5`). The mermaid theme is branded with `mermaidConfig.themeVariables` (`lineColor: '#5C7CFA'`, system-ui, 14px), mirroring the pm-skills reference. With the new 14.11 guards below, the site satisfies every Astro site clause. See ADR 0026 (full Astro site conformance: local 14.11 guards, node-version-file, branded mermaid).
- **The Advanced Skill Library Standard is amended to v0.9: the runner Node baseline rises from EOL Node 20 to `>=22.12.0` (recommended pin Node 24).** Node 20 reached end-of-life (2026-04-30) and is below Astro 6's floor (the basis of every family documentation site), so the published `>=20` baseline pointed downstream plugins at an unsupported runtime. STANDARD.md sec 4.1, the engines example, and the resolution log are updated; see ADR 0025. The toolkit's own `library.json` adopts `standard: "0.9"` in this 1.0.0 release (see the standard-bump entry above); the Standard sets a floor, so a plugin requiring stricter remains conformant.
- **Astro docs site converged to the family site standard (site clauses 14.1-14.10).** Dropped end-of-life Node 20 family-wide: `engines.node >=22.12.0` in `library.json`, root `package.json`, and `site/package.json`, with committed `.nvmrc` and `.node-version` pinning Node 24 (Active LTS). CI and the GitHub Pages deploy now build on Node 24, and the Pages actions are pinned to `actions/upload-pages-artifact@v5` + `actions/deploy-pages@v5`. Added a non-deploying `build-site` job to `ci.yml` so a PR proves the Astro build compiles; a `catalog-coverage` drift test (`tests/unit/catalog-coverage.test.mjs`) that keeps the hand-authored catalog page (`site/src/content/docs/catalog.md`) honest with `library.json` (every component named, no orphan `askit-` name) without flattening its editorial prose; and `site/public/robots.txt` pointing at the auto-emitted sitemap. Made the splash hero action and the `overview.md` intra-site links base-relative so the GitHub Pages base lives only in `astro.config.mjs` (clause 14.7). The site already satisfied Pattern S and the stock `docsLoader()`. Deferred to the planned shared `@product-on-purpose/astro-docs-preset`: a default `og:image`.
- **The toolkit now declares `tier: advanced` (Gold) and self-validates at Gold.** The full gate (Bronze U1-U11, Silver S1-S8, Gold G1-G7) is green and `tier-report` reports `advanced` with an empty burndown - a non-vacuous claim backed by real checks (the same discipline used at Silver). Version bumped `0.2.0` -> `0.3.0` (the Phase 4 full catalog + the Phase 5 Gold gate). `INDEX.md` regenerated for the new tier/version; README, AGENTS.md, and the conformance docs reframed Silver -> Gold.

### Added
- **Installable through the `product-on-purpose` marketplace.** The toolkit is registered as a plugin in `product-on-purpose/agent-plugins` (pinned to the `v1.0.0` tag): `/plugin marketplace add product-on-purpose/agent-plugins`, then `/plugin install agent-skills-toolkit@product-on-purpose`. The repo already shipped a valid root `.claude-plugin/plugin.json`; this release cuts the first Gold tag the registry can pin to. The one-way rule holds - the toolkit does not reference the marketplace; association lives only in the registry.
- **Standardized release CI (`.github/workflows/release.yml`).** A tag push (`v*`) mints a GitHub release with notes from `RELEASE-NOTES.md`, gated by a version-consistency guard that fails unless the tag matches every version-bearing manifest (`package.json`, `library.json`, and both native manifests). The existing `ci.yml` validate and site-build jobs and `deploy-pages.yml` are unchanged.
- **Astro site clause 14.11 (link and route integrity): two zero-dependency build-aware guards in `site/scripts/`.** `check-rendered-links.mjs` resolves every internal href against each page's real served URL in the built `dist` and validates `#anchors` (enforced via `STRICT_ANCHORS=1`); `check-route-parity.mjs` fails if a published route disappears from the committed `site/scripts/route-manifest.txt` (presence-only by design, additions allowed, `--update` regenerates). Both run in the `build-site` PR job (`if: always()`), are ported from the pm-skills reference, and preserve the standard's normative robustness contract (hard-fail an empty-but-existing `dist`, defensive `decodeURIComponent`, fail on their own assertions not on parse errors). Proven by `tests/unit/rendered-links-guard.test.mjs` + `tests/unit/route-parity-guard.test.mjs` (16 cases over crafted fixture dists, including bare-relative and single-quoted broken-link regressions). On its first run the strict guard caught a live favicon 404 (Starlight emits `<link rel="icon" href="{base}/favicon.svg">` on every page); the shared family favicon `site/public/favicon.svg` (the `#5C7CFA` three-diamond mark, reused from pm-skills) was added to resolve it. Site-scoped (not in the portable `scripts/check.mjs` gate, mirroring `catalog-coverage`). They migrate to the shared reusable workflow when it ships (ROADMAP Phase 1). See ADR 0026 (full Astro site conformance: local 14.11 guards, node-version-file, branded mermaid).
- Phase 5 Gold-check hardening from the adversarial gate (5 confirmed soundness fixes + minors): **G1** `hook-documentation` now rejects an array-valued `hooks`, errors on a non-object hook entry (was a silent skip), and validates `type` against the enum; **G2** `self-hosting` strips YAML comments before matching (a gate named only in a comment no longer passes), anchors the path (no `scripts/check.mjsx` match), and accepts `npm run`/`npm test` indirection that resolves to the gate (was a false positive against conformant plugins); **G5** `release-notes` flags an empty `RELEASE-NOTES.md` or one byte-identical to CHANGELOG; **gen-index (G4)** renders each component as a link to its path (restoring INDEX click-through navigation, the one regression from the generated-INDEX rewrite), sanitizes descriptions, and restores governance pointers (decisions/backlog/templates/chain-contract). Fixtures + tests added (`anti/hook-array`, `anti/ci-comment-only`, `golden/ci-npm-gate`); 191 tests.
- Gold-tier check G4 generated-INDEX drift (Phase 5, advanced; completes the Gold check suite G1-G7): `gen-index` now produces the COMPLETE `INDEX.md` deterministically from `library.json` + component frontmatter (name-sorted components + manifest/governance pointers), and the new `index-drift` check (`scripts/checks/index-drift.mjs`) flags a hand-edited generated INDEX as an error (line-ending-normalized to avoid CRLF flakiness), alongside U8's manifest drift. `INDEX.md` was regenerated as a clean navigational map; editorial narrative/positioning lives in `README.md`/`AGENTS.md`. Fixtures + tests (`anti/index-stale`); spine now 25 checks, 188 tests. With G1/G2/G3/G5/G6 the deterministic Gold gate is complete, so the `tier: advanced` flip is non-vacuous.
- Gold-tier checks G1, G2, G5 (Phase 5, advanced tier; complete the deterministic Gold gate alongside the existing G3 + G6): `hook-documentation` (G1, conditional on hooks existing - each hook in `hooks/hooks.json` documents its `type` per action and a `matcher` for tool-matched events); `self-hosting` (G2 - a CI workflow under `.github/workflows/` runs `node scripts/check.mjs`, so the plugin passes its own validators); `release-notes` (G5 - a curated, user-facing `RELEASE-NOTES.md` distinct from CHANGELOG). Added `RELEASE-NOTES.md`. Each ships fixtures + tests; the toolkit passes all (no hooks -> G1 vacuous, ci.yml runs the gate -> G2, RELEASE-NOTES present -> G5). With G4 (generated-INDEX drift) these complete the Gold check suite for a non-vacuous `tier: advanced` claim. Spine now 24 checks; 184 tests. `docs/reference/gold-checks.md` gains the G1/G2/G5 rows.
- Live Astro Starlight documentation site (`site/`, deployed to GitHub Pages by `.github/workflows/deploy-pages.yml`): the toolkit's own docs site, built on the pinned stack from `askit-build-docs`'s recipe (Astro 6 + `@astrojs/starlight` ~0.39 + `astro-mermaid` ~2.0 ordered mermaid-before-starlight, GitHub Pages `site`/`base`). Kept in an isolated `site/` subdir with its own `package.json` so the toolkit's core stays a zero-dependency portable spine; `npm run build` is verified green (7 pages + Pagefind search + sitemap). A curated public surface (landing, overview, the Standard, tiers, catalog, getting-started) that links out to the full `docs/` tree on GitHub; mounting the full Diataxis tree in-place is a follow-up (it needs frontmatter titles on the docs). `no-dashes` (U10) now skips build-output/tool-cache dirs (`dist`, `.astro`) so generated artifacts are not style-checked.
- `askit-template-manager` skill (Phase 4 tail / area 25): curates the global `templates/` directory the `askit-build-*` and `askit-init-*` skills consume (create/maintain modes, Standard sec 11). Keeps a template in sync with the component shape it scaffolds so a contract change is fixed in one place rather than chased across every builder; a template must produce a conformant component (the `seed-plugin/` template is asserted to pass the Bronze anatomy). Completes the Phase 4 full-catalog tail.
- `askit-deprecate` skill + the G6 `deprecation` check (Phase 4 tail / area 21, Gold G6 baseline): the skill marks a component `status: deprecated` with its replacement (`deprecated-by`) and removal target (`remove-in`) and keeps it validating until removal (deprecate/check modes, sec 3.7/7.5); the new advanced-tier check (`scripts/checks/deprecation.mjs`) enforces that contract over the library.json component entries and flags any invalid status. Conditional in effect (a plugin with only `active` components produces no findings). Fixtures + tests (`anti/deprecated-incomplete`, `golden/deprecated-complete`); `docs/reference/gold-checks.md` gains the G6 row.
- `askit-build-settings` skill (Phase 4 tail / area 14): authors and improves a plugin's settings and permissions per target (Claude Code `settings.json` permissions/env/hook registration; Codex `config.toml` subset) and recommends least-privilege allowlists, folding in the catalog's permission-advisor as a mode. Enforces least privilege and secret hygiene (env indirection, never commit secrets; Standard sec 9).
- `askit-build-statusline` skill (Phase 4 tail / area 13): authors and improves a Claude Code status line script (reads the session JSON on stdin, prints one fast line) and its `statusLine` settings registration, following the builder pattern. Claude-only (F-06 asymmetry, sec 2.3): Codex configures a built-in picker via `config.toml`, so the script targets Claude only and its absence on Codex is not a conformance failure.
- `askit-init-marketplace` skill (Phase 4, Sub-phase C / area 1): scaffolds and validates a marketplace index that catalogs plugins, in each target's native format (Claude Code `.claude-plugin/marketplace.json`; Codex `.agents/plugins/marketplace.json`, Standard sec 12). `create` scaffolds the index; `validate` checks each entry resolves to a real plugin and versions are consistent. Enforces the marketplace-vs-plugin separation rule; per D8 a plugin is registered only at its first Gold tag.
- `askit-init-plugin` skill (Phase 4, Sub-phase C / area 23): onboards a maintainer and scaffolds a starting plugin in three modes (interview, questionnaire, hybrid; ADR 0023). All three scaffold the Bronze seed anatomy from `templates/seed-plugin/` (`library.json` with the five required fields + `AGENTS.md` + README/CHANGELOG starters), which passes every Universal check with 0 errors - the structural anatomy match the new `tests/unit/init-anatomy.test.mjs` asserts (not a byte-exact diff). Ships `templates/onboarding-questionnaire.template.md` (the reusable maintainer-feedback/agent-response collaborative-doc pattern). Distinct from `askit-migrate` (adopt an existing repo); the richer surfaces are scaffolded by their own skills, so init composes the toolkit.
- `askit-build-samples` skill (Phase 4, Sub-phase B / area 19): authors and drift-validates a skill's evidence layer following the builder pattern. `create` generates `examples/` (>= 3 golden + >= 1 anti, sec 7.2) and `evals/<name>.eval.json` (a >= 20-case `{query, should_trigger}` triggering set, sec 8.3, plus chain behavior cases) in the eval-set format the G3 `library-regression` check and the `askit-evaluate` behavioral mode consume; `validate` flags drift (a changed golden, an anti-example that now fires, a stale eval) as an error. Anchors samples to the ADR 0021 example-threads (greenfield Bronze + pm-skills adopt + the toolkit as Gold).
- `askit-build-docs` skill (Phase 4, Sub-phase B / area 15): authors and improves a plugin's documentation following the builder pattern - the Diataxis doc modes (readme/quickstart/tutorial/how-to/reference/glossary/faq/troubleshooting/architecture, summary-plus-detailed for README + architecture) plus a `site` mode that stands up an Astro Starlight docs site from a pinned recipe (`references/docs-site-recipe.md`: Astro 6 + `@astrojs/starlight` ~0.39 + `astro-mermaid` ~2.0 ordered mermaid-before-starlight, the in-place `docs/` content loader, the GitHub Pages `site`/`base`, the Starlight 0.39 `items`-wrapped sidebar, and the post-build md-link strip), adapted from the `../pm-skills` stack (ADR 0021/0023). Mermaid is a validated convention, not a skill. Standing up the toolkit's own live site is a separate build-verified slice; the recipe is the contract.
- Eval engine hardening from the Sub-phase A adversarial gate (3 confirmed soundness fixes + minors): S4 (`chain-contract`) now rejects a scalar (non-list) callee in `_chain-permitted.yaml` as a contract-shape error, closing a false negative where `caller: callee` shorthand silently dropped a real edge from both S4 and the G3 regression check; G3 (`library-regression`) now reports malformed eval JSON and dangling (stale) evals even when the contract is absent or emptied, instead of an early return suppressing eval hygiene; G3 keys edges structurally (`JSON.stringify`) so a name containing the rendered arrow cannot collide, checks `covers.chain` and `covers.hook` independently (a multi-claim eval is honored, a wrong-typed claim is diagnosed), and detects stale hook coverage. A new `registry-sync` test asserts every gate check returns synchronously, guarding the deterministic/LLM boundary against a future model-calling check. Fixtures + tests added (`anti/chain-scalar-callee`, `anti/regression-orphan-evals`); 163 tests.
- Eval engine Layer 2 (the opt-in LLM-judge layer, beside the deterministic gate; ADR 0023): `askit-evaluate` gains a `behavioral` mode (run a skill against its eval-set and judge fire/no-fire + output quality, delegating to `askit-quality-grader`) and a `review` mode (qualitative review, delegating to `askit-reviewer`), alongside the default deterministic `conformance` mode. `askit-quality-grader` is un-deferred and rebuilt in its genuine behavioral-judge role (advanced tier, Claude-only) - not the `tier-report` wrapper the Phase-4 gate rejected. The two new chain edges (`askit-evaluate` -> `askit-quality-grader`, `askit-evaluate` -> `askit-reviewer`) are added to `agents/_chain-permitted.yaml` and carry eval sets under `evals/` (the G3 `library-regression` check forces this - the toolkit dogfoods its own check). Only `conformance` is gate-safe; `behavioral` and `review` produce advisory evidence and never a CI pass/fail (Design Principle 3). Closes new-components backlog N1.
- `library-regression` check (G3, Advanced tier) + the `evals/` eval-set format (Phase 4, eval-engine Layer 1 / ADR 0023): each chain edge in `agents/_chain-permitted.yaml` and each hook event in `hooks/hooks.json` MUST carry at least one eval/regression case under `evals/`, and an eval covering an edge the contract no longer permits is flagged as stale (the deterministic regression signal that a component change would otherwise silently break a chained consumer). Conditional (fires only when a chain contract or hooks exist) and advanced-tier (gates only a plugin declaring `advanced`; otherwise it is a Gold burndown item). `templates/eval-set.json` scaffold; the toolkit dogfoods it with eval sets covering its three chains. This is the G3 baseline the Standard requires; the LLM-judge engine (Layer 2) lands next as opt-in evidence beside the deterministic gate, never inside it (Design Principle 3). Docs: `docs/reference/gold-checks.md`, `docs/how-to/add-eval-coverage.md`.
- Judgment subagent roster (Phase 4, Claude-only per Standard sec 3.3): four reusable bounded delegate roles. `askit-explorer` (broad read-only repo survey, `Read`/`Grep`/`Glob`) and `askit-file-search` (pinpoint search, same tools, distinct breadth) are the discovery pair; `askit-file-ops` (`Read`/`Write`/`Edit`, no `Bash`) is the bounded mutation primitive; `askit-reviewer` (`Read`/`Bash`) is the qualitative review layer that complements the existing mechanical `askit-evaluator`. Each declares the narrowest tool set with a documented why (sec 9) and no `chain:` (leaf roles, so no S4 contract entries). Registered in `library.json` components.subagents + INDEX.md. (A fifth proposed role, `askit-quality-grader`, was dropped at the Phase-4 adversarial gate: as a deterministic `tier-report` wrapper it duplicated the evaluate path, which already returns tier + burndown. Its intended behavioral/LLM-judge role was subsequently built in the eval engine Layer 2 entry above.)
- `askit-capability-advisor` skill (Phase 4): reports which component types a target agent can run and recommends a conformance tier before building (advise/check modes, area X / Standard sec 2.3, 3.1-3.10). Encodes the Claude/Codex capability matrix (the Codex subagent constraint pinned to CLI v0.135 per Standard sec 3.3): skills/MCP/AGENTS.md portable on both; commands realize on Codex as the backing skill; subagents/output-styles/statuslines Claude-only for plugin distribution; hooks a Codex subset. `check` mode flags a Claude-only component shipped under a `codex` target. Folds in the catalog's check-agent-support.
- `askit-migrate` skill (Phase 4): adopts an existing skills repo into a conformant plugin (assess/plan/adopt modes, area 0 / Standard sec 2-5). `assess` inventories components and reports the gap by tier; `plan` orders a staged Bronze-to-Silver roadmap naming the `askit-build-*` skill or check that closes each gap; `adopt` writes the minimal canonical `library.json` + a root `AGENTS.md` so the conformance core can grade the repo. Distinct from `askit-init-plugin` (greenfield); migrate reconciles what already exists.
- `askit-release` skill (Phase 4): runs a plugin's release (version/changelog/notes/gate modes, Standard sec 4.3/7.4/10.6 + ADR 0022) - the deterministic max(component-bump) version, the CHANGELOG/RELEASE-NOTES split, and the release-readiness gate.
- `askit-decision` skill (Phase 4): authors numbered MADR ADRs + RFCs in `docs/internal/` and the mandatory 3-line TL;DR (adr/rfc/summary modes, Standard sec 10.4 + ADR 0021). `templates/adr.md` scaffold. The toolkit dogfoods it (ADRs 0020-0022).
- `askit-backlog` skill (Phase 4): manages the two backlogs (new-component proposals + enhancements, Standard sec 7.1; intake/triage/prune modes) with the why-gate that routes would-be micro-skills to enhancements.
- `askit-build-workflow` skill (Phase 3C-2d): authors workflows (`_workflows/*.md`, ordered multi-skill arcs, Standard sec 3.4; create/improve), reusing S5 (workflow skill-existence). `templates/workflow.md` scaffold. Completes the Phase 3 (Convergent) builder set: skill, evaluate, subagent, command, mcp, hook, chain-contract, agents-md, output-style, workflow.
- `askit-build-output-style` skill (Phase 3C-2d): authors Claude Code output styles (Claude-only response-mode definitions, Standard sec 2.3; create/improve). States the Claude-only asymmetry plainly (F-06). `templates/output-style.md` scaffold.
- `askit-build-agents-md` skill (Phase 3C-2d): authors and syncs a plugin's `AGENTS.md` (Standard sec 3.10; create/improve), with brevity guidance from the ETH-Zurich finding that verbose context files reduce agent task success. Reuses U2 + the `sync-agents-md` generator.
- `askit-build-chain-contract` skill (Phase 3C-2d): authors a plugin's chain contract (`agents/_chain-permitted.yaml`, Standard sec 3.6; create/improve) and resolves S4 orphan/phantom findings. `templates/chain-permitted.yaml` scaffold.
- `askit-build-hook` skill (Phase 3C-2d): authors event-driven hooks (Advanced tier; create/improve modes) per Standard sec 3.5 - event/trigger/scope/failure documentation, actionable block messages, idempotency, least privilege. Codex hook ingestion is verified at build time (known caveat); the deterministic hook check ships with Gold (Phase 5). `templates/hooks.json` scaffold.
- `askit-build-mcp` skill (Phase 3C-2c): authors a plugin's MCP servers as one portable `.mcp.json` and wires the per-target `mcpServers` manifest pointer (create/improve modes); `templates/mcp.json` scaffold.
- U11 `mcp-valid` check (Universal): each `.mcp.json` server is well-formed (stdio has `command`, http has `url`) and commits no inline secret. S3 now validates `components.mcpServers` against `.mcp.json`; S6 gains a component-level rule requiring the per-target `mcpServers` pointer when `.mcp.json` ships. `gen-manifest` emits the pointer + indexes servers (conditional, so the toolkit's own manifests are unaffected).
- U9 `version-match` check: `package.json` version MUST equal `library.json` version (the version source of truth, Standard sec 5); reconciled `package.json` to 0.2.0 so the two manifests agree.
- U10 `no-dashes` check: the no-em-dash / no-en-dash house style is now CI-enforced by a portable check (scans committed `.md`/`.mjs`, built from code points), not only a maintainer-local hook, so the rule travels with the repo for every contributor.
- `license: Apache-2.0` declared in `library.json` and `package.json`, with a `LICENSE` file (canonical Apache-2.0 text).
- S2 prefix check extended: it now enforces the `askit-` prefix on every component name (skills, commands, subagents), not only the `library.json` `prefix` field.
- `askit-build-command` skill (Claude slash-command authoring: create/improve).
- `docs/reference/builder-pattern.md` - the shared builder-skill contract.
- `/askit-evaluate` + `/askit-build-skill` commands (cross-agent invocation parity).
- S3 now validates `library.json components.commands`.
- S7 `command-contract` check: a command must map to exactly one on-disk skill/workflow and declare a non-empty description (conditional on commands existing).
- `manifest.generated.json` now indexes commands.
- `askit-build-subagent` skill (Claude subagent authoring: create/improve).
- The toolkit's own `skill-author` and `evaluator` subagents and its first chain
  contract (`agents/_chain-permitted.yaml`), making the Silver claim non-vacuous.
- S3 now validates `library.json components.subagents` against disk (each declared
  subagent must exist on disk and each on-disk subagent must be declared).
- S4 chain-contract orphan detection: a frontmatter `chain:` invocation not
  permitted by `agents/_chain-permitted.yaml` is flagged as an orphan (S4 error).
  Phantom detection (a contract entry naming a missing component) was already
  present; both modes are now active.

### Changed
- Subagents renamed to carry the `askit-` prefix (`skill-author` -> `askit-skill-author`, `evaluator` -> `askit-evaluator`) for cross-agent future-proofing (ADR 0020 O-2: Gemini extensions ship subagents, so they will enter a cross-agent pool). The chain contract, `library.json` components index, skill `chain:` refs, native manifests, and docs were updated in lockstep.
- STANDARD sec 3.9/10.1: corrected the Codex MCP registration mechanism. A distributed plugin registers MCP via a bundled `.mcp.json` at the plugin root (the portable `{ "mcpServers": {...} }` format) referenced by each native manifest's `mcpServers` pointer; the user-level `config.toml` `mcp_servers` table is a separate, non-plugin path. (Previously sec 3.9 wrongly said the plugin's `config.toml`.)
- STANDARD sec 3.3/10.1: Codex subagents are a user/project `config.toml` concern,
  not a plugin-distributable component (Codex v0.135). Subagents are Claude-only for
  plugin distribution; declare `agent-targets: [claude]` in the subagent frontmatter.

## [0.2.0] - 2026-05-28

### Changed
- The toolkit now declares and satisfies Silver: `library.json` `tier: convergent` with a `components` index (closes S3). All Convergent checks (S1-S6) gate green; `tier-report` reports `convergent` with an empty burndown. Version bumped to 0.2.0.

### Added
- Validation spine (Node, zero test-framework deps): `loadPlugin` context, `Finding` model, and a `yaml`-backed frontmatter parser.
- Eight Universal (Bronze) conformance checks (U1-U8): library.json schema, AGENTS.md presence, skill frontmatter validity, name-equals-directory, description scoring (0.7 warn), reference-link resolution, instruction-budget warning, and native-manifest drift. Composed behind a `checkAgentskills` seam (reimplemented per decision Q1.1).
- `tier-report` (machine JSON + human line; satisfied tier capped at the declared tier, blocked list keyed to requirement IDs) and an aggregate gate runner (`scripts/check.mjs`) that fails on error and surfaces warnings.
- Generators: `gen-manifest`, `gen-index`, and `sync-agents-md` (render functions; AGENTS.md write-back deferred to Silver).
- CI wired to run unit tests and the conformance gate via scripts only (local/CI parity).
- The repository now self-validates: it declares `tier: universal` and passes its own Bronze checks (`tests/unit/seed-bronze.test.mjs`), retiring risk R1.
- `scripts/evaluate.mjs`: conformance-core assessment report (plugin and component scope) composed over the checks; scope auto-detection; CLI + `--json`; npm `evaluate` script.
- `askit-evaluate` skill: natural-language front door that runs the assessment and presents per-rule findings, tier, and remediation.
- `askit-build-skill` skill: `create` and `improve` modes that author conformant skills and consume the evaluate report to fix findings (Claude target; subagent delegation and multi-agent emission deferred to Phase 3).
- `templates/SKILL.md` skeleton; `manifest.generated.json`; refreshed `INDEX.md` + `AGENTS.md` for the proof-loop skills.
- Widened the U5 description heuristic to recognize evaluate/assess/audit/check/report as action verbs (surfaced by dogfooding the toolkit's own evaluate skill).
- Dual documentation: per-skill `SKILL.md` + `README.md` + `references/`, and Diataxis `docs/how-to` + `docs/reference` + `docs/explanation` for the build/evaluate loop.
- The toolkit now contains and self-validates its own skills at Bronze - the `build-skill` -> `evaluate` -> `improve` proof loop works end to end (verified by a recorded manual dogfood).
- Standard revised to v0.8: Codex emission contract updated to Codex's native plugin + marketplace model (sec 3.2/3.3/3.9/10.1 + Appendix A, per 2026-05-27 spike against Codex CLI v0.133); sec 12 describes Codex's concrete native marketplace alongside Claude's.
- Five Convergent (Silver) conformance checks (S1-S5): agent-targets, prefix, components-index drift, chain-contract integrity (phantom + workflows-without-contract), workflow skill-existence. Composed via the existing registry; tagged with the `S` reqId prefix so tier-report buckets them into the convergent tier automatically.
- `scripts/lib/tier.mjs`: shared TIER_ORDER + tierForReq + ceilingIndex helpers used by tier-report and the gate runner.
- Gate runner (`check.mjs`) filters errorCount by the declared-tier ceiling: convergent errors on a Bronze plugin appear in the printed findings + `tier-report.blocked.convergent`, but do not fail CI. This is the milestone-validity model on the gate side.
- The repository's own `tier-report` now prints the Silver burndown - exactly the items 3B/3C must close to declare `tier: convergent`.
- Documentation: `docs/reference/silver-checks.md` (per-rule reference), `docs/how-to/climb-from-bronze-to-silver.md` (walkthrough), `conformance-and-tiers.md` extended with Silver subsection + visible-burndown, `askit-evaluate.md` notes multi-tier findings, AGENTS.md updated.
- library.json bumped to standard 0.8 with prefix askit- declared.
- Multi-agent emission spine (Phase 3B): `gen-manifest.mjs` now emits `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json` from `library.json` via a `--target=resolved|claude|codex|all` flag; the Claude manifest is now generated rather than hand-authored.
- `library.json` declares `agent-targets: ["claude", "codex"]` (closes S1) and carries canonical `author`/`keywords` for both native manifests.
- S6 (per-target-presence) Convergent check: each declared `agent-targets` entry must have its native manifest on disk (conditional on `agent-targets`). U8 manifest-drift extended to cover the Codex manifest.
- `askit-build-skill` documents multi-agent emission (`library.json.agent-targets` + `gen-manifest.mjs --target=...`).
- Local Codex round-trip integration test (`tests/integration/codex-roundtrip.test.mjs`): installs the emitted manifest into a throwaway local marketplace against the real `codex` CLI and asserts the skill is ingested (not just listed); runs locally, skips in CI (set `CODEX_REQUIRED=1` to fail instead). Re-verified against Codex CLI v0.135.0.
- Docs: `docs/how-to/emit-for-multiple-agents.md`; `silver-checks.md` and `conformance-and-tiers.md` add S6; AGENTS.md and INDEX.md note generated native manifests.
- The repository's Silver burndown shrank from `[S1, S3]` to `[S3]` - only the components index remains before declaring `tier: convergent` (3C).

## [0.1.0] - 2026-05-26

Phase 0 - repository bootstrap. The hand-authored Universal (Bronze) seed and the
promoted Standard. Not installable; not self-validating yet (see
`docs/internal/BOOTSTRAP.md` for the bounded exemption).

### Added
- `STANDARD.md` - the Advanced Skill Library Standard (v0.7), promoted to the
  repository root.
- `library.json` - authored canonical cross-agent manifest (`tier: universal`).
- `.claude-plugin/plugin.json` - Claude Code native manifest (hand-authored seed).
- `AGENTS.md` - agent navigation entrypoint (Standard sec 3.10).
- `INDEX.md` - human map of the repository.
- `README.md` - overview, positioning, and quickstart pointer.
- `docs/internal/DESIGN.md` - consolidated design and decision record (D1-D19).
- `docs/internal/BOOTSTRAP.md` - the self-hosting bootstrap exemption.
- `docs/internal/{decisions,rfcs,backlog}/` - governance directories.
- `.github/workflows/ci.yml` - CI stub that will shell out only to scripts.
- `skills/`, `scripts/`, `templates/` - placeholders for later phases.

[Unreleased]: https://github.com/product-on-purpose/agent-skills-toolkit/compare/v1.1.0...HEAD
[1.1.0]: https://github.com/product-on-purpose/agent-skills-toolkit/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/product-on-purpose/agent-skills-toolkit/releases/tag/v1.0.0
[0.2.0]: https://github.com/product-on-purpose/agent-skills-toolkit/releases/tag/v0.2.0
[0.1.0]: https://github.com/product-on-purpose/agent-skills-toolkit/releases/tag/v0.1.0
