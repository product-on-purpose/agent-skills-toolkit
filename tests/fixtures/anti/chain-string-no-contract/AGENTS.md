# chain-string-no-contract
The only chaining signal in this fixture is a `metadata.chain` STRING declaration; no
`agents/_chain-permitted.yaml` exists. Used to exercise the ADR 0041 scheduled-tightening
warn path (see `tests/unit/chain-contract.test.mjs`).
