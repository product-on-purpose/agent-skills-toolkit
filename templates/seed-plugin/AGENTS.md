# AGENTS.md

Agent navigation entrypoint for this plugin (the agent-facing counterpart to `INDEX.md`).

## What this is

REPLACE - one or two lines on what this plugin is and the conventions an agent should follow.

## Components

- **Skills:** none yet. Add the first with `askit-build-skill` (create mode).

## Build and validate

- Validate conformance: `node scripts/evaluate.mjs .` (once a validation spine is present).
